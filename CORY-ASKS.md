# CORY ASKS — every request Cory made, who owns it, and whether he got it

**Cory, 2026-08-17:** *"when I type things to you Im not necessarily expecting
you to do it but youre the project manager so you need to deligate effectively
but also make sure i get what I want."*

That is two jobs, and this file is the second one. `ROUTES.md` tracks what was
**assigned**. `DEFECT-REGISTER.md` tracks what is **broken**. Neither tracks
**what Cory asked for and whether it arrived** — so on 2026-08-17 an ask went a
full day with no owner, no row and no route (row A6 below), and nothing in the
repo would ever have noticed.

**The rule: an ask is not done when someone starts it. It is done when Cory has
the thing.** `DELEGATED` is a status, not a finish line.

**Status words:** `ASKED` = captured, not yet routed · `DELEGATED` = has an owner
and a deadline · `DELIVERED` = the work exists · `VERIFIED` = the relay confirmed
it does what Cory asked · `CORY` = waiting on Cory, nobody else can move it.

`draft/tests/test_cory_asks.py` fails on any row without an owner and a status.

---

## CORY RULED 2026-08-17 — executing

| # | ruling | status |
|---|---|---|
| ① | **Projection source — "RUN IT"** | ✅ **DONE.** `sleeper-hist-proj.yml` dispatched **from `main`** so the verdict commits this time (the 08-16 runs were dispatched off a worktree branch and the guard discarded the answer). Next: the three-way prereg, then Sleeper vs FP vs blend on 2025, matched population. |
| ② | **Ceiling weight — "lets see the blast radius"** | 🔄 **IN HAND, and the structure already changes the question — see below.** |
| ③ | **ADP-sd ratchet — "leave it"** | ✅ **CLOSED.** No change; revisit post-season. |
| ④ | **Session E — "already started"** | ✅ **LAUNCHED.** Lane, inbox, charter and Rule 3e all live. |
| ⑤ | **Model assignments — "do it"** | ✅ **RECORDED** in `OPERATING-MODEL.md` Rule 5b. A/D/E Opus; B/C Sonnet. |
| ⑥ | **War-room screenshots — "sent B already"** | ✅ B is unblocked. Register row 4b's blocker is cleared; 4c–4f (the truth defects) still stand. |

### ② THE CEILING BLAST RADIUS — what the code already settles

**The term is POSITION-NORMALISED.** `engine.js:1217-1220` divides each player's
`proj_ceiling − proj_mean` spread by the typical spread **at that player's own
position**, then rescales to the board. So "QBs all move" — the result my first
proxy produced — is exactly the artifact the engine was built to prevent, and my
proxy was wrong for the reason I flagged rather than by luck. The code's own note
records that dividing by median spread would have handed QBs a 2.35× boost.

**And the term is LATE-ONLY.** `CEILING_LATE_FROM: 0.6` (`engine.js:346`): the
ceiling contributes **zero until 60% of the draft**, then ramps, with a bench
gate that fires when every starting slot is full (measured near pick 70).

**So shipping a non-zero ceiling weight cannot re-rank Cory's early picks at
all.** It moves the late/throwaway rounds — the lottery-ticket picks — which is
far lower risk than "it changes every number." My earlier framing overstated it.

**STILL OWED:** the count of who moves and how far, through `recommend()`. A
direct call to `upsideBonus` returned zero for every player at every pick number,
but `_ceilingScales` is initialised inside `recommend()`, so that path proves
nothing — **no known-positive control, therefore not a finding.** Owner: relay.

## ✅ ANSWERED 2026-08-17 — SLEEPER vs FANTASYPROS vs BLEND

**Cory asked this four times over two days. Here is the measurement.**
Run `32051713260`, dispatched from `main`, matched population **376 players**,
Spearman within position (higher is better):

| arm | QB | RB | WR | TE |
|---|---|---|---|---|
| **SLEEPER** | **.7860** | **.7896** | .7385 | **.7964** |
| **FantasyPros** | .7531 | .7753 | .7409 | .7710 |
| BLEND-0.25 | .7709 | .7802 | .7444 | .7814 |
| BLEND-0.50 *(Cory's 50/50)* | .7812 | .7902 | .7439 | .7914 |
| BLEND-0.75 | .7899 | .7896 | **.7442** | .7937 |
| NAIVE *(control — must lose)* | .7018 | .7326 | .6961 | .7592 |

> **VERDICT: NO SEPARATION.** The best blend (w=0.75) beats both sources in only
> **2 of 4** positions; the prereg required 3. **The board keeps Sleeper.**

**What it means, stated plainly:**

1. **Sleeper alone beats FantasyPros at QB, RB and TE.** FP wins only WR, by
   .0024. **Using Sleeper was the right default — it was simply never a decision
   anyone made, which is what Cory was angry about, and rightly.**
2. **Blending helps but not enough.** BLEND-0.75 is the best WR arm on the board
   and edges both sources at QB and WR. Two positions, not three.
3. **The control passed cleanly** — NAIVE lost at every position by a wide
   margin, so the harness is sound and these numbers mean something.
4. **⚠️ THE BEST BLEND SITS AT THE EDGE OF THE GRID.** 0.75 is the top of
   {0.25, 0.50, 0.75}. **The optimum is not bracketed** — it plausibly sits at
   0.80–0.90, which was never tested. That is a live edge candidate with a cheap
   test, and the prereg's own edge-of-grid rule says so rather than letting us
   quietly call 0.75 "the answer."

**✅ SCORING PARITY VERIFIED (Cory asked: *"and we made sure fantasy pros
projections were for our league scoring before comparing?"*).** Both sources are
scored from STATLINES through `score_stat_line` against our house table — neither
provider's own points are used. But `_FP_STAT_MAP` covered only 9 of 32 priced
categories. Eighteen gaps were K/DEF and irrelevant to a QB/RB/WR/TE grade; five
were skill categories Sleeper carries (`pass_2pt`, `rush_2pt`, `rec_2pt`,
`fum_rec`, `fum_rec_td`) — a systematic tilt against FP inside a head-to-head that
had FP losing. **The map was extended to all 14 priced skill categories and the
run repeated: EVERY NUMBER IS IDENTICAL to four decimals.** FantasyPros does not
publish those fields at all, so there was nothing to drop. **The concern was real
in principle and zero in fact — and the verdict now rests on a scoring map proven
complete rather than assumed complete.** `test_fp_stat_map_coverage.py` fails if a
priced skill category ever goes unparsed again.

**NEXT (post-08-22, per the prereg's shipping cap):** extend the grid to
0.80/0.85/0.90 and re-run. One season, n≈376 — the most this licenses is
"adopt for 2026 and re-test when 2023/2024 become gradeable."

## CORY RULED 2026-08-17 (evening)

| # | ruling | what it means |
|---|---|---|
| ⑦ | **`ceiling` weight → NON-ZERO. "IS THIS STUDIES? IF SO, YES."** | Yes, studies: three preregistered runs, two independent seed sets, every value 0.15–0.65 beats zero, 3/3 separable. **"Should it be higher?" — NO, and there is evidence.** `FRONTIER.md` exp 21 found an INVERTED-U across 150 paired rooms on his real keeper base: λ=0.25 **+$44/season**, λ=0.5 **+$56** (CI [33, 78]), λ=2 **−$18**, λ=3 **−$27** with CI excluding zero. `POLICY-TOURNAMENT.md` §5 reproduced the shape independently. **0.45 sits at the measured peak; higher moves toward the negative arm.** Caveat that stands: both ran in the simulated-room proxy on the v1 money model, not held-out seasons. |
| ⑧ | **E owns input policy — "YES"** | Confirmed rather than inherited from the relay. E decides which sources feed the board and at what weight, subject to Cory; A keeps correctness and merges. |
| ⑨ | **EDGE IS DEFINED** — beat his own drafting or top 4 · more money · title >3/10 · playoffs ≥50% · PF > league average. **Any one counts.** | `EDGE-DEFINITION.md`. **This is the target nobody had written down.** It exposes that almost everything we measure is ACCURACY (MAE, Spearman) and not one of his five is. Three of the five are reachable with machinery that already exists. |

## OPEN — Cory has not got this yet

| # | what Cory asked for | owner | status | what "done" looks like |
|---|---|---|---|---|
| A1 | **War room redesign**, *"too busy and wordy… a professional draft buddy"* — **and it must be the DESKTOP screen**, which is where Cory drafts. | **B** | 🔴 DELEGATED — draft-critical, 08-22 | Desktop-first. Fix density by hierarchy, NOT by deleting data. **Four truth defects found in B's own screenshot are blocking regardless of the redesign** — register rows 4c–4f, of which 4c (every pick number computed for the wrong seat) invalidates the page. |
| A2 | **Decide the projection source: Sleeper, FantasyPros, or a mix.** | **A** | ✅ CLOSED — **A RULED 08-18: SLEEPER STAYS THE BOARD BASELINE THROUGH THE DRAFT; the FP question moves to the weekly lab.** The ruling is the one Cory's own orders produced, now backed by graded evidence on both sides: (1) Cory pre-authorized switching to our own projection only if V7 beat the bar (A11) — V7 ran three times and V8 once, and NOTHING met it (usage/efficiency 0/3 folds, C4/C6 double null with the C6 costume catch, V8's season-grain WR correction a two-fold null with the champion surviving best-of-K). (2) The three-way source-blend grade ran (SOURCE-BLEND-2025-PREREG, cited in P95): the blend edges Sleeper at some positions but the live board already carries the sourceGapCaveat surface for exactly the FP-vs-Sleeper divergences that matter in Cory's pick window, and no preregistered study earned a baseline swap four days before the draft. (3) The V8 per-game finding says our model's real WR information is production-shaped — start/sit value, not draft-board value — so the place FP/own source choice gets decided PROPERLY is the weekly lab from week 1, where Cory's 09-15 goal is graded. E's QB-board and WR-order evidence (08-17, TO:A) consumed into this ruling. A rules on source policy. The finding that forces it: `proj_mean` is Sleeper × adjuster, and FP never enters it (register row 21). **📐 NEW EVIDENCE FOR THIS RULING, 2026-08-18 (relay) — the disagreement is POSITION-SHAPED, and RB is the outlier by 3-5×.** Measured in Cory's actual pick window (ADP 27-160, the 123 players there carrying BOTH sources; `proj_mean == proj_sleeper` for **123 of 123**): **RB n=37 — median FantasyPros − Sleeper = +21.4 pts (+15.3%), FP higher for 33 of 37.** WR n=48 → +6.1 (+4.3%). QB n=21 → +4.8 (+1.4%). TE n=17 → +4.2 (+3.0%). **So "Sleeper vs FantasyPros" is not one decision — at WR/TE/QB the two sources are within a few percent and the choice barely moves a pick; at RB they differ by a sixth of a season and it moves ranks by dozens of slots.** **AND THIS IS THE MECHANISM UNDER REGISTER ROW 2c** (*high-upside young RBs are under-ranked*), which I re-measured today and confirmed TRUE: RJ Harvey board **144** vs ADP **83**, Bhayshul Tuten board **94** vs ADP **56**. Their Sleeper numbers are 125.6 and 158.8; FantasyPros says **145.2** and **192.5**. Both sit below the RB replacement level of 179.3 on Sleeper's number, which is exactly why they rank where they do. **The market is closer to FantasyPros on these players.** **⚠️ WHAT THIS IS NOT: EVIDENCE THAT FANTASYPROS IS RIGHT.** `ROUTES.md:1662` states the binding constraint — *"Sleeper/FantasyPros past accuracy is unmeasurable — 2026 is the first gradeable season."* This measures the SIZE and SHAPE of the disagreement, not which side of it is correct, and no amount of staring at it will settle that before 08-22. **WHAT IT DOES CHANGE: the ruling can be POSITION-SCOPED.** A blanket source choice pays the RB cost or takes the RB risk across the whole board; ruling RB separately is a much smaller commitment, and RB is where Cory's picks 33-93 mostly land. **⚠️ CORRECTION TO THE EVIDENCE ABOVE, SAME DAY, BEFORE ANYONE RULES ON IT — I TRIANGULATED AGAINST OUR OWN MODEL AND IT DOES NOT SUPPORT THE READING.** The RB gap above is real, but I let it drift toward *"Sleeper under-projects RBs and the market agrees with FantasyPros"*. **Our own model — the source P39 grades BEST at RB (ρ .7968 vs FP .7649 vs Sleeper .7538) — sits BELOW Sleeper, not above: median `proj_ownmodel − proj_sleeper` = −20.1, higher for only 11 of 36.** FantasyPros is above Sleeper for 33 of 37; our model is below it for 25 of 36. **The two non-Sleeper sources disagree with Sleeper in OPPOSITE DIRECTIONS — only 11 of 36 RBs (31%) have both above it, and 21 of 36 are split.** **THE CRUX, AND IT IS A CATEGORY ERROR I NEARLY SHIPPED: ρ IS AN ORDERING STATEMENT AND MY +15.3% IS A LEVEL STATEMENT.** Spearman is rank-based and therefore scale-invariant — *"we are better at RB"* means our model ORDERS RBs better and says nothing about whose point totals are right. **Those are different currencies and they do not add up to "Sleeper is low at RB".** **WHAT SURVIVES:** the source rankings genuinely REVERSE between RB (ours .7968 > FP .7649 > Sleeper .7538) and WR (Sleeper .7724 > ours .7634 ≈ FP .7621) — **that is graded, and it is the real argument for ruling per position** — and the RB disagreement is far larger than any other position's, so RB is where a ruling is worth making. **WHAT DOES NOT SURVIVE: any claim about the DIRECTION Sleeper is wrong at RB.** **✅ AND HERE IS THE SAME QUESTION ASKED IN THE RIGHT CURRENCY — ORDERING, not level — which is what ρ actually measures. This is the version to rule on.** Ranking the 36 RBs in Cory's window by each source: **Spearman(Sleeper order, our order) = 0.727, with 11 of 36 moving 8+ slots.** The orderings genuinely differ, and **P39's graded numbers say ours is the better one at RB (.7968 vs Sleeper's .7538).** No level claim is involved — this is rank against rank. **WHERE IT MOVES A PICK CORY CAN MAKE (Sleeper-rank → our rank):** Zach Charbonnet **33 → 14** (ADP 137.7) · Woody Marks **30 → 16** (ADP 154.3) · Rico Dowdle **20 → 9** (ADP 79.3) · RJ Harvey **24 → 13** (ADP 83.0). **Every one is available inside his pick range (33-148), and the board is ranking them by the ordering that grades worse.** In the other direction: Bhayshul Tuten **12 → 34** (ADP 56.3) · Rhamondre Stevenson **13 → 26** · David Montgomery **8 → 17**. **THIS ALSO HALF-CORRECTS REGISTER 2c.** That row names Harvey AND Tuten as under-ranked young RBs. **Our best-grading RB source agrees on Harvey (24→13, we like him more) and flatly disagrees on Tuten (12→34, we like him much less).** 2c should be re-scoped to the players our own ordering actually supports. **AND THE WR CONTRAST IS THE STRONGEST ARGUMENT FOR RULING PER POSITION:** at WR the two orderings are **ρ 0.294** — 29 of 48 move 8+ slots, near-total disagreement — **but at WR Sleeper is the source that grades BEST (.7724 vs ours .7634), so the board's Sleeper-only choice is already the right one there.** Same board policy, opposite correctness, adjacent positions. **🛑 SECOND CORRECTION, AND IT PULLS MY OWN RECOMMENDATION — I WENT TO THE ARTIFACT BEHIND ρ AND IT DOES NOT SAY WHAT P39'S PROSE SAYS.** **(1) P39's per-source numbers are UNTRACEABLE.** `.7538` (Sleeper RB), `.7649` (FP RB), `.7724` (Sleeper WR), `.7621` (FP WR) **appear in no committed artifact** — every grep hit is an unrelated `rec_epa`/`wopr`/`rush_epa` coincidence. **(2) THE COMMITTED ARTIFACT CONTRADICTS THEM AT WR.** `draft/backtest/model_accuracy_v6.json` reports `spearman_beats_both = TRUE at ALL FOUR positions` — QB .7225 vs .7213 · RB .7968 vs **.7682** · **WR .7634 vs .7344** · TE .7987 vs .7871. P39 says we LOSE at WR to Sleeper's .7724; the artifact says we beat the best baseline there by .029. **They cannot both be right.** **(3) THE ARTIFACT NEVER NAMES ITS BASELINES** — zero occurrences of `sleeper` or `fantasypros` anywhere in it, only `best_baseline_spearman`. **So "our model beats Sleeper at RB" is not a claim this artifact can support: it does not record which source the baseline was.** **(4) AND ITS OWN HONESTY NOTE SHOULD DECIDE THIS BEFORE 08-22:** `graded_season: 2025` — **ONE season** — and *"third candidate graded against 2025 in this lineage (v4, v5, v6)... **2025 has now been read three times; the January 2027 grade is the first untouched evaluation**"*. **SO I WITHDRAW THE PER-POSITION RECOMMENDATION ABOVE.** I told A to order RB by `proj_ownmodel` and keep Sleeper at WR *"because that is what the graded ρ says"*. **The artifact says our model beats both baselines at EVERY position, which removes the asymmetry the recommendation was built on** — and it rests on one season, read three times, with unnamed baselines. **Not a foundation for changing the value term four days before the draft.** **WHAT STANDS, because it needs no ρ:** the orderings differ materially (RB ρ 0.727 Sleeper-vs-ours, WR ρ 0.294) and the named RB movers are facts about the board. **What I no longer claim is that we know which ordering is better.**  **🔑 RESOLVED, 2026-08-18 — AND THE ANSWER IS THAT THE COMPARISON HAS NEVER BEEN MADE.** I traced P39's claim to its artifacts. **(1) THE PROMOTION BAR IS NOT ABOUT SLEEPER.** `model_accuracy_v5/v6.promotion_bar.bar` reads *"beat BOTH **naive** baselines at ALL four positions on BOTH metrics, shared population, leak-free walk-forward — REC-3, Cory-ratified"*. So `spearman_beats_both = True` means our model beat **two naive baselines** (a recency blend and its sibling). **It says nothing whatsoever about Sleeper.** That is why no version of the artifact names Sleeper: Sleeper was never in the comparison. **(2) SLEEPER HAS NEVER BEEN GRADED AGAINST OUR MODEL ON ANY SEASON.** v5 records `api.sleeper.app: "no route"` and `api.fantasypros.com: "no route"` (probed 08-16), and states the consequence outright: *"Arm B is ungradeable on any past season; its bar — beat FP alone — **first becomes measurable at the January 2027 grade** of the frozen 2026 proj_series, which prices Sleeper, FP and own_v5 on the same season."* **(3) THE FP CELLS THAT DO EXIST ARE ON A DIFFERENT POPULATION.** v5's `fp_bar` has real FP 2025 numbers — RB ρ **.7795**, WR **.7423**, QB **.768**, TE **.7892** — against ours of .7968 / .7634 / .7188 / .7987. But its own caveat: *"FP cells are graded on FP's OWN shared population (n=57/88/141/83), v5's on OURS (n=58/99/150/84)... **Per-player FP comparison is impossible from committed disk** — the archive rows were never committed and api.fantasypros.com is unreachable."* **And these are not P39's numbers either** (P39 cites FP RB .7649 / WR .7621; v5 says .7795 / .7423), so P39's figures remain untraceable to anything. **SO THE HONEST ANSWER TO CORY'S QUESTION — *"if we are better at RBs shouldn't we use our projections?"* — IS: WE HAVE NEVER MEASURED OURSELVES AGAINST SLEEPER.** Not once, at any position, on any season. **This is not "one season read three times" as I said an hour ago. It is zero seasons.** The first comparison that can settle it is the January 2027 grade, and the artifact said so before I got here. **WHAT THAT SETTLES:** the source ruling cannot be made on accuracy evidence, because none exists for Sleeper. **It can only be made on judgement, and it should therefore hold through 08-22 under the no-change rule.** **WHAT IT MAKES URGENT INSTEAD:** the 2026 `proj_series` freeze is what makes January 2027 gradeable at all — **that capture is the whole experiment, and it is a weekly job (`weekly-proj-snapshot.yml`) that register row 41 flags as treating a missed week as a benign no-op.** A hole in that series does not just cost a week; it costs the only measurement that can ever answer this. **REVISED RECOMMENDATION: hold source policy through 08-22 under the no-change rule** — the same call A already made for the durability prior — **and make the first post-draft job naming the baselines in `model_accuracy_v6.json`, because a promotion bar that cannot say what it beat is not a bar.** **✅ RULED (A, 2026-08-18) — AND THE EVIDENCE GAP CLOSED THE SAME NIGHT: the three-way grade RAN (`sleeper_vs_fp_grade.json`, 360 shared players, season 2025, leak-gated — the first own-vs-Sleeper measurement ever; "zero seasons" was true when written and is now one).** Preregistered rule applied: QB — Sleeper wins clear and the blend LOSES (−0.014); RB — three-way wash (tie band); WR/TE — the three-way blend BEATS the best single source (+0.016 / +0.012), consistent with the measured mechanism (own_v6 decorrelated 0.64–0.90 while Sleeper|FP sit 0.93–0.97). own_v6 solo loses to Sleeper at all four. **Ruling: NOTHING SHIPS before 08-22 (the prereg fixed that before any number); post-draft, the position-scoped recommendation goes to Cory as his THIRD decision — QB pure Sleeper, WR/TE blend, RB no change. N=1 and the 7.1%-hollow caveat ride with it. Full write-up: `draft/audit/sleeper_vs_fp_grade_run_2026-08-18.md`.** |
| A3 | **Capture ALL the FantasyPros data** — projected points, ceilings, ranges, everything — not just one scalar. | **D** | DELEGATED | FP's per-player range fields walked through the eight questions. They are a real per-player upside signal, which this project has been missing entirely. Register row 22. |
| A4 | **Every session asks more questions and stops moving past things.** *"common logic shouldve told us that everyone having the same ceiling makes no sense."* | relay | **DELIVERED — verify at next surprising result** | Rule 3d shipped (`OPERATING-MODEL.md`), routed to all four lanes, and applied immediately: it reopened the Vegas null (row 18) and caught the FP-vs-FP comparison (row 19). VERIFIED only once a lane other than the relay applies it unprompted. |
| A5 | **Stop throwing data away.** *"we dont just throw out vegas odds or weekly routes because we havent seen a pattern yet."* | relay | **DELIVERED** | Rule 3c + `test_retention_rule.py`, which fails if any lane-facing doc issues a stop-the-job instruction. Known-positive control carries the exact bad text the relay shipped. |
| A8 | **Site has too many top tabs — consolidate into existing pages without losing anything.** What to Watch → matchup; Pick'em → an existing page; The Races → not its own tab; My Team + Matchup possibly merged. *"I STILL WANT ALL THOSE."* | **B** | DELEGATED — after the war room | A tab map proposed to Cory BEFORE any rebuild, then hierarchy-and-grouping (never deletion). In-season-facing, so ~week 1 rather than 08-22. |
| A6 | **Re-test EVERY adjuster now that ceiling and floor changed, and tune the auto function to change DURING the draft** by round, circumstance, position. | **A** | 🔴 DELEGATED — answered in part; the auto function EXISTS and is UNMEASURED | **The round-aware adjuster is already built**: `autoWeights()` runs Anchor/Build/Fill/Endgame phases plus four situational responses. **But its own docstring says it is NOT backtested** — every phase constant is a reasoned judgement, never measured (register 26). **And it disagrees with the composite about upside**: auto ships ceiling 0.45–0.8 while `MEASURED_WEIGHTS` ships 0, with a browser toggle deciding which Cory drafts under (register 25). Tuning needs the all-seats replay, not three drafts — post-08-22. |
| A7 | **A session that takes the macro view and red-teams the model's output** — *"our current big board has trey mcbride over justin jefferson. that makes no sense."* | relay | **DELIVERED — awaiting Cory's launch** | `SESSION-E.md`, `ROUTES.md → TO: E`, `OPERATING-MODEL.md` Rule 3e (E sits BESIDE the pipeline, gates nothing), and the lane-aware tests taught about E. Scoped as red-team-on-outputs, NOT a second PM: it raises questions with the player and number attached, and never overrides a measurement. |
| A9 | **Two decisions D put to Cory, which Cory handed to A** — 2026-08-18, verbatim: *"Send to A for answers."* (1) **what instrument grades E1**, now that the all-seats replay is measured at a ±41.8 pts/season detection floor and a PERFECT game-total oracle scores 77% of it; (2) **whether the asymmetric-environment arm is built before or after 08-22.** | **A** | 🔴 DELEGATED — routed 08-18 | A rules on both. **Q-A is the one that matters:** name the estimand that replaces the seat delta, because every null this project has published was measured on an instrument that could not have seen the effect anyway. D's REC is paired-within-room with its noise floor measured BEFORE any arm runs; DEFAULT if unruled by 08-23 is that D builds that harness. Q-B REC and DEFAULT are both *after the draft*. `ROUTES.md → TO: A`, `OPEN-QUESTIONS.md` Q13, register DS1 + 18b. |

## WAITING ON CORY — nobody else can move these

| # | what | why it is Cory's |
|---|---|---|
| A10 | **RULED — WHAT TIME THE DRAFT STARTS. Cory, 2026-08-18, verbatim: *"Yes it's 6pm"*.** The site was showing a hardcoded `6:00 PM` fallback that nobody had confirmed; it is now a ruling. **WRITTEN DOWN:** `draft/config/league_config.json` carries a `draft` block — `start_date 2026-08-22`, `start_time 6:00 PM`, `tz CDT`, Cory's words verbatim — and `preserve_local_rulings` keeps it through the nightly rebuild because Sleeper does not supply that key. **Two tests guard it**: one that the merge preserves it (the fixture) and one that the committed file actually carries it, because `preserve_local_rulings` can be correct while nobody has written the ruling down — that pair is exactly what the keeper deadline needed. **⚠️ ONE HALF IS NOT DONE AND IT IS NOT MINE: the member dashboard reads `world.config.draft_time` from the RUNTIME store, not from git.** So the league still sees the fallback until someone sets it at `/admin`. **Routed to A as item (000), above everything else in that inbox.** | **A** | ✅ **RULED — recorded; runtime value routed to A** | `league_config.json` `draft` block · `test_config_local_rulings_survive.py` (2 new tests) · `ROUTES.md → TO: A` item (000) |
| A11 | **RULED — V7 ROLLS NOW, AND PROMOTION IS PRE-AUTHORIZED. Cory, 2026-08-18, verbatim: *"okay! lets get V7 rolling and if its better, lets use it."*** The residual lab starts tonight instead of post-draft; if the challenger clears the preregistered ships-if bar on the graded folds (per-position λ>0 with CI excluding zero at ≥2 positions AND the start/sit gain survives on the startable pool AND the winner is outside the BEST-OF-K null band), it SHIPS as the published projection with no second ask. The bar itself does not move, and nothing reaches the draft board before 08-22. Recorded as AMENDMENT 1 in `RESIDUAL-ARM-PROPOSAL.md`. | it is a promotion rule for the league's published number — only Cory can pre-authorize it |
| — | **Nothing.** War room ruled 2026-08-18 (see V5 below); resources question still open. | Kept as a live section: when it is empty, no lane is idling on Cory. |
| A12 | 💰 **~49,000 API CREDITS — ABOUT HALF A MONTH'S PLAN — TO ANSWER "DOES PAYING FOR HISTORICAL PLAYER PROPS IMPROVE OUR PROJECTIONS?" The harness is built, tested and waiting on one dispatch.** `draft/audit/historical_props_study_2026-08-16.md`. It pulls weekly prop lines for 2023/2024/2025, converts each week's line to implied fantasy points under OUR scoring, sums to a season total, and grades that against `own_v6` — not the naive baselines, the harder bar you actually care about — on MAE and Spearman per position, leak-free. **Cost: ~16,356 credits per season, ~49,068 total, against a confirmed ~100,000/month plan.** **MY RULING, so nothing idles: HOLD UNTIL THE WEEK OF 08-25, and I have already recorded it that way in `ROUTES.md`.** Two reasons and neither is timidity. Nothing this study returns changes a pick on Saturday — it grades a SEASON projection, and `PROJECTION-PROGRAM-2027.md`'s first real grade is 09-15, which waiting clears easily. And spending half the month's credits in the one week we may need headroom for board rebuilds or a draft-morning re-fetch, to save four days on an answer that arrives after the event, is a bad trade in exactly one direction. **Nothing decays by waiting — the harness is identical whenever it runs.** **WHY IT IS ON YOUR DESK ANYWAY: it is your money, not mine.** If you want the answer sooner, say go and I dispatch it. | It spends real money from your plan. I can rule the timing; I should not rule the spend. |
| A14 | 🔴 **YOUR "TOO MANY RBs" AND THE NEW BOARD ARE NOW IN DIRECT CONFLICT, AND I WANT YOU TO SEE THE EVIDENCE BEFORE SATURDAY.** The multi-source mean you ruled to ship raises **RB projections +11.4% and WR +2.1%**. That moved RB replacement 179.30 → 170.47 and WR 162.60 → 171.85, and since every VORP is `proj_mean − replacement`, **every back gained ~8.8 VORP and every receiver lost ~9.3.** On the board you will actually read, the **top 48 by VORP went RB12/WR26 → RB22/WR18.** **I TOLD YOU THIS MORNING THAT THE POSITIONAL OFFSET WAS HARMLESS — *"absorbed by VORP's replacement level"*. THAT WAS WRONG.** It is absorbed within a position and not across them: a position-specific lift changes which position wins the FLEX seats (RB 21 → 24, WR 29 → 26), replacement follows, and every VORP moves. **BUT IT IS NOT A BUG, AND THAT IS THE HARD PART.** I checked whether one bad scraper carried it. **All three do it independently — CBS 1.182, ESPN 1.137, FFToday 1.108 above Sleeper on RB — while agreeing with him within 2–8% on WR.** Three separately-built forecasters think Sleeper underrates running backs. **SO THE QUESTION IS YOURS, NOT A MEASUREMENT'S: if they are right, the board ranking more backs is a CORRECTION and your complaint is about DRAFT STRATEGY — scarcity, roster construction, what wins a ten-team league — rather than about the projections. Those are different objections and only the second is a defect I can fix.** **ONE THING I CAN SEPARATE CLEANLY: the TE lift is carried by CBS ALONE (1.149 against 0.991 and 0.994).** Any tight-end conclusion off this board is one scraper wearing a consensus label — and TE is exactly the position the roster study flagged, so treat that finding as weaker than the RB one. ⚖️ **AND A SECOND MEASUREMENT DISAGREES WITH THE ONE ABOVE. I AM NOT GOING TO RESOLVE IT FOR YOU BY PICKING THE ONE THAT SUITS MY STORY.** Running the SAME engine over the pre-blend board and the published one, **12 of your 15 picks differ, and the roster goes RB10/WR1 → RB8/WR3** — i.e. on what the tool actually DRAFTS, the blend moves sharply toward the shape you have been asking for, and RB10/WR1 is register 59's exact complaint reproduced on the OLD board. **So: the VORP RANKING says the blend favours running backs (top-48 RB12→RB22), and the DRAFT WALK says it drafts fewer of them (RB10→RB8). Both are mine and they point opposite ways.** **Which do I trust? The ranking.** It is a sort — no simulated draft, nothing to be unstable. The draft walk is the strict-ADP-order probe that **register 74 caught swinging a roster by seven running backs on a single board rebuild**, and this comparison IS two board rebuilds, so I cannot separate the blend's effect from that instability. **The measurement that flatters the blend is the one I already declared unreliable, which is exactly when I should be most suspicious of quoting it.** ✅ **RESOLVED — I TESTED THE HYPOTHESIS INSTEAD OF LEAVING IT AS A STORY, AND IT HOLDS.** The reconciliation was: VORP is a LEVEL measure, VONA a SCARCITY one, so lifting every RB together raises VORP and cancels in VONA. **Measured on a stable instrument (both are SORTS at one fixed context — no simulated draft, so register 74's instability does not apply): at your first pick the VORP ranking moves +2/+7/+7 toward running backs at top-12/24/48, while the VONA ranking moves +0/+0/+1. At pick 48 it is VORP +1/+1/+4 and VONA −2/−2/−3 — VONA moves AWAY from backs.** **AND THE WAR ROOM SORTS ITS MAIN LIST BY `score`, NOT VORP** (`app.js:8201`), and `score` IS VONA at your shipped weights. **So the alarming number I gave you above — top-48 going RB12 → RB22 — describes a sort the war room does not actually put in front of you.** **ONE SURFACE IS GENUINELY AFFECTED AND IT IS WORTH KNOWING AT THE TABLE: the market-lean note.** It compares a VORP-sorted board rank to ADP, and across the blend the median shift is **RB −41.5, WR +28.0** — a ~70-rank swing between your two flex positions, the same direction for essentially every player at each. **So that note will tell you running backs are bargains and receivers are reaches, systematically, all night.** QB and TE barely move (0.0 and +4.0), and the QB/TE/K/DEF scarcity tiers sort within position where a uniform lift changes no order at all. Routed to B as a decision. **WHAT THIS DOES TO THE DECISION: the roster-shape objection to the blend is substantially weaker than I presented it an hour ago. What remains is the clean question — do you believe three independently-built scrapers over Sleeper on running backs.** **NO ASK IF YOU AGREE WITH THE SCRAPERS — the board stays as published and P124 grades them in January.** **DEFAULT: it stays.** If you think they are wrong, say so and I will center each position's blend on our own positional level, which keeps the per-player information and the ceilings while dropping the level shift — a contained change I can make before Saturday. | Whether three outside forecasters know more about running backs than Sleeper does is a football judgement, and the tilt runs against your stated instinct. |
| A15 | ℹ️ **YOUR KEEPER NUMBER IS UNDERSTATED — THE DECISION DOES NOT CHANGE, BUT DON'T BE PUT OFF BY WHAT IT SAYS. NO ACTION NEEDED FROM YOU.** The keeper sheet now reads **total surplus 30.0** (was 83.1) with **Derrick Henry at −5**, and that −5 is an artifact, not football. **All three of your keepers are still priced on Sleeper-only projections while the rest of the board moved to the multi-source mean** — the capture joined over the draftable pool, and `build.py` moves kept players out of that pool, so a keeper could never match. Their VORP is therefore measured against a replacement level that DID move (RB 179.30 → 170.47). ⭐ **NO LONGER AN ESTIMATE — I RECOVERED THE REAL NUMBERS.** The scraper rows were never lost: they are in `draft/data/ffanalytics_raw_projections.csv`, dropped only at the JOIN, so I re-scored them through the capture's own scorer with no network needed.

| player | Sleeper | CBS | ESPN | FFToday | **blend** | vorp now → real |
|---|---|---|---|---|---|---|
| Derrick Henry | 238.4 | 282.7 | 263.9 | 251.6 | **259.1** | 67.9 → **88.7** |
| Kenneth Walker | 225.5 | 221.4 | 251.5 | 236.9 | **233.8** | 55.0 → **63.4** |
| Ja'Marr Chase | 256.6 | 279.1 | 279.0 | 272.5 | **271.8** | 84.8 → **100.0** |

**Run through the REAL keeper optimizer, not arithmetic: total surplus 30.0 → 74.2, and Derrick Henry goes from −5 to +16.** Keep 1 is +40.8, keep 2 is +56.4, keep 3 is +74.2 — **every additional keeper earns its pick, which is not what the current sheet shows.** ⚠️ **And my own estimates were wrong player-by-player, which is why this was worth measuring:** I told you ~95 / ~81 / ~90 and the truth is 88.7 / 63.4 / 100.0. **Walker was the big miss — I applied the RB positional median lift and his scrapers actually sit close to Sleeper.** **KEEP ALL THREE. That was the recommendation before and it only gets stronger once they are priced like everyone else.** The join is fixed on `main`; the store needs a re-capture with scraper egress, which is routed to C with a Friday deadline. **I am telling you because keeper lock is Friday 6pm and I did not want you reading a −5 next to Derrick Henry and second-guessing it.** | Nothing — this is a heads-up, not a decision. Flagged only because the wrong number lands in front of you before an irreversible deadline. |
| A13 | ⚖️ **ONE RULING: POINTS OR ROSTER SHAPE? Everything below is re-verified on the board that published at 05:11 today, not on the earlier one.** **THE TWO CANDIDATES, both graded on the same bundle in one CI run against a control that is exact (0.00 drift):** **① `need: 1.0` — +68.6 points per seat-year**, better in 20 of 30 seats, positive in 3 of 3 seasons. **And it is the WORST of five arms on roster shape:** it takes RB from 4.77 to **5.83** against this league's top-3 mean of 4.44 (3.83 on the 15-pick seasons that match 2026), and drops WR 5.10 → 4.33. **It buys its points by drafting MORE running backs.** **② `VONA_SLOT_AWARE` — +58.2 points per seat-year**, better in 21 of 30 seats, 3 of 3 seasons, **and the BEST of five on shape** — the only arm that moves RB down and TE up toward the winners at once. **THE HONEST FRAMING: your complaint has been the roster, and the bigger graded number belongs to the arm that makes the roster worse.** **SLOT-AWARE'S SCORECARD — 3 of 4 preregistered conditions, and I will not talk myself past the 4th.** ① Collapse test that originally switched it off: **PASS**, modal share 0.9% against a 78.9% historic failure, 502 distinct values of 562, control confirms the branch ran. ② Seat replay: **PASS**. ③ Fieldability: **PASS** — 1 un-fieldable skill week against shipped's 2, and below shipped on both boards I have measured. ④ One-start pileup: **FAIL** — QB+TE 3.07 → 3.47. **That rise is entirely TE (QB actually FELL), and TE is the one position this league's winners genuinely draft more of — so I believe I mis-specified condition 4. I am not shipping on a rationalisation I formed after seeing which way it broke.** **TWO THINGS I GOT WRONG TODAY AND CORRECTED BEFORE YOU RULE:** I first said no position separates winners from losers on RB — that pooled a season with an 18-pick draft, and on the 15-pick cut that applies to Saturday **winners draft FEWER backs (3.83 vs 4.57), which is your complaint** (register 71). And I said slot-aware was *"the only arm at zero"* un-fieldable weeks — true on one board, and on the published board **no arm is at zero** (register 74). **⭐ AND HERE IS A13 AS A THING YOU CAN JUST LOOK AT — `need: 1.0` CHANGES ONLY THREE OF YOUR FIFTEEN PICKS, AND ONE OF THEM IS YOUR FIRST.** `pick 8: QB Josh Allen → WR Drake London` · `pick 53: WR Jameson Williams → WR Davante Adams` · `pick 68: RB Rhamondre Stevenson → QB Dak Prescott`. Everything else is identical. **In one sentence: shipped takes an elite QB at 8; `need: 1.0` takes a receiver at 8 and waits until pick 68 for Dak Prescott.** That is the conventional 1-QB-league play against a contrarian one, and it is the whole of what the +68.6 points buys. Roster goes RB8/WR3 → RB7/WR4 — **note that `need` does NOT fix the RB count**, both are far from the winners' 3.83. `draft/tools/need_weight_pick_diff.js`. **Caveat that travels with it (register 74): the room is drained in strict ADP order, so WHEN the tool takes a quarterback is the robust part; the specific names are not.** **MY REC: ship slot-aware, hold `need` at 0.** Full numbers: `draft/audit/roster_shape_and_slot_aware_2026-08-19.md`. **DEFAULT if you say nothing by 08-21 6pm: NOTHING SHIPS and you draft today's configuration.** **⭐ ADDED 2026-08-19 AFTER THE ABOVE WAS WRITTEN — THE LAST LINE OF THIS ROW SAYS *"points-versus-shape is a preference, not a measurement"*, AND I CAN NOW PARTLY MEASURE IT.** Replaying the shipped engine into all 30 seat-years and splitting each season total into **points ACQUIRED × share of them STARTED**: in 2023 and 2025 the engine's roster holds MORE points than the owners' (+2.1%, +5.1%) **and loses anyway, entirely on conversion** — 0.740 and 0.771 against the owners' 0.828 and 0.834. **In the same units this row already uses, that shape deficit costs 180.5 pts/seat-season in 2023 and 131.4 in 2025** (2024 is the outlier at 19.6, and it is the one season the engine simply picked worse players, −17.3% on roster points). **So the prize on the SHAPE side is roughly 2–2.6× the +68.6 that `need: 1.0` buys on the ACQUISITION side.** ⚠️ **WHAT THAT DOES AND DOES NOT SETTLE — and the distinction is the whole of it: nothing has demonstrated that any arm CAPTURES that 131–180. It is the size of the prize, not an offer.** Closing it is P127, graded 09-05, and I am deliberately not ranking the two arms by it. **What it does change is the framing of your choice: shape is not the soft option here, it is the bigger number, which strengthens the REC above (ship slot-aware, hold `need` at 0) without being new evidence for either arm specifically.** **The mechanism is also now named: `need` is the only roster-aware term and it ships at 0, so nothing penalises a pileup — one 2023 seat drafted SEVEN quarterbacks in your 1-QB league, and the live 2026 board takes RB10 instead. Both boards draw exactly ONE tight end in 30 of 30 rosters.** **This does NOT change the DEFAULT: nothing ships if you say nothing by 08-21 6pm.** Full working: `draft/audit/seat_rank_and_the_conversion_gap_2026-08-19.md`, register 87. **⭐⭐ GRADED 2026-08-19, 17 DAYS EARLY, BECAUSE THE ANSWER IS DUE BEFORE YOU RULE AND NOT AFTER (P127). THIS IS THE TABLE THAT DECIDES IT.** **BOTH candidate arms buy their points ENTIRELY by CONVERSION — neither drafts better players.** Conversion (share of roster points that reach a starting slot) against the owners' 0.828/0.826/0.834: **shipped 0.740/0.815/0.771 · `need: 1.0` 0.876/0.849/0.829 · slot-aware 0.878/0.825/0.815.** `need: 1.0` holds **99 FEWER** roster points per seat-season than shipped and starts **+68.6** more of them. **So your instinct was right and the framing was mine to fix: this was never points-versus-shape. Both arms ARE shape fixes. They differ in WHICH shape.** **AND HERE IS THE DECIDER:** **⭐ AND WHAT YOUR DEFAULT COSTS, measured 2026-08-19 (P128) — because if you say nothing you draft behind AUTO, and nobody had asked what that is worth.** Auto converts BEST of six arms but acquires least. **Where it loses, by round, per seat-season: Anchor (r1-2) −1.5 · BUILD (r3-6) −175.7 · Fill (r7-10) −64.3 · TIGHT (r11+) +84.8.** **On your real schedule that is: nothing at picks 8 and 13; the whole cost at picks 28, 33, 48, 53; and a GAIN at 108, 113, 128, 133, 148.** So Auto trades ~176 points of middle-round acquisition for ~85 late plus the best conversion of any arm — **net lineup +16.9, which is still POSITIVE.** **THIS IS NOT AN ARGUMENT TO TURN AUTO OFF. It is what your default is doing, stated in points and located on specific picks, so that accepting it is a choice rather than a silence.** The mechanism: `need` ramps 0.35 → 0.9 → 1.45, and the loss appears exactly where the weight is big enough to move a pick while the board still holds real value — which is your rounds 3-6. **Sanity check that makes me trust it: the per-round parts sum to −156.7 against the −157 measured by a completely different aggregation.** `draft/audit/auto_phase_cost_2026-08-19.md`. **⭐⭐ AND THE CLEANEST WAY TO SEE YOUR CHOICE, measured 2026-08-19 (P130) — ALL THREE ARMS PAY THE SAME PRICE IN THE MIDDLE ROUNDS AND DIFFER ONLY IN WHAT THEY EARN BACK LATE.** Roster points vs shipped, per seat-season: **⭐ FINAL PIECE, measured 2026-08-19 (P131): THE MIDDLE-ROUND COST IS NOT A DEFECT ANYONE CAN FIX — IT IS THE PRICE OF THE TRADE, AND THERE IS NO WASTE IN IT.** I classified every rounds 3-6 pick against the shipped arm at the same slot. **The entire loss is POSITION TRADES — the arm declining the best available player to fill a slot** (auto 70 trades −191.5, `need: 1.0` 62 trades −179.3, slot-aware 53 trades −180.9). **And the picks where roster fit CANNOT be the explanation — same position, different player — are POSITIVE for all three arms (+15.8, +17.7, +18.1).** So none of them is making worse middle-round picks; where they deviate for any other reason, they deviate slightly better. **WHAT THIS SETTLES: no weight you or I could choose reclaims those rounds, because there is nothing wasteful in them to reclaim. The cost is real, it is deliberate, and it is expensive only because rounds 3-6 are when the board is still good.** **SO THE WHOLE OF YOUR DECISION IS LATE-ROUND RECOVERY** — Tight +84.8 / +111.4 / **+123.9** — and slot-aware recovers the most, at picks 108, 113, 128, 133, 148. **That is the entire case for it, stated as plainly as I can: it does not draft better in the middle, it recovers better at the end.** **MY REC IS UNCHANGED (ship slot-aware, hold `need` at 0) and the DEFAULT IS UNCHANGED (nothing ships if you say nothing by 08-21 6pm).** **⚠️ ONE CAVEAT ON MY OWN REC, FOUND BY RE-CHECKING THE LIVE BOARD RATHER THAN ASSUMING IT (2026-08-19 late).** The board rebuilt twice today, and I re-ran the live-board roster drive on the newest one (08:52:22Z). **Shipped and `need: 1.0` are UNCHANGED. Slot-aware moved the wrong way: TE 2 → 1, RB 10 → 11, un-fieldable weeks 1 → 2.** So on the board as it stands tonight, slot-aware draws MORE backs than shipped and loses its second tight end — **the opposite of what I have been recommending it for.** **WHY I AM NOT CHANGING THE REC: that drive fills the room in strict ADP order, which your draft will not do, and register 74's standing ruling is that it MUST NOT be used to rank arms. The recommendation rests on the seat replay — 30 seat-seasons, controlled, and reconciled against a second independent aggregation — where slot-aware is RB 4.63 / TE 1.47.** **But I was quietly leaning on the live-board draw as corroboration, and tonight it does not corroborate. You should have that before you rule, not after.** **What it also shows: shipped and `need: 1.0` were bit-stable across both rebuilds while slot-aware and Auto moved — so slot-aware sits near a decision boundary where a small projection change flips a pick and cascades. That is a real reason for caution about it that I did not have this morning.** **DEFAULT UNCHANGED: nothing ships if you say nothing by 08-21 6pm.** **✅ RETRACTED THE SAME EVENING, BY TESTING IT — THE CAVEAT ABOVE OVERCLAIMED AND I AM TAKING IT BACK (P133).** I wrote that caveat from ONE pair of boards. I then ran the same probe across **five** real board versions committed today and measured how many of the 15 drafted players change between consecutive rebuilds. **Result: 00:54→05:11 churned 10-14 players FOR EVERY ARM — that is the multi-source blend landing — and the three rebuilds after it churned 0, 1 and 0, for all of them.** **Excluding the blend transition, shipped, slot-aware and `need: 1.0` are IDENTICAL — [0, 1, 0], mean 0.33 players per rebuild.** **So on an ordinary rebuild, which is what Saturday morning will be, slot-aware is exactly as stable as what ships today.** **WHAT I GOT WRONG:** the swing I reported was ONE player — and shipped and `need: 1.0` each swapped one player over the same window too. The only real difference is that slot-aware's single swap crossed a position boundary and shipped's did not (position vector changed on 2 of 4 transitions vs 1 of 4). **That is a much narrower thing than "least stable", which is what I implied.** ⚠️ **I want to flag the shape of this: I raised a caveat against my own recommendation, then tested it and it dissolved. Softening your own objection is exactly the move that can be self-serving, so the full numbers are above and in `PREDICTION-LEDGER.md` P133 — check them rather than take the retraction on trust.** **What survives: slot-aware's roster SHAPE is marginally more boundary-sensitive at equal churn. What does not: any suggestion it is unreliable across rebuilds.** **DEFAULT UNCHANGED: nothing ships if you say nothing by 08-21 6pm.** | arm | Anchor (r1-2) | **Build (r3-6)** | Fill (r7-10) | **Tight (r11+)** | net | |---|---|---|---|---|---| | auto (your default) | −1.5 | **−175.7** | −64.3 | **+84.8** | −156.7 | | `need: 1.0` | −0.8 | **−161.6** | −48.0 | **+111.4** | −99.0 | | **slot-aware** | **0.0** | **−162.8** | −44.8 | **+123.9** | **−83.7** | **Build costs −162 to −176 for every one of them — a 14-point spread across three arms that differ enormously elsewhere. Tight separates them by 39.** **So slot-aware is NOT better because it protects your rounds 3-6. It does not — it loses the same as the others there. It is better because it earns the most back after round 11, which is exactly where your last five picks are (108, 113, 128, 133, 148).** **AND NOTHING COSTS YOU ANYTHING AT PICKS 8 AND 13:** Anchor is 0.0 to −1.5 for all three, because roster-awareness cannot cost you anything before you have a roster — every slot is empty, so the term has nothing to discriminate on at any weight. **Your first two picks are unaffected by this entire decision.** ⚠️ **I predicted the opposite of this and was wrong** (P130 FALSE: I expected the constant-weight arm to bleed early where the ramped one does not; it does not bleed early either). **Engine-on-bundles, so treat the SHAPE as the finding and not the decimals.** | | QB | RB | WR | TE | |---|---|---|---|---| | **winners (15-pick seasons)** | 1.33 | **3.83** | 5.17 | **1.83** | | shipped | 2.07 | 4.77 | 5.10 | **1.00** | | **`need: 1.0`** | 1.63 | **5.83** ⛔ | 4.33 | 1.13 | | **slot-aware** | 2.00 | **4.63** ✅ | 4.87 | **1.47** ✅ | **`need: 1.0` fixes conversion by drafting A FULL EXTRA RUNNING BACK — 4.77 → 5.83 against the winners' 3.83.** Backs are flex-eligible so a pile of them starts well; it converts beautifully and looks nothing like a winning roster. **It makes your own complaint worse.** **Slot-aware is the ONLY arm that moves both positions toward the winners at once, and the only thing that breaks the one-tight-end constant** (exactly 1 TE in 30 of 30 shipped rosters). **THE PRICE TAG, WHICH IS THE WHOLE DECISION: the 10.4 pts/seat-season between them (+68.6 vs +58.2) is what a winner-shaped roster costs — and that is well inside the ±41.8 detection floor, so the two arms are NOT separable on points at all. Shape is the only thing that separates them that we can actually measure.** **MY REC IS UNCHANGED — ship slot-aware, hold `need` at 0 — and I want you to know the measurement did not move my advice, it gave it a reason.** ⚠️ **Two honest limits: this is engine-on-BUNDLES, handed strictly less than your live board; and the winners' target rests on 6 top-3 team-seasons where neither RB nor TE clears p<0.05, so the DIRECTION is better evidenced than the magnitude — do not read 3.83 as a number to hit.** **DEFAULT UNCHANGED: nothing ships if you say nothing by 08-21 6pm.** `draft/audit/conversion_by_arm_2026-08-19.md`. | Both are engine changes three days before your draft, and points-versus-shape is a preference, not a measurement. |
| — | ⚠️ **One thing is still with Cory but is not a decision — the uploaded resources.** D reported 08-18 that **nothing was uploaded to its session**; anything living only in a chat window is invisible to A and every other lane. `RESOURCES.md` is the committed home for it. Not blocking any lane. | Only Cory has the material. |
| B-E1 | 📧 **BOTH IN-SEASON EMAIL CRONS HAVE NEVER FIRED, EVER — a missing repo secret, not a code bug.** Cory asked *"I don't think emails are sending, investigate."* Checked the actual GitHub Actions run logs, not just the green checkmark: `sunday-alert.yml` (ran 08-09, 08-16) and `weekly-recap.yml` (ran 08-11, 08-18) both hit `##[warning] SUNDAY_ALERT_KEY is not set` / `neither WEEKLY_RECAP_KEY nor SUNDAY_ALERT_KEY is set` on every single run and exited without ever contacting the site. **Fix is one secret:** `weekly-recap.yml` falls back to `SUNDAY_ALERT_KEY` if `WEEKLY_RECAP_KEY` is absent, so setting just `SUNDAY_ALERT_KEY` as a GitHub Actions repo secret (Settings → Secrets and variables → Actions) covers both — same value also needs to be a Netlify site env var (`SUNDAY_ALERT_KEY` or `CRON_SECRET`), which is what the site checks against. **Second thing to verify on the same trip:** `RESEND_API_KEY` on Netlify — the Commissioner Console's automation health panel already says so directly, in red, if it's missing (`notify.configured()`, `src/routes/admin.js:65-70`). No harm done yet (pre-draft, nothing was due to send), but it stays broken silently into the season otherwise — week 1's Sunday alert and Tuesday recap would both go the same way. | Only Cory (or someone with repo-admin) can set GitHub/Netlify secrets — not code either lane can fix. |

## DELIVERED AND VERIFIED

| # | what Cory asked for | how it was verified |
|---|---|---|
| V1 | **A data-stewardship session** (D) | `SESSION-D.md`, inbox, `OPERATING-MODEL.md` row, and `test_lane_coherence.py` — which failed the moment D's inbox appeared without a role file, then passed. |
| V2 | **Nothing gets left behind** | `DEFECT-REGISTER.md` + `test_defect_register.py`, which fails on any row with no owner. It caught lane D's own rows on the first run after the re-route. |
| V3 | **The eight-question data chain** | `DATA-LIFECYCLE.md`, all ten stores measured rather than recalled: two complete the chain, four stop with no recorded reason. |
| V4 | **Everyone in their own lane, A decides and merges** | `OPERATING-MODEL.md` — one screen, Rules 1–5, ASK/EVIDENCE/REC/DEFAULT so silence is consent and nobody idles. |
| V5 | **War room satisfies the rejection.** Cory, 2026-08-18, verbatim: *"mark complete, I like current war room."* Answers B-A9 — the taste call only he could make. `DEFECT-REGISTER.md` row 4b is B's to route closed (not editable directly); routed in `ROUTES.md`. | Cory's own words, this session. |

---

**Rule for adding a row:** if Cory said it and it would change something, it goes
here the same turn — even when the answer is "already covered by X." Especially
then, because "already covered" is what A6 looked like right up until someone
checked, and nothing was covering it.

---

## 🔴 2026-08-18 — THE MARKET AXIS WILL PRODUCE NOTHING ALL SEASON UNLESS YOU SAY GO. ONE DECISION, WITH THE PRICE.

**Context, measured tonight:** the champion (`own_v6`) already contains usage share,
pace, xFP efficiency and a Vegas week-1 tilt. Two arms I proposed as "new axes" turned
out to be **already graded and failed** because they re-added what the model has
(`pace_arm.json` and `advanced_efficiency_study.json`, both `clears: false`).
**The one family the model does NOT contain is MARKET INFORMATION** — lines set by
people with money at stake. That is now the priority for beating Sleeper and
FantasyPros next year.

**AND ITS PIPELINE IS NOT SCHEDULED.**

`draft/weekly_props_arm.py` (`props_weekly_v1`) is **built and already wired into the
Tuesday grader** — `weekly_own_grade.py` imports it and grades it as a study arm. But
its input folder is *"empty pending a human-dispatched real fetch"*, and
**`weekly-props-fetch.yml` is `workflow_dispatch` only — it has NO cron.**

**So on Thursday of week 1 the projection job runs, on Tuesday the grader runs, and the
props arm is silently absent from every grade, all season, because nobody pressed a
button.** That is the exact shape of failure you have been calling out all week.

**THE PRICE, computed from the fetcher's own `estimate_credits()`:**

| scope | per week (16 games) | full 18-week season |
|---|---|---|
| **all 8 player-prop markets** | 1,280 credits | **23,040** |
| confirmed market only (`player_pass_yds`) | 160 credits | 2,880 |

**Budget last seen: 75,681 credits remaining**, on what the fetcher's header reads as a
~100,000/month plan. **The full-fat option is ~31% of the current balance spread across
four months.**

**THE ASK — one word:**
1. **GO, all 8 markets** (23,040/season) — the complete market picture, and the version
   that can answer P12 (alternate lines imply the per-player *distribution* no source
   will publish — the thing we went looking for all week and could not buy).
2. **GO, narrow** (2,880/season) — passing yards only; cheap, but QB-only signal.
3. **HOLD** — and the market axis produces nothing in 2026, which pushes the
   "beat Sleeper and FP" verdict to 2028.

**RELAY'S REC: option 1.** It is the only untapped family, the arm is already built and
graded-ready, and the cost is a fraction of a budget that otherwise expires monthly
unused. **A capture not taken cannot be backfilled** — that is your own standing rule,
and week 1 is ~09-10.

**DEFAULT IF YOU SAY NOTHING: nothing happens.** This one cannot have a relay default —
it spends your money.

---

### ✅ RULED 2026-08-18 — AND MY ASK ABOVE WAS PARTLY MOOT. CORRECTING IT.

**Cory:** *"I guess use our credits until this run out but expire at the end of this
month I believe. Use while you can. Do deep search for 2026 days for free."*

**THE ASK ABOVE WAS WRONG IN ITS CENTRAL ASSUMPTION AND I SHOULD SAY SO PLAINLY.** I
priced a **season-long weekly fetch** at 23,040 credits. **That is not purchasable:
the credits expire ~08-31 and week 1 is ~09-10.** The plan's own framing, recorded in
`free-betting-probe.yml`'s header on 08-16, already said so — *"the Odds API plan is a
ONE-MONTH purchase for HISTORICAL data; 2026 live betting must come from free sources
or not at all"* — and I asked Cory to approve a recurring spend anyway. **He answered
the question I should have asked.**

**WHAT THE CREDITS CAN ACTUALLY BUY BEFORE 08-31, in priority order:**

1. **2026 week-1 (and week-2) player props, if books have posted them yet.** The only
   in-season data this plan can ever produce, and it feeds `props_weekly_v1` — already
   built and already wired into the Tuesday grader. **Verifying availability now with a
   dry run** (costs ~nothing, shows the live credit balance).
2. **Nothing from `historical-props-fetch.yml`** — it offers 2023/2024/2025 only, and
   **we already hold all three in full.** There is no unbought season there.

**THE HONEST CEILING ON THIS: at most a week or two of in-season market data, not a
season.** That is worth having — it is the only market data 2026 will get unless a free
source is found — but it will not settle P11/P12 on its own.

### 🔴 AND THE FREE PATH IS MEASURED SHUT — BALL DON'T LIE CANNOT DO IT

Cory: *"We can get for free from ball don't lie key."* **Tested properly rather than
argued** (`bdl-key-matrix.yml`, artifact `bdl_key_matrix.json`), because the repo's
existing "no" had probed **1 of 4** BDL secret names and I would not report a limit on
that basis.

| BDL endpoint | status | what it means |
|---|---|---|
| `odds` (season/week, by date) | **401** | exists, **needs a paid tier** |
| `player_props` + 5 other spellings | **404** | **route does not exist — BDL has no player-props product** |
| stats · season_stats · injuries · advanced_stats | 401 | paid tier |
| teams | 429 | free-tier rate limit |

**And only ONE of the four secrets is actually set** — the other three are empty, so the
"wrong key" theory is dead too. **A BDL paid tier could unlock game-level ODDS, but the
404s say no tier unlocks player props.** BDL is not a substitute for the market axis.

**⭐ SO THE REAL ASK IS THE ONE CORY ALREADY GAVE ME: find a free 2026 source.** That is
now a tracked row (**P53**) rather than a good intention, and Kalshi is the live
candidate — free, already captured daily, and its catalog carries season player totals
and fantasy-points markets. Its known problem is thinness, not access: every ticker
sampled in mid-August returned **zero open markets**. **Re-probing after the season
opens is the test.**


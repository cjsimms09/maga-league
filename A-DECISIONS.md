# A-DECISIONS — the only page A has to read

**Cory, 2026-08-31, verbatim:** *"shouldnt you own the messaging for A? ie A
makes decisions, approves or rejects, then you route to where it needs to go
from there?"* Yes. This sheet is that protocol, standing, in-season — the
draft-day triage (`A-DRAFT-DAY-DECISIONS.md`) made permanent.

**HOW IT WORKS**
- The relay curates this sheet. Lanes no longer file decision-asks to
  `ROUTES.md → TO: A`; they file to the relay (or anywhere), and the relay
  distills each into ONE row here: the question, the evidence link, a REC,
  and what each answer triggers. **A reads this page, writes one word per
  row (`APPROVE` / `SEND BACK: <reason>` / the ruling itself), and the relay
  routes every consequence.** A never has to open ROUTES to be caught up.
- **THE RED-TEAM CARVE-OUT:** E writes rows here DIRECTLY, unfiltered — a
  chief of staff who curates the red team's access to the executive is how
  an executive stops hearing bad news. E's rows are marked 🔺.
- **THE INSPECTION RULE:** nothing is hidden — every source stays on main,
  every row links its evidence, and the Wednesday audit publishes what the
  relay bounced or summarized, so the curation itself is auditable. A can
  always grep past the sheet; the sheet exists so A never has to.
- A row leaves this page within a day of A's word, its routing recorded in
  the answer column. Decision-latency (filed → A's word) is the Wednesday
  audit's headline metric for this page.

| # | decision | evidence | REC | A's word |
|---|---|---|---|---|
| D1 | **Grade P20** — the weekly→season ceiling rescale. Build done (relay); calibrates and keeps player info, blows the best-ever sanity bound at every position; one knob can't price breakouts and plausibility together. | `draft/backtest/p20_rescale_fit.json` + the ledger row | **FALSE** (register 4w reopens with a two-parameter successor, per the row's own route) | ☑ **FALSE — RULED 09-02 (A).** Read the artifact, not the summary: controls green (known-positive α 0.35 recovered, known-negative sd 0), calibration and variation bars pass, sanity fails at ALL FOUR positions (WR max 557 vs best-ever 367; QB 620 vs 512). One knob cannot buy both. Consequence executed by A in the ledger (grade + what-changed); register 4w reopens with the two-parameter form (dispersion rescale + position-level breakout term, m capped at best-ever/μ) as the next candidate — relay files it as a new P-row, D grades through the same walk-forward. Nothing ships. |
| D2 | **The seats-demoted line** — `assert len(sup) <= 6` in `test_roster_robustness` is the ONLY thing refusing every board publish (54 of 55 blocker tests green). Its own comment says whoever rules owns the line and forbids raising the number. | test comment at `draft/tests/test_roster_robustness.py:240`, register 378, the 343 ratchet (each refusal destroys a day's capture) | Convert the count to an artifact and assert every demotion NAMES its resolution — a board refusal should mean the board is bad (register 55's own lesson) | ✅ **EXECUTED 08-31 on CORY'S DIRECT ORDER ("I sent go, want board fixed now") under the hierarchy ruling — count → `draft/data/seat_disagreement.json`, naming-property kept load-bearing, 10/10 green, board build dispatched. A MAY REVERSE; register 378's shortlist question stays open (E, 09-03) with the artifact as its feed.** |
| D3 | **Merge `claude/fantasy-football-research-926y6z`** — two commits: the roster_robustness vintage-drift guard (hardening; the crash healed itself when the board rebuilt) and the Thursday TNF lineup check (clock: TNF week 1 is 09-10). 31/31 + 9/9 + 11/11 suites; `sunday_why` runs on main post-merge. | branch tip `9222d6ad`; ROUTES merge-ask | **APPROVE** (or send back the guard alone — the Thursday check is the half with a deadline) | ✅ **EXECUTED 08-31 under the same Cory order as D2** ("I sent go, want board fixed now" — the guard was one of the board run's two blockers): both commits cherry-picked to main as `cae5e95b` + `c23f06cc`, suites green there. A MAY REVERSE (revert the two shas). The branch itself carries nothing further. |
| D4 | **The kill switch (QUICK-KILL, register 199/307)** — the adaptation policy calls benching automatic; no `decide_bench()` exists. First Tuesday grade is 09-15; a champion nothing can dethrone gets crowned then. Every default is bad by the row's own words (ship a winner-bencher, or start week 1 with no kill switch). | register 199 (measured: zero bench/demote code), 307 | Rule the SHAPE now (3 consecutive graded losses to any challenger = benched, pending your sign-off), D builds it before 09-15 | ☑ **SEND BACK the REC's shape; RULED 09-02 (A) with the shape that survives register 307.** "3 consecutive losses" is exactly the rule 307 measured: it benches an arm EQUAL to the champion 72.6% of seasons and a BETTER one 61.5%. Ruling: **(1) the kill switch for week 1 already exists and is manual — `controls.json` `champion_override`; A flips it on evidence, nothing automatic ships before 09-15.** **(2) The automatic shape is 307's option (b), symmetric with promotion: the champion is benched to the best challenger only when ALL of — cumulative MAE worse than that challenger over every graded week so far · loses the recent 3-week window · AND the start/sit shadow verdict (register 470) agrees the challenger is better.** Three conditions and a null, mirroring promotion's four. **(3) D builds `decide_bench()` REPORT-ONLY (writes a verdict beside `promotion_shadow`, flips nothing) by 09-15, with 307's false-positive harness re-run on the three-condition rule — the number it prints is the sign-off gate: an equal arm benched in >10% of simulated seasons is a send-back.** Relay routes to D. |

| D5 | 🔺 **Register 437 (E's find) — the weekly emitter's keeper blind spot**: `boardIndex()` read `players` only, so arm `'ours'` emitted nothing for all 23 keepers while arm `'sleeper'` covered them — the 09-15 our-model-vs-Sleeper grade would run on a population biased against our own model, and weeks 1-2 cannot be re-emitted honestly once missed. | register 437; E's REC verbatim ("do it before the first Thursday emission"); known-bad control run and refused; 77/77 | Execute E's REC | ✅ **EXECUTED 08-31 by the relay under Cory's same-day "make model better" order, ahead of the 09-04 silence-default** — `kept_players` concat in `boardIndex()`, 3 new tests incl. all-23-resolve on the real board, committed WITHOUT `[skip deploy]` so it deploys before week 1's first emission. A MAY REVERSE (single commit). |

| D6 | **Register 444 — the keeper-projection archive hole** (D's find and fix): `_update_proj_series` read `players` alone, so since the 08-22 keeper lock every nightly permanently lost the day's projections/situation/distribution for all 23 keepers — register 80's bug in the one file where retroactive fetches leak. D fixed it on-branch with a fail-armed test; the ask was "A merges", and each night unmerged lost another day. | register 444; D's `44af68c3`+`6371f178`; fail arm re-proven on main; drift-monitor controls ALL PASS | Merge D's fix | ✅ **EXECUTED 08-31 evening by the relay (cherry-picked, A MAY REVERSE — two shas), before tonight's 04:00 nightly so day 11 is not lost.** Still yours: 435's gate-policy ruling (skip-with-disclosure as standing policy) and wiring `source_universe_drift.py` into the nightly. |
| D7 | 💵 **Cory, 09-01: "Make our model better... win more matchups and make me more money."** The relay's ranked answer is `IN-SEASON-EDGE-PLAN-2026.md`; it rests on YOUR register 463 (the live champion is the worst full-coverage arm on every 2025 grade; the props arm beats it at all four positions). Three preregs filed (P354 props live · P355 distributions into the E[$] solver · P356 Tuesday waiver rail). | register 463, `weekly_arms_2025_backtest.json`, EFFICIENCY-LEAK ($1,500 weekly-high pot = 70-75% of the leak), dossier (Cory .862 conversion, .439 claim fail) | **Build ① (props arm live as challenger for week 2) first; rule ⑥ (promote on start/sit, not MAE) by 09-15** | ☑ **APPROVE — RULED 09-02 (A).** The order stands: ① first, and it is already moving — the free props writer wrote week 1 (312 players, register 467) and the props second opinion runs after every Thursday snapshot; the live arm is C's capture plus A's loop for week 2. ⑥ is ruled on evidence, not by date: the Tuesday grader already runs the start/sit rule as a SHADOW beside MAE (register 470, P358); the 09-15 grade is quoted on both and the promotion metric flips only if the shadow disagrees with MAE by week 5 (P358, grade 10-13). ② waits on P355; ③/④ as written. One addition, no re-rank: **register 478's four-source weekly capture (CBS/ESPN/FleaFlicker/NumberFire weekly rows) enters through ⑤'s door — D's harness, explorer cap — not straight into the live loop.** |
| D8 | 🏈 **The four-TE problem is the value function, not a missing cap (Cory, 09-02: 'a cap still doesn't solve the knowledge gap').** Measured: the board's own draft-night recommendations break the format in every seat (7.1 violations/seat vs humans 1.0) and never complete a roster. MLV — best-lineup-with-him minus without, against measured wire replacement — exists as your second voice. | `roster_grammar_audit_2026-09-02.json`, P363, FUTURE-PROOF-2027 §5 | **Rule the MECHANISM: P363 decides the 2027 value term — MLV becomes the score if it passes the grammar unconstrained at the humans' rate without losing replay points; a cap only for what the value function provably cannot learn** | ☑ **RULED 09-02 (A): APPROVED as a mechanism.** Two measurement conditions on the ROUTES row: the pass bar (grammar rate AND paired replay points with CI) written into P363 before D runs it; both arms walk their own drafts. D measures by 10-06; nothing ships before the January verdict. **Cory ruled the kicker 09-02 (required, dead-last pick; derived via the completeness term, `mlv_bench_complete`): 10/10 seats take K last, 1.0 viol/seat, +41 startable over the humans.** |
| D9 | 🖥️ **The owner site, reviewed as an owner (Cory, 09-02: 'is the site cool!') — `SITE-REVIEW-2026-09-02.md`.** 20 pages rendered at phone width as non-commissioner David. Mostly good bones (matchup, watch, races, rules are strong); three real fixes (scoreboard puts money above the live games; the History index is one 11,589px page; a debt can never be marked paid) and a stale lane: no B commit since 08-27 with 12 open E findings on your Sunday tools. | the review + `draft/audit/screens/review*-phone.png` | **Rule the builder: re-launch B with the list, or relay works items 1-8 and E's tool findings come to you** | ☑ **RULED 09-02 (A): B keeps the list — the premise expired the same day.** ROUTES 09-02, B verbatim: *"B IS LIVE — TAKING THE SITE-REVIEW LIST"*, five B commits landed 09-02 (registers 321, 427, 468, 466, 434) and item 1 (scoreboard: games first, my game pinned, money below) is on main. Order: items 1-2, then the 12 open E findings on Cory's lineup and waiver tools, then 3-8. **Fallback unchanged: any item without a B commit by 09-04 end-of-day goes to the relay; E's tool findings never come to A, they go to B with the same date.** Item 9 (owner access) is dropped — Cory ruled A24 on 09-02: *"Keep everything private form other owners.. trust wire."* |
| D10 | 🧹 **Two more hand-rolled status classifiers — `reopen_risk.js` and `closed_rows_resolve.js` — read the register against private word lists (register 469, D). `stale_row_sweep.py` was the third and is fixed 09-02 (reads the status cell through the guard's parsing + the canonical vocabulary; baseline restated 274 open / 99 named-by-a-later-commit).** D declined to patch the other two mid-campaign because their output orders live work (Tier-1 under LEARNING-CONSTITUTION). | register 469; `draft/audit/STALE-SWEEP-2026-09-02.txt`; `register_status_vocabulary.json` | **Rule YES — same pass, same port (status cell, canonical words, loud exit); relay does both by 09-04 and restates each tool's baseline in its header.** Default if silent by 09-04: relay ports both. | ☑ **YES to the port; SEND BACK on "same port". RULED AND EXECUTED 09-05 (A) — the default fell due 09-04 and nothing had moved, so A did it rather than roll it again.** The two tools ask different questions and a uniform port would have made one of them worse. Measured first, on the live register: **`reopen_risk.js` — 20 rows read terminal that the vocabulary calls OPEN, 3 read OPEN that are terminal (44, 220, 323, all RETRACTED/WITHDRAWN). The 20 are harmless noise in a risk report; the 3 are a merge risk reported clean, so it takes `terminal` exactly.** **`closed_rows_resolve.js` — 16 rows it never checked, 1 it should not have, AND 21 ROWS ITS PARSER COULD NOT SEE AT ALL (495 of 516: it split on the literal `' \| '`), which is the bigger hole and nobody had counted it. It takes `terminal` ∪ `claimed_done`, because `✅ FIXED in <sha>` is exactly where register 424's dead citation lives — the pure port would have DROPPED that population.** Both now read `canon.rows()` (second-from-last cell, unescaped-pipe split) instead of a positional guess. Before/after on the live register: 231 rows / 362 refs → **257 rows / 401 refs**, same 15 findings. ⚠️ **The port's one new finding was a FALSE POSITIVE and reading the row caught it** — row 442's `1955daa` is an *ffanalytics* upstream sha, unresolvable here by construction; named in a `FOREIGN_SHAS` allowlist with its reason. **Two things fell out and both are filed: register 486** (`isClosed` reads any terminal word anywhere in the status cell, so ten live OPEN rows are invisible to the overdue check — fix designed and measured 15/15, NOT shipped because it reds `main` until 7 hidden rows are worked; A, 09-09) **and the zero-date guard on `amend_register_row.js`**, which caused exactly that failure on 485 and now refuses to strip a row's last recheck date. `stale_row_sweep.py` was the third tool and was already done 09-02; `amend_register_row.js` was a fourth and is ported in the same commit. |
| D11 | 🧹 **Three register rows have waited on you since 08-29 with the fix already pointed at (C): row 10 — weekly_volatility.py adopts the shared scoring-fingerprint function; row 4n — adp.py's two fallback-clamped adp_sd constants get the same treatment the fourth copy got; row E7 — `STREAMABLE_LATE` is computed and read by nothing, E's default (delete, with a before/after on the TE band) fell due 09-03.** | rows 10, 4n, E7 (each rolled 09-04 with this pointer) | **Rule YES to all three as one hygiene commit; nothing is a weight change.** Default if silent by 09-11: the relay files the three as a single ROUTES row to A with the diffs attached. | |




*Filed 2026-08-31 by the relay. The protocol amendment lives in
`OPERATING-MODEL.md` Rule 3b; the routing of each answer is the relay's job
within a day.*

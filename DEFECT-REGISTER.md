# DEFECT REGISTER — every open data/logic concern, with an owner

**Cory, 2026-08-17:** *"it's also your job to make sure nothing gets left behind
or not chased down, especially potential data or logic errors or anything that
messes with our models draft or inseason tools."*

Every row has an **owner** and a **next action**. A row with neither is a defect
in this file. `draft/tests/test_defect_register.py` fails if one appears.

**This file tracks what is BROKEN. `CORY-ASKS.md` tracks what CORY ASKED FOR and
whether he has it** — a defect row and a promise row are different things, and an
ask with no defect behind it used to fall between them.

**Status words mean one thing each:** `OPEN` = nobody has looked since it was
filed · `IN HAND` = someone is on it now · `WAITING` = blocked on a named person
or date · `CLOSED` = fixed and verified, kept for the record.

---

> ### ⏰ FROM 08-20 THIS FILE CAN TURN THE BUILD RED — READ THIS ONCE
>
> **Cory, 2026-08-17:** *"tons of findings but no one is following up or acting
> on any of them.. that needs to change."*
>
> Every row already carried an owner and a recheck date. **Nothing read them.**
> `draft/tools/register_recheck_check.js` now does, in CI: it fails on exactly
> one thing — **a row still OPEN past its own recheck date.**
>
> **Every open row now has a date** (32 were assigned by the relay on 08-17 by
> section: blocking 08-19, correctness 08-24, data 08-31). **The owner may
> change any of them, and changing a date WITH A REASON is a legitimate way to
> clear the check.** Acting on the row is the other way. Silence is not.
>
> **SO, PLAINLY: on 08-20, 22 rows go overdue and `main` goes red unless they
> are resolved or re-dated — two days before the draft.** That is the forcing
> function working, not a bug, and it is stated here in advance so nobody
> discovers it at 2am on draft eve. **If that pressure is wrong, the answer is
> to move the dates deliberately and say why — not to mute the check.**

## 🔴 BLOCKING — draft-critical, this week

| # | what | owner | status | next action |
|---|---|---|---|---|
| 42 | 🔴🔴 **P0, LEAGUE-FACING, KEEPER LOCK IS IN TWO DAYS — THE KEEPER-DEADLINE BANNER ON B'S UNMERGED BRANCH WOULD PIN AN URGENT ALERT SAYING *FRIDAY AUGUST 21 AT 6:00 PM CDT* TO ALL TEN MANAGERS, WHILE FIFTEEN FILES IN THIS REPO SAY KEEPER LOCK IS 08-20.** `claude/in-season-surface-fixes-6nyayc`, commit `b26e1713`, stranded 16h. **B built it correctly and config-driven** — `keeperDeadlineAnnouncement(config, nowISO, seasonYear)` reads `config.keeper_deadline_date` and only falls back to `seasonYear + '-08-21'`. **The defect is one line further on: the success path returns `configured: true` UNCONDITIONALLY, including when the date came from the fallback**, and `src/routes/member.js` gates the pinned alert on exactly `if (keeperInfo.configured && keeperInfo.message)`. **So a hardcoded fallback is indistinguishable from a deliberate answer — the same defect class as every other finding today, wearing a date.** `configured` reads as *someone set this*; it actually means *a date was derived*. **MEASURED: nothing in this repository sets `keeper_deadline_date`** — it appears only in `draft/tests/dashboard.test.js`'s fixture and in the reading code itself (`git grep` across the branch). So the fallback is the live path. **STATED LIMIT: I cannot read the live config.** `world.config` resolves from the Netlify Blobs store at runtime and this sandbox has no access to it, so I have verified the REPO sets nothing and I am NOT claiming the live store carries nothing. **That is the one thing that would make this a non-issue, and it is the one thing I cannot check from here.** **⚠️ SHARPENED, AND IT MOVES THE BLAME OFF B ENTIRELY.** `draftAnnouncement()` — the sibling function directly above, ALREADY ON `main` — has the IDENTICAL shape: `configured: true` returned unconditionally with `fallbackDate = seasonYear + '-08-22'`. **B copied a pattern the codebase already established, faithfully. This is not B's defect; it is house style with one field missing.** **THE ACTUAL ASYMMETRY, and it is the whole fix:** `draft_date` is READABLE AND WRITABLE — `src/routes/admin.js:826-833` and `views/admin/console.ejs:710` give Cory an input for it, so its fallback is a genuine safety net that fires only if he never sets one. **`keeper_deadline_date` is READ-ONLY. No admin input exists for it anywhere** (`git grep` over `src/routes/admin.js` and `views/admin/` on B's branch: nothing). **So the fallback is not a safety net, it is the ONLY path, and Cory has no way to correct it even if he knows the right date.** **SO THE FIX IS SMALLER AND BETTER THAN THE ONE I FIRST PROPOSED: add the admin input for `keeper_deadline_date`/`_time`/`_tz`, matching the `draft_date` pattern that already exists three lines away.** Then Cory sets the true date, the fallback never fires, and the question answers itself. Keep the `configured`/`derived` split as well — it is what stops an urgent league-wide alert firing off a guess — but the missing input is the root cause and the one-line-of-UI fix. **AND A SMALLER ONE WHILE YOU ARE THERE:** the admin form's own default is the literal `'2026-08-22'` (`admin.js:1072`, `console.ejs:710`), which is exactly the hardcoded year literal `dashboard.js` documents itself as avoiding (*"never a hardcoded year literal, per the no-season-literals guard"*). The guard is real in one file and absent in the file that feeds it. | **Cory** · **B** | OPEN | **Cory rules the date; B owns the code.** **TWO ACTIONS, AND THE SECOND DOES NOT WAIT ON THE FIRST.** (1) **CORY RULES THE DATE** — 08-20 as fifteen internal files say, or 08-21 6pm CDT as the banner would announce; a keeper deadline the evening before an 08-22 draft is entirely plausible, which is why this is a question and not a correction. (2) **B: an urgent league-wide alert must not fire off a fallback.** Split `configured` (config set it) from `derived` (we guessed), and gate the PINNED ALERT on `configured` only — the dashboard banner can still show the derived date as informational. **That fix is strictly correct under EITHER answer to (1)**, so it should land before the date is settled, not after. **Recheck 2026-08-19** — one day, because the lock is 08-20 and a wrong deadline reaching managers is unrecoverable. **SECOND, SEPARABLE P0 ON THE SAME BRANCH — and it is NOT the CI red I first called it.** `11e253bb` is not repairing a red (`matchup_placed_bet` 6/6 and `trashtalk` 27/27 both PASS on `main` today; the commit ADDS 44 test lines). It carries `views/admin/warroom.ejs:456`, which tells Cory the ceiling slider is off because the measurement *"COULD NOT BE SIGNED... −4.8 [−26, +17]"*. **That reading is retracted — brief §7b: it came from a board where `proj_ceiling` was `proj_mean × a constant` (Spearman 1.0000 against value), and the re-run says a non-zero weight BEATS zero, 3/3 seeds, 0.15 through 0.65. The zero is held through 08-22 deliberately, not for want of a measurement.** A retracted reason on the screen Cory drafts from at 8s/pick. **`11e253bb` is independent of the banner commit and should be cherry-picked to `main` on its own.** |
| 1 | **Board has not published since 08-15.** Gate refuses the fresh board. | **A** | IN HAND | Build fresh board; confirm rows 2 and 3 below before regenerating anything. **recheck 08-19** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** **✅ PREMISE IS FALSE AS OF 2026-08-18 — relay.** The published board `public/draft_data.json` carries `built_at 2026-08-18T05:33:24Z` and was committed 05:38 (`9322b022`, the routine daily rebuild). `test_published_board_is_not_stale.py` passes 4/4, `board_input_staleness.js` reports every declared input predating the board, and the war-room rehearsal renders 200 rows off it with all 192 ranks and 192 projections matching. **The gate is not refusing anything.** Left IN HAND for A to close, but the row as written describes 08-15 and it is three days and several rebuilds out of date. **CLAUDE.md's own lesson applies to the register itself: verify a row's premise before working it — five of six premises handed to a lane on 08-17 were wrong, and none was a measurement error.** |
| 2e | ✅ **P0 (keeper lock 08-20) — RESOLVED. K/DEF HAD NO CALIBRATION CELLS BECAUSE THEY WERE NEVER CAPTURED, AND ONE LINE OF OURS IS WHY.** The calibration's 20 cells are 4 positions × 5 bands; `cells_unmeasurable: 0` counts only cells that were *attempted*, so K/DEF read as "no problem" rather than as refusals. The graded stores hold **0 K and 0 DEF** player-weeks (2023, 2024) against a scoring table that prices both (44 keys incl. `fgm_*`, `xpm`, `def_td`, `sack`) and a board that ranks **44 K + 32 DEF**, every one on `proj_ceiling_source: "gaussian_z"` while skill positions get `measured-2023-25-p90`. **Root cause: `fetch_component_stats.py:104`, `POSITION_GROUPS = ("QB","RB","WR","TE")`, no recorded reason** — kickers are `position_group "SPEC"` and are dropped wholesale. **K is a DROP-IN:** the file we already fetch returned **200** today with **569 kicker rows / 43 kickers / weeks 1-22** (2024) and a 1:1 map to all eight kicker scoring keys. **DEF is a BUILD** — no team-defence rows exist in the player file at all. `draft/audit/kdef_calibration_p0_2026-08-17.md` | **D** | ✅ RESOLVED (honest refusal) | **DONE 08-17 the second legitimate way:** `projection_error.py` now declares `POSITIONS_NOT_MEASURED` and `save()` emits it, so the next real run carries it forward. Artifact regenerated by a pure `load()`→`save()` round-trip — **every cell, scalar and population field byte-identical, one key added, no number recomputed.** Nothing estimated; **no interim haircut**, per A. **K stays parked to C** as the upgrade path (add K to the filter, re-emit 2023-25, and `fgm_50p` must absorb `fg_made_50_59` **+** `fg_made_60_` or every 60-yarder is silently dropped); D re-runs the calibration same-day, no code change needed. **DEF: A's stated default stands** — exclude from cross-position dollar comparisons; the `gaussian_z` stamp already supports that for $0 (register 8b). Pinned by `test_calibration_covers_every_board_position.py` (`repo_parity`, **green via the declaration**, and verified to go red again if it is removed). **unblocked by** C adding K to the filter — that upgrades the refusal to a measurement; **owner D**, **recheck 08-19.** |
| 2b | **CORRECTED — the TE effect is real but HALF what I first reported, and the big one is K/DEF.** My first pass excluded K/DEF, which was MY filter, not the board's. On the real full board Waller is rank **201** (not 136) vs ADP 262 — **+61, not +126**. The genuine outlier is **K and DEF: board 105–150 vs ADP 323–336, drifts of +186 to +220**, because their replacement levels (K 97.0, DEF 103.0) give marginal kickers positive VORP while the market takes them last. The engine demotes onesies at `recommend()` time (`demoteFlaggedOnesies`); the raw board ordering does not, so anything reading `vorp` directly sees it. | **A** | 🔴 OPEN | Decide whether the published board should carry the demoted order or a flag. **unblocked by nothing, owner A, recheck 08-19.** **⚠️ RE-MEASURED 2026-08-18: the NUMBER is stale and the substance may be WORSE.** Darren Waller is **overall_rank 130** on the 05:33 board — not 201, and not the 136 of the first pass — against an **ADP of 303**. So the board ranks him 173 slots ahead of the market, a larger anomaly than when this row was written. **Direction unchanged, magnitude moved a lot; re-scope rather than close.** |
| 2c | 🔴 **HIGH-UPSIDE YOUNG RBs ARE UNDER-RANKED — AND RE-MEASURED TODAY THE GAP HAS ROUGHLY DOUBLED, WITH NO CHANGE SHIPPED TO EXPLAIN IT.** This row recorded RJ Harvey board 141 vs ADP 79 (**−62**) and Bhayshul Tuten board 114 vs ADP 54 (**−60**). **Re-measured 2026-08-17 on the board built 16:35Z: Harvey board 210 vs ADP-rank 79 (−131), Tuten board 148 vs ADP-rank 54 (−94).** `ceiling = 0.45` has NOT shipped, so this is the BEFORE number moving on its own — the board's disagreement with the market on upside RBs is WIDENING, not stable. **⚠️ I CANNOT SAY WHY FROM HERE AND I AM NOT GOING TO GUESS:** the board was rebuilt between the two measurements and an ADP refresh, a projection refresh or a weight change would each produce this. **That the reference point moved silently is itself the finding** — every before/after test resting on this row (including Q13) was about to be graded against a baseline nobody had re-read. **And it is not two players.** With the drift check now split by position (K/DEF were swamping the top of the list — row 2b), **every one of the biggest skill-position disagreements is the same shape**: Tuten −94, DJ Moore −86, Jadarian Price −84, Carnell Tate −74, Courtland Sutton −53, all ranked BELOW the market, all with **no stated model reason**. | **A** | 🔴 OPEN | 🔴 **STOP — THE TEST THIS ROW PRESCRIBED CANNOT WORK, FOUND 2026-08-17 BEFORE ANYONE RAN IT.** This row said *"re-run the drift check after shipping `ceiling = 0.45` and see whether the gap closes."* **`adp_drift_check.js` ranks the board on `vorp`, and `vorp` is `proj_mean − replacement` with NO weight in it** — verified by arithmetic on the live board: Gibbs `120.6 = 299.9 − 179.3`. **`w.ceiling` enters ONLY the composite `score` inside `recommend()` (`engine.js:1811`), never `vorp`.** So the prescribed check would have come back "no movement" **no matter how good the ceiling weight is**, and that null would have been read as *the ceiling weight does not help* — a false negative about the one edge decision Cory ruled on. Same family as everything else this week: a check that cannot detect the thing it is assigned to detect. **THE CORRECT TEST, and it is a different measurement:** compare `recommend()` output at Cory's picks with `ceiling` 0 vs 0.45 — which `engine.js`'s own comment already reports (0 of top-60 move at picks 33/48/68; 19 at 108, max 4 spots; 32 at 128, max 10; **top recommendation never changes**). **On that evidence the young-RB drift is NOT what the ceiling weight moves**, so 2c and the ceiling ruling are two separate questions that this row had merged into one. **Two actions remain, and the first is unchanged and still new.** **(1) Diff the 16:35Z board against the build 2c was measured on and NAME what moved** — a baseline that drifts silently makes every before/after claim unfalsifiable. **(2) If you still want the upside question answered, measure it in `recommend()` ORDER, not in `vorp` order** — the drift check is structurally blind to it. The check's positional split is still worth having: it is what surfaced that this is five players of one shape, not two. **unblocked by nothing, owner A, recheck 08-19.** **✅ RE-MEASURED 2026-08-18: TRUE AND STABLE.** RJ Harvey **board 144 vs ADP 83** (row recorded 141 vs 79); Bhayshul Tuten **board 94 vs ADP 56.33**. Both still ranked well BELOW the market, which is what this row asserts. **One of only two blocking rows I checked today that is accurate as written. Stays open.** |
| 31 | 🔴 **THE PROJECT'S HEADLINE EDGE NUMBER IS MISREAD IN FOUR FILES, INCLUDING `CLAUDE.md`.** All four say the tool *"ties Cory (−6.5) and loses to the league's best drafter (−163)"*. **The −163 seat is ds7mmet, whom the same artifact ranks 4th of 10.** Rank 1 is Schmelley, where the tool is **−29.0** — better than the median seat (−31.4). The label was assigned FROM the delta it was offered to explain, and **both source files forbade the read in advance** (`honesty`: *"only the top3-vs-bottom-half group contrast is quotable"*; the audit doc's preregistered small-n rule). **Measured, n=10 seats: the tool's delta does NOT track drafter skill — ρ −0.25, permutation p=0.50, known-positive control fires at p=0.013.** **And the spread swamps everything: 30 seat-years, mean −32.8, sd 116.9, 15 of 30 positive, minimum detectable effect ±41.8 pts/season.** **In the units a projection study reports that is ΔMAE ±0.310 pts/player-week — 5.4% of own_v6's own weekly error (5.70)** — so **the projection model's weekly error must fall by more than a twentieth before this instrument can see it.** The one study this week with a points-denominated effect (team Vegas line, ΔMAE +0.008) is **39× below** that; the other three were nulls with nothing to convert. `draft/audit/replay_best_drafter_claim_2026-08-18.md`, `draft/backtest/replay_seat_read.json` | **D** | ✅ TEXT FIXED, ⚠️ SEND BACK OFFERED | **DONE 08-18:** all four sentences corrected in place (originals kept and marked, per `league_benchmark`'s own retraction precedent), pinned by `test_best_drafter_claim.py` (10 `repo_parity` prose nodes, 5 controls inside the gate). **⚠️ `CLAUDE.md`, `OWNERS.md`, `ROUTES.md` and `test_gate_selection.py` are TERRITORY: A, edited by D — SEND BACK and I re-prep as a diff.** **What is NOT done and is A's + E's:** the ±42 detection floor means E1 cannot be graded by this instrument at the effect sizes we are chasing — a decision is owed on what instrument replaces it. **unblocked by nothing, owner A, recheck post-08-22.** |
| 32 | ✅ **THE ASYMMETRIC ENVIRONMENT ARM: ONE REAL SIGNAL, ONE NULL — AND THE PREREGISTERED BAR PASSED BOTH.** Cory ruled this built before 08-22 (overriding D's "after"). Preregistered (`ASYMMETRIC-ENV-PREREG.md`, committed first in `f7010f1`), graded (`asymmetric_env_arm.json`), five seasons, **leave-one-season-out throughout**, oracle known-positive gate PASSED, join survival ≥0.99 everywhere. **Both arms cleared the bar out-of-sample, 5 of 5 seasons positive: `game_total` +0.0491, `team_implied` +0.0832.** **Then a placebo the prereg did NOT require — permute team→multiplier within each week, same distribution, zero information, 60 draws each running the full LOO fit — killed one and halved the other:** `game_total` **p=0.377, net +0.0034 → NULL**; `team_implied` **p=0.0164, net +0.0343 → real, but 59% of the headline was placebo.** **Mechanism: the baseline is a running mean of a right-skewed quantity, so it is biased HIGH and ANY rule that shrinks a subset improves MAE.** λ fitted to 1.00/0.00 in every fold — one-sided, not asymmetric, which is what a pure shrink rule looks like. **Corroborated twice independently:** the grid corner **binds** for `game_total` (wants λ_low=1.5 — a shrink rule always wants to shrink harder) and **does not** for `team_implied`; and this reproduces register 18's *"a game total cannot separate a 45-point offence from the 3-point one it played"* by a completely different route. **⚠️ AND MY OWN PREREG BAR WAS BROKEN BY CONSTRUCTION:** it required ≥4 of 5 seasons positive but judged on a 3-season primary set — a bar that could not be met however the data came out, which is the check-that-cannot-fire defect inverted, in my own file, the same day I audited the repo for it. `draft/audit/asymmetric_env_arm_2026-08-18.md` | **D** | ✅ CLOSED (graded) | **Nothing installs — the prereg said so before the number existed.** +0.0343 is **11% of the ±0.310 detection floor**, 14% of a perfect oracle, 0.6% of `own_v6`'s weekly error — but **4.3× register 18's symmetric arm**. What it establishes and what transfers: **the two-sided form is the right shape for environment features, and the team-level line carries information the game total does not.** Guarded by `test_asymmetric_env_arm.py` (8 checks, all 8 proven to fail under hand-planted corruptions, incl. a known-NEGATIVE control that `game_total` stays null). **Triggers:** split the shrink from the signal in the FORM; per-position; **and row 33.** **unblocked by nothing — graded; owner A for any wiring, post-08-22.** |
| 33b | ✅ *(renumbered from 33 at merge — 33 was already the 2025 coverage-divergence row)* **INVENTORY DONE 08-18 — six of eight multiplicative MAE arms are SAFE BY CONSTRUCTION, and the reason is the same in each: the multiplier is mean-centred or week-normalised, so it cannot move the prediction's mean.** `pace_arm` (`1+k(mean−x)/mean`), `advanced_efficiency_study` (`1+ADV_W·z`, z mean-zero, weight fixed before data), `exp_weekly_env`, `vegas_team_arm`, `oracle_lambda_sweep`'s main curve — none exposed. **The design rule that falls out: centre the multiplier and the artifact disappears.** Two arms skip it: register 32's (placebo run, one arm killed) and the opportunity adjuster (row 34). **AND THE CONTROL WAS RUN ON MY OWN CLAIM FIRST:** 18b's asymmetry survives — every side beats all 40 placebo draws (p=0.024) — **but the ratio is corrected from "5–10×" to "2.3–7.2×"** (net +0.4514/+0.2967 dud, +0.0627/+0.1295 shootout). **And the placebo is EXACTLY 0.0000 on the shootout side in both seasons — the free lunch exists only in the shrink direction, which is row 32's premise proved outright.** `draft/audit/opportunity_adjuster_row33_2026-08-18.md` §5-6 | **D** | ✅ CLOSED | Superseded by row 34, which is what the inventory found. |
| 34 | 🔴 **THE BOARD'S ONLY EDIT TO SLEEPER IS ZERO-INFORMATION, AND EVERY NUMBER WAS ALREADY IN A COMMITTED ARTIFACT.** Register 21 established `proj_mean` = Sleeper × the opportunity adjuster (`proj_baseline == proj_sleeper`, 422 of 422), so the adjuster **is** the entire difference between our board and a free public projection. Across **27 graded cells** in `opportunity_adj_grade.json`: **it makes MAE worse in 27 of 27**; it **adds a mean +9.10 points of BIAS** to baselines already biased high in 23 of 27; it beats its own **zero-information rank surrogate** — same adjustments, reassigned purely by descending baseline order — by a mean of **0.54 MAE points on a ~40-point error** (one cell: 51.543 vs 51.498, a gap of 0.045); and its ordering beats a **shuffle of itself** at p<.05 in **1 of 27 cells** (median p **0.265**), which is what 27 tests produce by chance. **WHY NOBODY SAW IT:** `grade_cell` runs its permutation **only for Spearman**, and the artifact's **only** verdict is `verdict_ordering` — every cell reads NEUTRAL. **The MAE half was measured and never given a verdict**, so nine pooled cells of CI-excludes-zero degradation sat unread. The permutation to grade it is **two lines away in the same function**, used for the other metric. **Mechanism, and it is register 32's seen from the other side:** a biased-high baseline pays you for shrinking and charges you for inflating, whatever the rule knows. `draft/audit/opportunity_adjuster_row33_2026-08-18.md` — answers **Q1** | **A** | 🔴 OPEN | **NOT a "remove it before 08-22" — the no-change rule holds and this is A's call.** **Ordering is NEUTRAL, not harmed, so Cory's PICK ORDER is largely unaffected; the harm is in MAGNITUDE**, which reaches anything reading points, VORP or dollars. **ASK A: determine the live-board impact** — this study grades RECONSTRUCTED baselines (the artifact's own `_note`: `proj_baseline` was never archived before 2026-08-09, so no number here grades the shipped baseline step; three different baselines all give the same answer). **The obvious next arm needs no new data: centre the adjuster (`adj − mean(adj)`), which removes the +9.1 inflation mechanically.** If the residual still tracks the surrogate, the adjuster carries nothing. Pinned by `test_opportunity_adjuster_grade.py` (6 checks, all 6 proven to fail under hand-planted corruptions). **unblocked by nothing — measured; owner A, recheck post-08-22.** |
| 2d | **46 of 150 board players disagree with ADP by more than a round with NO stated model reason**, and 62 move more than four rounds. | **E** | OPEN | Each is a suspected defect per Cory's rule. **unblocked by nothing, owner E, recheck 08-19.** **❌ RE-MEASURED 2026-08-18 — BOTH NUMBERS ARE WRONG, AND THE ROW IS INTERNALLY CONTRADICTORY AS WRITTEN.** Of the 150 board-ranked players carrying an ADP: **94 move more than one round (10 slots)** and **31 move more than four rounds (40 slots)**. The row says *46 ... more than a round* and *62 move more than four rounds* — **62 cannot exceed 46, because the four-round set is a SUBSET of the one-round set.** That contradiction has sat in a BLOCKING row unnoticed. The real shape is the opposite of what it implies: **far MORE small disagreements (94) and far FEWER large ones (31).** Re-scope with the corrected numbers. |
| 2 | **A new field joined the constant-multiple family** on the fresh board. This is the ceiling-defect class by name — a field that is a rescaled copy of another cannot be weighted independently. | **A** | OPEN | Run `test_constant_multiple_sweep` on a fresh board; NAME the field. Do not regenerate to green. **recheck 08-19** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** |
| 3 | **Board's published ranks disagree with its own vorp ordering** (`test_A_ZERO_BONUS_REPRODUCES_THE_BOARDS_OWN_RANKS`). No artifact involved — internal inconsistency in the board Cory drafts from. | **A** | OPEN | Same fresh board; identify which players' ranks disagree. **recheck 08-19** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** **❌ FALSE — the test this row names PASSES.** `python3 -m pytest draft/tests/test_qb_scoring_arbitrage.py::test_A_ZERO_BONUS_REPRODUCES_THE_BOARDS_OWN_RANKS` → **1 passed**, on the 05:33 board. The row's entire evidence is that named test, and it does not fail. **Left OPEN for A to close.** |
| 4c | 🔴 **The war room is computing every pick number for the WRONG SEAT, and it shipped to Cory as a demo.** Its own banner: 3 keepers → he owns 12 picks in a 15-round draft, the board gives him 15, the seat is set to someone with 0 keepers, and *"every pick number, survival % and timing call on this page is computed for the wrong seat."* | **B** | 🔴 OPEN | Fix the seat, **or make the page REFUSE to compute rather than compute wrongly.** A page that knows its inputs are wrong and renders confident numbers anyway is the worst of both. **recheck 08-19** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** **✅ PREMISE IS FALSE ON THE CURRENT BOARD AND THE CURRENT PAGE — relay, 2026-08-18, verified in a real browser against a real war room, not read off the artifact.** **The board:** `pick_order.my_picks` is **12 entries** — 33, 48, 53, 68, 73, 88, 93, 108, 113, 128, 133, 148 — with `my_picks_before_keepers` carrying the 15 and the three keeper-consumed picks (8, 13, 28) sitting only in the latter. `forfeited` names Chase/Henry/Walker at `cost_round` 2/1/3, all `team_slot: 8`. The numbering model is documented and verified: *"a keeper occupies his pick slot; the pick is not removed and nothing after it shifts up — verified against 2023/2024/2025, 150 picks and round 4 at overall 31 every year."* **The page:** rendered `/admin/warroom` as commissioner and read the DOM. It says **"🟢 YOU ARE UP · pick 33"** and **"seat 8"**. Pick 33 is exactly `my_picks[0]`. **None of the keeper-consumed picks appears as his.** **ALSO RETIRED BY THE SAME PROBE:** `#seat-plan` IS present in the DOM, so the separate blocked item *"ONE LINE UNBLOCKS THE SEAT PANEL: the view needs `<div id="seat-plan"></div>"* is stale too. **Left OPEN for B to close** — the owner confirms — but this row describes a demo state that no longer exists, and it is the third BLOCKING row today whose premise did not survive being checked (see rows 1 and 4). |
| 4d | 🔴 **Cory drafts on DESKTOP, and A's live order to B says PHONE-FIRST.** `7ee6f993` (16:06 08-17) specifies *phone-first* and *the 390px column*; the assumption dates to 08-13. Cory today: *"I will be using from desktop so that screen is most important."* B is about to rebuild the war room for the wrong device with 5 days left. | **A** | 🔴 OPEN | Invert the device priority; keep the rest of A's order. Relay has told B directly, default stated. **recheck 08-19** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** |
| 4e | **The shortlist is not sorted by the number it displays.** Gibbs +156, Allen +64, Nacua +125, Robinson +148, Bowers +81, JSN +97 — not descending by anything on screen. | **B** | OPEN | If a composite drives the order, say so on the surface. Under an 8-second pick, a big number that is not the ranking basis is the worst ambiguity available. **recheck 08-19** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** |
| 4f | **"left" means two different things in one glance.** RUNNING OUT: WR **2**, TE **2**, TAKE NOW. BIG BOARD directly below: WR **20 left**, TE **20 left** — and `20 left` is IDENTICAL across all four positions, the identical-value-across-many shape. Subtitle says *twenty deep*, so the label most likely means "20 shown". | **B** | OPEN | One word, one meaning per screen. Rename the mislabelled one. **recheck 08-19** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** |
| 4h | ✅ **CLOSED — `main` CI IS GREEN. Verified on the runner, not locally.** Run `32064471617`, sha `dea76e46`, and the proof is the job's own silent-skip sentinel: `ran: JS suite (conclusion='success')` · `ran: Python suite (conclusion='success')` · `ran: ParityPins suite (conclusion='success')` — **all three RAN and all three passed**, which is the distinction that step exists to make. **THE CAUSE WAS THE CHECKOUT.** `ci.yml` used a bare `actions/checkout@v4` — default `fetch-depth: 1`, one commit, no parents — and **two guards broke on that one line.** *(1)* `test_core_needs_no_reviewer.py`'s NORMAL-path arm runs `independent_review.py --base HEAD~1 --head HEAD`; `HEAD~1` cannot resolve at depth 1, so the reviewer refused an empty diff and exited 1. **The test was right; the environment could not satisfy its premise.** *(2)* **The worse half:** the merge-completeness step is gated on `git rev-parse -q --verify HEAD^2`, which never resolves at depth 1 — so on a **real merge commit** it printed *"not a merge commit — check N/A"* and passed. `ac4b2253` has two parents and its run `32055856888` printed exactly that. **The guard against a half-landed merge could not fire on a merge.** **⚠️ MY EARLIER LEAD ON THIS ROW WAS WRONG, KEPT AS A CORRECTION RATHER THAN DELETED:** it blamed unpinned NumPy vs `np.find_common_type`. That deprecation warning is real, harmless, and emitted by pandas **on a green run** — a plausible story built from a warning instead of from the failing log. **The log was retrievable the whole time and named one test in one line.** The lesson is the cheap one: read the failure before theorising about it. | relay | ✅ CLOSED | Fix shipped as `dea76e46` (`fetch-depth: 0` + `tools/test_ci_checkout_depth.py`, which reads the requirement from the workflow via `yaml.safe_load` and ships a known-positive control that refuses a bare checkout and an explicit `fetch-depth: 1`). **⚠️ THE MERGE WAS A'S CALL AND I TOOK IT** — recorded in `ROUTES.md` with an explicit *ratify or reverse*; A can `SEND BACK` and I revert. |
| 4i | 🔴 **THE WAR ROOM'S "⏮ RESTORE THE MEASURED CORE" BUTTON IS A SILENT NO-OP, AND IT IS ON THE SURFACE CORY DRAFTS ON.** `app.js:762` reads `state.frozenBaseline.engine_policy.MEASURED_WEIGHTS`; **`draft/data/pre_draft_freeze_2026.json` has no `engine_policy` key at all** (verified: `'engine_policy' in freeze` → `False`). So `w` is `undefined`, the handler hits `if (!w) return;`, and **the button renders, Cory clicks it mid-draft, and nothing happens with no message.** A control that looks present and does nothing is this repo's named defect class, now sitting on the draft-night surface with five days left. **Found 2026-08-17 while answering register 25 / queue item D4 — not by anyone testing the button.** | **B** | 🔴 OPEN | Either **write `engine_policy.MEASURED_WEIGHTS` into the freeze** when it is taken (the freeze is the immutable reference, so it should record the policy it froze), **or make the button REFUSE VISIBLY** — "no policy recorded in this freeze" beats a dead click at 8s/pick. **The published board has the same hole:** `public/draft_data.json` carries no `engine_policy` either, so **no artifact records which weights produced it.** unblocked by nothing, owner B, recheck 08-19. **❌ FALSE — verified 2026-08-18 in a real browser, and the cause is instructive rather than careless.** **The button renders and works.** `/admin/api/baseline?version=v1` returns `{ok:true, hasPolicy:true}`; `#baseline-restore` and `#restore-baseline` are both in the DOM; the label reads **"⏮ Restore the measured core — frozen 2026-08-10"**; clicking it succeeds. **THE ROW CHECKED THE WRONG FILE.** It verified that `draft/data/pre_draft_freeze_2026.json` has no `engine_policy` — **true, and irrelevant.** `loadFrozenBaseline()` fetches `/admin/api/baseline?version=v1`, which `src/routes/admin.js:1330` serves from **`draft/baseline/v1.json`**, and that file DOES carry `engine_policy.MEASURED_WEIGHTS` = `{value:1, tier:0, need:0, risk:0, ceiling:0, keeper:1, bye:0, stack:0.5}`, frozen 2026-08-10. The name `pre_draft_freeze_2026.json` is exactly where a frozen baseline sounds like it should live, which is why this was a reasonable mistake. **AND EVEN THE FAILURE MODE WAS MIS-STATED: it could never have been a SILENT NO-OP.** `renderBaselineControl()` opens `if (!b || !b.engine_policy) { host.innerHTML = ''; return; }` — **no policy means the button is not rendered at all.** A button that is absent cannot mislead under an 8-second clock; a silent no-op can. The code was already defensive in exactly the way the row asks for. (The cited line is also off by 28 — the read is `app.js:790`, not 762.) **Left OPEN for B to close; fourth blocking row today whose premise did not survive being checked.** |
| 4j | 🔴 **THE BOARD STILL CANNOT SAY "SAME PROJECTION, DIFFERENT UPSIDE" — MEASURED, 08-17 EVENING.** Every dispersion field is a **pure deterministic function of `(position, proj_mean)`**. The test that settles it: **0 of 535 players share a `proj_mean` and differ on ANY of `proj_ceiling`, `proj_floor`, `proj_sd`, `weekly_sd`, `variance`.** Not one. Within position, `Spearman(proj_mean, ·)` runs **ceiling .984–.9994, floor .898–1.0000 (WR is EXACTLY 1.0000), proj_sd .917–.989, weekly_sd identical to proj_sd at every position** (so it is a rescale of it). **THIS IS NOT THE OLD CONSTANT-MULTIPLE BUG AND I AM NOT CLAIMING IT IS** — that one had a single ratio per band; today's ratios genuinely vary (RB 1.43–1.89, QB 1.09–1.48), so the measured p90/p10 calibration is real and is an improvement. **What it is NOT is player-specific.** `CLAUDE.md` says the 08-17 work means *"a real per-player upside signal now exists"*; the measurement says it is a per-PROJECTION-LEVEL upside signal. Carnell Tate (rookie WR, ADP 72) and Courtland Sutton (established, ADP 91) both project 144.8 — so the board gives them **the identical ceiling 190.75, identical floor, identical sd**, because nothing else about a player can reach those fields. **WHY IT MATTERS BEYOND TIDINESS, AND IT EXPLAINS TWO STANDING PUZZLES.** (1) Because ceiling is ~monotone in the mean within a position, **the `ceiling` term cannot reorder players within a position** — which is exactly why the measured blast radius of `ceiling 0 → 0.45` is *top-60 unmoved at picks 33/48/68, top recommendation never changes*. It is not that the weight is small; it is that within a position it is a monotone transform of the `value` term sitting beside it. (2) It re-raises the ORIGINAL reason ceiling was zeroed — *"collinear with value"* — which 08-17 attributed to the constant-multiple artifact. **The collinearity is now .98–.999 rather than 1.0000: better, not different in kind.** | **A** | 🔴 OPEN | **Do not touch this before 08-22** — no change-before-draft, and the ceiling ruling stands. **After:** decide whether upside should carry ANY player-specific input (age / role / rookie or year-2 status / target-share volatility / Vegas), because until something does, *"who has more upside than his projection suggests"* is a question the board is structurally unable to answer — **and that question is most of what Cory means by edge.** Note `variance` is the ONE field with independent ordering info (Spearman to mean: QB −.89, RB −.74, WR −.47, TE **+.21**) — **the sign flips at TE**, which is its own thing to explain. unblocked by nothing, owner A, recheck 08-23. |
| 4k | 🔴 **CORY ASKED WHY FLOOR AND CEILING ARE STALE. ANSWER: NOTHING REFRESHES THEM AND NOTHING CHECKS THEM.** Every `proj_ceiling` and `proj_floor` on the board descends from `draft/backtest/projection_error_calibration.json`, **built 2026-08-14 23:11 and never since.** Three findings, each verified: **(1)** `projection_error.calibrate()` is called by **nothing** in the repo outside its own tests — the file was produced once, by hand. **(2)** **No workflow regenerates it** (`learning_loop.py` only READS it, line 207). **(3)** It is **not in `draft/data/artifact_registry.json` at all** — not among the 13 checked `entries`, not even among the 31 `_not_yet_migrated` — so `check_artifact_freshness.py` has never looked at it and never would have. **The freshness system this project built specifically to end artifact staleness does not cover the field Cory asked about.** For scale: that same check reports **11 of 13 registered artifacts STALE right now.** | **C** | 🔴 OPEN | `projection_error.py` is C's module. **(a) Give `calibrate()` a regeneration entry point** and **(b) register the artifact** so its staleness is visible from the next build onward. ⚠️ **DO NOT regenerate before 08-22** — it would move every ceiling and floor on the board four days before the draft, and the no-change-before-draft rule holds. **Register it now, regenerate after.** unblocked by nothing, owner C, recheck 08-19. |
| 4m | 🔴 **CORY: "adjusters seem muted… should be searching for more upside in later rounds." HE IS RIGHT, AND THERE ARE THREE INDEPENDENT REASONS AT ONCE — plus one that invalidates the evidence we shipped `ceiling = 0.45` on.** **(1) `opportunity_adj` IS EXACTLY 0.0 FOR ALL 535 SKILL PLAYERS** — not "229 of 603" as Q1 recorded. **This one is CORRECT and is Cory's own ruling** (`opportunity_cap = 0.0`, "Remove 1", after the layer graded NEUTRAL on ordering in 17/18 cells, WORSE on level in 18/18, and a shuffled control performed identically). The signal still exists — `opportunity_z` is non-zero for 377 of 527 RB/WR/TE — but nothing consumes it. **(2) THE CEILING TERM IS ZERO FOR HALF OF CORY'S DRAFT.** `CEILING_LATE_FROM = 0.6` zeroes it until pick 90 of 150. **Measured at his real picks: 33, 48, 53, 68, 73, 88 → ramp 0.00. Six of twelve.** First non-zero is 0.05 at pick 93. **(3) EVEN WHEN IT TURNS ON IT CANNOT REORDER WITHIN A POSITION** — register 4j: ceiling is a monotone function of `(position, proj_mean)`, ρ .984–.9994. So the late "upside search" re-ranks nobody against his own position. **⚠️ (4) AND THIS IS THE ONE THAT MATTERS MOST — THE BLAST-RADIUS MEASUREMENT THAT MADE `ceiling = 0.45` LOOK SAFE IS UNINFORMATIVE.** `engine.js` reports *"0 of the top-60 move at picks 33, 48 and 68"*. **Those are three of the six picks where the ramp is structurally 0.00.** The measurement did not show the weight is harmless; **it measured a term that was switched off.** Cory ruled the weight non-zero to get upside, and the gate then zeroes it for half his picks. **(5) The engine's OWN comment says the proxy is wrong:** *"Measured, that happens near pick 70"* — so the bench branch fires on the real condition via `gateOpen` while the composite waits for pick 90. **Two definitions of "late" in one engine, and picks 73 and 88 fall in the gap.** | **A** | 🔴 OPEN | 🛑 **MEASURED 08-17 — AND MY OWN RECOMMENDATION WAS WRONG. DO NOT LOWER `CEILING_LATE_FROM`; IT CHANGES NOTHING.** Built `draft/tools/ceiling_gate_blast_radius.js` and ran it at Cory's twelve real picks. **Opening the gate from pick 90 to pick 71 moves ZERO of the top 60 at EVERY pick, and never changes the top recommendation.** The constant I asked A to change is inert. **WHAT THE SAME HARNESS FOUND INSTEAD, AND IT IS INVERTED FROM BOTH THE DESIGN AND FROM WHAT CORY WANTS.** Swinging the ceiling WEIGHT 0 → 9 (20× the shipped 0.45): **picks 33/48/53/68 move 8/19/16/12 of the top 60, with the top recommendation CHANGING at 48 and 53 — while picks 108/113/128/133/148 move 1/1/0/0/0.** The ceiling term acts **EARLY and barely at all LATE**, because the bench branch scores on `wCeilPub` and `gateOpen` and never sees the lateness ramp, while the composite branch's ramped term is the collinear one (4j). **Cory asked for more upside in LATER rounds; the weight is doing its work in the EARLY ones and going quiet exactly where he wants it.** **⚠️ THE HARNESS'S FIRST VERSION WAS ALSO WRONG AND THE ERROR IS RECORDED:** it filled all seven starting slots at every pick, firing the bench branch at pick 33 where nobody has seven players. It reported 20 moving at a pick whose ramp is arithmetically 0.00 — **two numbers from one tool contradicting each other, which is how the flaw surfaced.** The roster now grows with the pick (3 keepers + one per completed round). **The numbers above are from the corrected harness.** ⚠️ **This is pre-draft-relevant and it is exactly what Cory asked for**, but it IS a change before 08-22: A rules whether it clears the no-change bar, and the honest framing is that the current state does not do what Cory's own ruling intended. **unblocked by nothing, owner A, recheck 08-19.** |
| 4n | 🔴 **`adp_sd` HAS 617/617 "COVERAGE" AND IS MOSTLY A DEFAULT VALUE — 60% OF THE BOARD SHARES ONE NUMBER.** Measured 08-17 while building a player-specific ceiling: **369 players sit at exactly `30.0` and 108 more at exactly `15.0`.** Per position the median is **30.00 with MAD = 0.0000 for QB, WR and TE** — i.e. **no dispersion at all in three of four positions**; only RB carries a real distribution (median 18.10, MAD 17.64). **This is the identical-value-across-many signature: coverage that is not information.** **IT ALREADY COST SOMETHING.** `UPSIDE-PLAYER-SPECIFIC-PREREG.md` named `adp_sd` as the arm to watch — *"617/617 coverage, 131 distinct values"* — on a distinct-count that hid the saturation. **A prereg written to prevent post-hoc stories picked its favourite arm from an unchecked distribution.** Corrected in the prereg itself rather than deleted. And the prepared fix built on it (`apply_player_specific_ceiling.py`) silently applied to **RB ONLY**, which is verbatim the defect that killed the opportunity layer — *"lifting three positions against three others in a VORP that compares across positions"*. **Not sent for approval; it would have rebuilt the layer Cory already removed.** | **C** | 🔴 OPEN | **Find out WHY.** Is 30.0 an FFC default for unranked/deep players, a clamp in our own capture, or a genuine ceiling in the source? `adp_sd_source` says `ffc-published`, so the answer is one fetch away. **If it is a default, `adp_sd` must be NULL where unmeasured, not 30.0** — missing-vs-zero, the same rule `opportunity_share` already follows. **A field that says "30.0" for 369 players is telling every consumer they are identical when it means it does not know.** unblocked by nothing, owner C, recheck 08-19. **✅ RE-MEASURED 2026-08-18 ON THE 05:33 BOARD — STILL TRUE, essentially unchanged.** 696 players carry `adp_sd` across 149 distinct values, but **369 sit at exactly 30.0 (53%) and 111 at exactly 15.0 (16%) — 69% of the board on TWO numbers.** The row said 369 and 108; it is now 369 and 111. **This row is real and should stay open.** Recorded because three other blocking rows checked today had premises that were false, and this one is not. **📐 REFINED 2026-08-18 — THIS ROW IS TRUE BOARD-WIDE AND THE DEGENERACY IS ENTIRELY OUTSIDE CORY'S DRAFT RANGE. But chasing it found a DIFFERENT live issue in his range.** **Where the degenerate values actually sit:** ADP 1-160 (his window) — 155 players, **109 distinct `adp_sd` values, ZERO at 30.0, two at 15.0 = 1% degenerate**. ADP 161-300 — 59% degenerate. ADP 301+ — **100%, two distinct values across 387 players.** So the 369/111 concentration is real and it is almost entirely in players nobody drafts. **Same shape as row 4v: a genuine board-wide defect with negligible reach into the picks Cory makes.** **THE LIVE ISSUE IS THE CAP, NOT THE DEFAULT.** `survival.js:201` — `adpSd(adpMean, provided)` returns **`provided` verbatim whenever it is > 0**, and only falls back to `min(CAP, max(FLOOR, RATE × adp))` when it is missing. `CFG.ADP_SD_CAP` is **15.0**. **So a supplied `adp_sd` bypasses the model's own ceiling entirely — 435 players board-wide carry one above it, 29 of them inside Cory's range, up to 24.2 (1.6× the cap).** **MEASURED COST, at each affected player's OWN nearest pick boundary rather than an arbitrary one:** worst distortion **8.7 percentage points** (Fairbairn, pick 133). **The ones that matter are TEs in the 90-110 band: Dalton Kincaid 7.3pp at pick 93, George Kittle 6.5pp at pick 108** — real skill players, real picks, a 6-7pp error in *"will he still be there"*. Everything else is kickers and deep RBs where it rounds away. **RECOMMENDATION (post-08-22, one line): apply the cap to provided values too** — `return Math.min(CFG.ADP_SD_CAP, provided)`. **NOT before the draft**: it moves a survival number on the board under the no-change rule, and 8.7pp on a kicker is not worth a pre-draft change to the engine. **This row should stay OPEN and be re-scoped from *"adp_sd is mostly a default"* to *"provided adp_sd bypasses the survival cap"*, which is the half that reaches Cory.** |
| 4p | 🔴🔴 **THE BOARD SAYS LATE PLAYERS HAVE PROPORTIONALLY LESS UPSIDE THAN EARLY ONES. THAT IS BACKWARDS FROM EVERY FANTASY THEORY, AND CORY CAUGHT IT BY INSTINCT.** Measured 08-17, median `proj_ceiling / proj_mean` by ADP band: **1-24 → 1.640 · 25-60 → 1.506 · 61-100 → 1.434 · 101-150 → 1.434 · 151-250 → 1.434 · 251+ → 1.317.** **The ratio DECLINES with ADP.** A round-12 flier is the textbook definition of high variance relative to his projection; the model gives him the TIGHTEST distribution on the board. **Cory: *"so even in later rounds your saying i shouldnt look for upside? this goes against every fantasy footbal theory ever and doesnt make sense."* He is right and the number agrees with him.** **⚠️ IT ALSO MAKES A SHIPPED CONCLUSION CIRCULAR.** `use_measured_ceiling`'s own justification says turning it on *"moves the board TOWARD anchors and away from deep fliers"* and cites the barbell study finding *"anchors out-ceiling swings with ZERO OVERLAP."* **With `proj_ceiling` a monotone function of `proj_mean` (4j) AND the ratio favouring early picks, anchors out-ceiling swings BY CONSTRUCTION.** That study could not have returned any other answer. **It is not evidence about football; it is a restatement of the formula.** **AND THE THREE IDENTICAL 1.434s ARE THE MECHANISM SHOWING THROUGH:** bands 61-100, 101-150 and 151-250 share one ratio to three decimals, i.e. one calibration cell spanning 190 ADP slots. Inside a cell, ceiling IS a constant multiple of the mean — the exact defect 08-17 claimed to have fixed, still live wherever a cell is that wide. | **A** | 🔴 OPEN | **This is the root cause of "adjusters seem muted" (4m) and it outranks both the weight and the gate.** **Do NOT patch the ratio by hand before 08-22** — it feeds ceiling, floor, the bench branch, `champodds` and the money proxy. **DO, now: re-read the barbell/anchor conclusion as UNPROVEN** rather than as evidence, since it cannot fail. **DO, after 08-22:** the p90 calibration must be fit on realized outcome SPREAD, not on projected-rank band — a deep player's realized/projected ratio is empirically wide, and the current cells cannot represent that. `UPSIDE-PLAYER-SPECIFIC-PREREG.md` is the vehicle. unblocked by nothing, owner A, recheck 08-19. **A RULES, 2026-08-17: UNPROVEN.** The relay's circularity argument is correct — with a per-band constant ratio, ceiling is a monotone function of the mean, so anchors out-ceiling swings BY CONSTRUCTION and the comparison could not have come out any other way; no number changes, the barbell verdict text must stop citing it as a measured trade-off. The refit workflow just merged is the instrument that can answer it properly (per-band slopes on the regenerated calibration). |
| 4q | 🔴🔴🔴 **THE FIX IS IDENTIFIED AND IT IS ONE BAND. 72% OF THE CALIBRATION SITS IN A SINGLE CELL PER POSITION.** `projection_error_calibration.json` n by band: **1-3 → 36, 4-8 → 59, 9-16 → 94, 17-32 → 180, and `33+` → 935 of 1,304.** Ranks 1-32 get FOUR bands; **ranks 33 to 300+ get ONE** — which is every player Cory drafts from round 4 on. **This is the mechanism behind 4p and 4j both:** inside a cell `proj_ceiling` IS a constant multiple of `proj_mean`, so bands 61-100 / 101-150 / 151-250 shared the identical ratio 1.434 (all RB|33+), and the board cannot tell a round-5 breakout from a round-15 dart throw **because it assigns them the same number by construction.** **AND THE ONE NUMBER IT ASSIGNS IS BIASED TOWARD 'NO UPSIDE' BY ZERO-INFLATION.** `WR|33+` pools **403 players**, most of whom never played a meaningful snap. The unconditional p90 of realized/projected over that pool is dominated by non-participants, so it reports 1.317 for WR and **1.092 for TE** — lower than every band above it. **That is not a finding that deep players lack upside; it is the arithmetic of averaging 270 ranks of mostly-zeros into one cell.** Cory's football objection — *"this goes against every fantasy footbal theory ever"* — is correct, and this is the line of code behind it. | **C** builds · **A** rules | 🔴 OPEN | **THE FIX: split `33+` in `projection_error.band_of()` and refit.** n=935 is ample for 33-48 / 49-72 / 73-100 / 101-150 / 151+ at every position. **Also decide the population question, which is the half that matters more:** the p90 should be conditioned on players who were actually ROSTERED/PLAYED, or reported as two numbers (P(meaningful role) × upside given a role), because *"most deep players bust"* and *"a deep player who plays has huge upside"* are both true and the current single number conflates them into the wrong answer. ⚠️ **BLAST RADIUS IS WIDE — ceiling, floor, bench branch, `champodds`, money proxy — so this is CORY'S call, not a lane's, on whether it ships before 08-22.** The relay's recommendation: **refit and MEASURE it now, ship only if the replay in Cory's seat improves**; a fix that cannot be graded before Saturday should wait. **unblocked by nothing, owner C, recheck 08-19.** **A RULES, 2026-08-17 (band-refit run 32079634973): the band SPLIT does NOT ship — measured, not vetoed.** Split bands move deep cells +0.02..+0.12 but the slope stays INVERTED (deep 1.3378 vs shallow 1.7197); the comparator's own reading: arithmetic, not evidence. 4q (role-conditioned p90 population) is the live hypothesis and the next preregistration; the current calibration stands for 08-22. |
| 4r | 🔴🔴🔴 **A'S "NO SHIP" RULING ON THE BAND SPLIT WAS MEASURED ON A CONTAMINATED POPULATION — IT HAS TO BE RE-RUN.** Timeline: **22:11** `1c8bfb90` regenerates the calibration; **23:16** `a8158515` rules NO SHIP *"with the run's own numbers"* (deep 1.3378 vs shallow 1.7197). **Both went through `draft/backtest/cli.py`, and `cli.py` NEVER PASSES `positions` to `PE.error_rows()`, whose default is `None` — no filter.** So the fit includes every position in the bundle: **P (punters) 9, DB 4, LB 1, T 1, FB 20** — none of which this league rosters — while every skill position lost ~30% of its graded players (**QB 186→134, RB 335→215, WR 497→336, TE 286→190; graded 1,304→910; unmeasurable cells 0→15 of 32**). **THE RULING MAY STILL BE RIGHT — THAT IS NOT THE POINT.** A concluded the band split does not repair the inverted slope, and the role-conditioned population is now A's live hypothesis, which matches register 4q's own "harder half". **But the number it rests on was fitted on punters and is missing a third of the skill data, so it cannot support NO SHIP or SHIP.** Re-run on a clean population, then rule. **THE ARTIFACT IS ALREADY REVERTED** (`88b58a5d`, restored to 1,304 graded / 0 unmeasurable / skill positions only) because `main` was red on it — 11 tests including `test_upside_ordering_now_differs_from_projection_ordering` and the whole `test_player_spread_in_sd` suite. **The PIPELINE defect is NOT fixed:** the next regeneration will re-contaminate unless `cli.py` passes the skill positions. | **C** fixes the filter · **A** re-rules | ✅ RESOLVED 08-18 | **(1) `cli.py` must pass `positions=('QB','RB','WR','TE')` to `calibrate()`** — the parameter already exists and is simply never used. **(2) Re-dispatch `band-split-refit` and re-rule.** **(3) A test that FAILS if a non-rostered position ever enters the calibration** — this was invisible because nothing asserted the population, and it moved every ceiling and floor on Cory's board four days before his draft. unblocked by nothing, owner C, recheck 08-19. **RESOLVED 08-18 (A):** both real fixes unified on `main` — C's `only_positions` filter API on every call path (cli.py AND projection_error.regenerate(), the path the workflow runs) + the `_rostered_only()` population wrapper; the inert `positions=` map-not-filter trap is pinned behaviorally (test_calibration_population.py feeds real rows and checks what comes out). Clean regeneration landed 08-18 (run #5, after a band_edges=None crash in run #4 was fixed): **seasons [2023,2024,2025], graded 1,320, 20/20 cells measured, QB/RB/WR/TE only.** The band-split re-rule on the clean population is registered at 4q and is still owed. |
| 4s | 🔴 **THE POSITION FILTER WORKED — AND THE REGENERATION SILENTLY LOST 2025, THE MOST RELEVANT SEASON.** Re-dispatched 2026-08-18 on `c77e7fdd`: intruders **GONE** (positions now QB/RB/WR/TE only, unmeasurable cells **15 → 4**). **But `seasons` came back `[2023, 2024]` against the previous artifact's `[2023, 2024, 2025]`, and `graded` fell 1,304 → 875 — the same ~30% loss that was blamed on contamination is actually a MISSING SEASON.** So the calibration behind every `proj_ceiling`/`proj_floor`/`proj_sd` on Cory's board is now fitted without the most recent and most relevant year, four days before his draft. **AND IT IS INVISIBLE BY CONSTRUCTION.** `regenerate()` records the reason (`skipped.append({season, reason})`, both skip paths do it) and then `cal["skipped_seasons"] = skipped` — **but `calibrate()`'s return dict, which is what gets serialised, has no such field.** The artifact therefore carries **no record that a season was dropped or why**. That is why nobody caught it: the file looks complete. **A dropped season that leaves no trace is worse than a crash.** | **C** | ✅ RESOLVED 08-18 | **(1) Persist `skipped_seasons` in the saved artifact** — one field, and it turns a silent loss into a visible one. **(2) MECHANISM FOUND — WE HOLD 2025 AND THE REGENERATION THROWS IT AWAY.** `draft/backtest/nflverse_weekly_points_2025.json` is committed (22 weeks, since 08-14) and is what every other study grades against. **`regenerate()` does not read it.** It re-fetches live — `nfl.import_weekly_data([y])` for 2021–2025 — and any year whose fetch raises is appended to a local `missing` list **that is never surfaced, never persisted, and never fails the run**. With no 2025 weekly rows, `rest_of_season_points(weekly, 2025, …)` returns nothing, the season is skipped, and `save()` drops the skip record. **Three silent layers stacked: a swallowed fetch error, an unsurfaced `missing` list, and an unpersisted skip.** So the artifact behind every ceiling and floor can quietly lose the most recent season to a transient network blip while reporting success. **THE FIX IS TO PREFER THE COMMITTED STORE and treat a live-fetch failure for a season we already hold as an error, not a shrug.** **(3) Add to `test_calibration_population.py` a check that `seasons` covers all of `CALIBRATION_SEASONS`, or names what it dropped.** **📏 THE HOLD IS NOW MEASURED, NOT CAUTIOUS. Cory asked "why does 2025 matter to the board?" — diffed the two fits cell by cell:** mean change in the ceiling multiplier is **0.179**, i.e. **±36 points of ceiling on a 200-point projection** — larger than most gaps being decided at a real pick. **The single biggest move is `RB|33+`: 1.434 → 1.794 (+0.361)**, the deep-running-back band that governs every late-round upside call from round 4 on. `WR|9-16` +0.336, `WR|33+` +0.173, `QB|17-32` −0.142. **AND IT POINTS THE WRONG WAY FOR THE RIGHT-SOUNDING REASON:** dropping 2025 makes deep RBs look ~25% more explosive, which is the direction Cory has been arguing for — but it is not evidence about football, it is what deleting the most recent season does to a sample. Including 2025 pulls deep-RB ceilings DOWN, i.e. 2025 was a poor year for deep RBs against projection. **Second cost: the cells thin out** — `RB|33+` 240 → 151 graded, `WR|33+` 403 → 273 — so it also trades a real season for a noisier estimate. ⚠️ **Do NOT rebuild the board on this artifact until (2) is answered** — a ceiling fitted without 2025 is not obviously better than one fitted with punters. unblocked by nothing, owner C, recheck 08-19. **RESOLVED 08-18 (A):** all three silent layers closed at the source — `regenerate()` now grades a fetch-failed season from the COMMITTED `nflverse_weekly_points_<season>.json` store (recorded with `recovered: true`), the skip list is persisted onto the artifact (`skipped_seasons`), and the seasons-trace pin (test_the_artifact_names_every_season_it_fitted_or_dropped) makes a traceless drop impossible. The clean artifact fits ALL THREE seasons — nothing skipped. The ±36-point ceiling hold is lifted: the board rebuild on this artifact dispatched 08-18. |
| 4v | 🔴 **32% OF CORY'S DRAFT RANGE SHOWS A CEILING THAT CONTAINS NO INFORMATION ABOUT THAT PLAYER, AND NOTHING ON SCREEN SAYS SO.** Per-player volatility tails landed 08-18 and are a genuine improvement (RB: **92 distinct ceiling ratios** where v23 had one per band). **But the fix landed where the problem was smallest.** Measured on the live board across **ADP 25–220, Cory's actual draft range**: `measured-2023-25-p90-x-player-cv` **142 (67%)** · `measured-2023-25-p90` **37 (17%)** · `gaussian_z` **31 (14%)** — so **68 of 210 carry a cohort constant.** Board-wide the coverage is inverted against the complaint that started this: **100% at ADP 1–24, 69% at 109–160, 46% at 161–300, 24% at 300+, and 0 OF 66 ROOKIES** — the volatility term needs realized weeks, which rookies and deep-bench players do not have. **19 rookies sit inside the draft range with a constant ceiling, including Jeremiyah Love at ADP 26.3 stating a 347.6 ceiling.** That is precisely the pick Cory would use a ceiling to justify. | **B** renders | 🔴 OPEN | **DISPLAY FIX, NOT A MODEL FIX — `proj_ceiling_source` is already on every row with three values.** Render the ceiling differently when it is not `…-x-player-cv` (grey/asterisk) so a cohort constant is visibly not a claim about that player. **A real rookie ceiling is preregistered as ledger P9, graded 09-05, deliberately AFTER the draft** — a new dispersion model four days out is what the no-change rule exists to stop; marking the existing one costs one conditional. **DEFAULT: mark them.** unblocked by nothing, owner B, recheck 08-21. **✅ RE-MEASURED 2026-08-18 — TRUE, BUT THE NUMBER IS STALE IN THE GOOD DIRECTION AND BY A LOT.** In Cory's actual pick window (ADP 27-160, bracketing `my_picks` 33→148, 132 players): **`measured-2023-25-p90-x-player-cv` 108 (82%) · `measured-2023-25-p90` 15 (11%) · `gaussian_z` 9 (7%).** **The row claims 32% of his draft range shows an information-free ceiling. It is 7%** — the per-player volatility work that landed 08-18 did what it was meant to. **AND THE REMAINING 9 ARE ALL K AND DEF — not one skill player in his range is affected:** Brandon Aubrey, Ka'imi Fairbairn, Cameron Dicker, Jason Myers (K, ratios 1.290-1.307) and Houston, LA Rams, Denver, Seattle, Philadelphia (DEF, ratio 1.394 every one — a position constant, exactly as row 2e's *"K/DEF had no calibration cells"* honest refusal predicts). **So the practical statement is: EVERY SKILL PLAYER IN CORY'S DRAFT RANGE NOW CARRIES A MEASURED, PLAYER-SPECIFIC CEILING.** The row's *"nothing on screen says so"* half may still stand — `proj_ceiling_source` IS present on every player, so the page COULD distinguish them, and I did not check whether it does. **Owner should close or re-scope; the 32% is not the current number.** |
| 4x | 🔴🔴 **THE DOCTRINE BANNER CANNOT ANNOUNCE A STRATEGY SWITCH ON DRAFT NIGHT — the scores are DEGENERATE and the leader gap is always 0.000.** Found while diagnosing the last red on `main`: `robot-mock.js` **146/148**, both failures being *"a QB run triggers EXACTLY ONE switch announcement"* → **0 announcements**. **MEASURED on the live board, the test's own scenario:** nine doctrines produce **THREE distinct scores.** Full board: `balanced`, `ceiling`, `zero_rb`, `hero_rb`, `elite_te`, `early_qb` **all exactly 94.700**; `late_qb` and `robust_rb` 88.510; `wr_anchor` 80.530. After the QB run: six tie at **88.510**, `zero_rb`/`early_qb` 84.750. **`early_qb` PRICES THE RUN CORRECTLY (94.70 → 84.75, −9.95) — the input is fine.** The failure is that **six doctrines tie for the LEAD, so `leader − runner_up` is 0.000 in both states, and `DoctrineState.update` only counts a challenge when `gap > noiseBand` (4.0).** 0.000 > 4.0 is never true, so the hysteresis counter never advances and **no switch can ever be announced, for any board event.** 🛑 **CORRECTION, SAME NIGHT — MY FIRST DIAGNOSIS WAS WRONG AND A'S ORIGINAL SUSPICION WAS RIGHT. IT *IS* THE `$4` BAND.** I read the switch gap as `leader − runner_up` and concluded the ties made it structurally zero. **The code computes `gap = leader.score − curScore` — leader minus the CURRENT doctrine** (`doctrine.js:446`), so the ties are irrelevant. **Measured by running the real state machine through the test's four steps:** after the run `early_qb` sits at **84.75**, the leader (`Balanced Value`) at **88.51**, so **gap = 3.76 against a band of 4.00 — it misses by $0.24** and is suppressed as *"a flap, not a signal."* **THAT IS EXACTLY THE ~18% DOLLAR-SCALE SHRINK A ALREADY FLAGGED:** 3.76 / 0.82 ≈ **4.59**, which cleared $4 comfortably on the old scale. **A fixed band on a shrunken scale is the whole defect**, and the pre-run step confirms the machine is otherwise healthy — gap **6.19** at `a`, correctly no switch on a first-ever call. | **A** | 🔴 OPEN | **RE-DERIVE `DG_NOISE_BAND` FROM THE CURRENT DOLLAR DISTRIBUTION — the work already asked for in D's inbox** (*"the $4 even-money bands sit on a dollar scale that shrank ~18%… find what the $4 was originally derived from; if the answer is 'a chosen round number,' say so"*). **This is the evidence that it is urgent rather than tidy-up: at $4 the QB-run alert cannot fire by $0.24.** Do NOT simply lower it to 3.5 to make the test pass — that is fitting the constant to one scenario. Derive it from the gap below which doctrine choice does not separate outcomes, on THIS board's scale. **The doctrine ties are real but are NOT the bug** — `scoreBoardDetail` scores each doctrine as the best player it is ALLOWED to take, so doctrines that permit the board leader tie by design, and the code already says so: *"when every doctrine ties, that is itself the finding: this pick is doctrine-neutral."* **The QB-run alert is a real draft-night capability and it is silently dead** — Cory would get no warning that the room is running the position. Test the fix with the robot-mock scenario, which already pins the behaviour correctly and is the reason this was found at all. unblocked by nothing, owner A, recheck **08-19** (before the draft). |
| 4w | 🔴🔴🔴 **BLOCKING, FOUR DAYS OUT: THE NEW PER-PLAYER CEILING APPLIES A *WEEKLY* VOLATILITY RATIO TO A *SEASON* PROJECTION, AND IT PUTS PHYSICALLY IMPOSSIBLE NUMBERS ON THE PICK-1 ROW.** Landed 08-18 in baseline v24. **THE MECHANISM, measured:** the board's season `proj_ceiling/proj_mean` tracks each player's **weekly** p90/p50 from `nflverse_player_ceiling.json` — Gibbs weekly **1.916** → board **2.263**; A.J. Brown weekly **2.078** → board **2.738**; Lamar weekly **1.621** → board **1.940**; CMC weekly 1.439 → board 1.312. **A season total is the SUM of ~17 weeks, so its spread is roughly the weekly spread ÷ √G, not equal to it.** A player whose best WEEK is 1.9× his median week does not have a best SEASON 1.9× his median season. **THE CONSEQUENCE:** **31 players carry a stated ceiling above the best season ever recorded at their position** (28 of them `-x-player-cv`), and **30 are inside ADP 1–220**: **Jahmyr Gibbs ADP 1.0, ceiling 678.7** (best RB season ≈420); **A.J. Brown 568.8** (best WR ≈400); **Lamar Jackson 721.6**; Bijan 505.6; Jeanty 495.0; Jonathan Taylor 494.2. **36 players exceed 400 points.** ⚠️ **AND THE CEILING WEIGHT IS LIVE AT 0.45**, so these feed `recommend()` — the 0-vs-0.45 blast radius A measured was taken on the OLD ceiling and no longer bounds this. **This is register 4t's defect inverted: we went from a ceiling that was too flat to one that is too big, and both come from applying a group/other-scale statistic to an individual season.** | **A** | ✅ RESOLVED 08-18 | ✅ **VERIFIED INDEPENDENTLY BY THE RELAY (the finder, not the fixer) ON THE PUBLISHED v25 BOARD, 08-18:** A took option (2), the correct one, and built the rail on a better basis than this row proposed — **measured** best realized 2023–25 season +20% (QB 674 / RB 525 / WR 451 / TE 303) rather than the eyeballed maxima I filed with. **Gibbs 678.7 → 525.0.** Max ceilings now **QB 582.6 / RB 525 / WR 451 / TE 303**. **Only 4 of 535 players sit exactly on a rail** (Gibbs, St. Brown, Bowers, A.J. Brown) — so it backstops rather than flattens, which was the obvious way this fix could have recreated 4j at the top of the board. **AND THE 4j REPAIR SURVIVED THE RESCALE, which was the real risk: 63 players sharing a `proj_mean` now DIFFER on ceiling** (4j measured **0 of 535**), with **QB 46 / RB 92 / WR 123 / TE 72 distinct ceiling ratios.** ⚠️ **Residuals, tracked not lost:** rookies remain **0 of 66** with a per-player ceiling (row **4v**, ledger **P9**, graded 09-05, deliberately post-draft), and the **QB rail is the loosest of the four** — 582.6 is live, under the 674 rail but above any real QB season, so at QB the rail bounds the arithmetic rather than the football. ~~**TWO OPTIONS, BOTH SMALL.**~~ **(1) SAFE, IMMEDIATE, RECOMMENDED FOR 08-22: fall back to the cohort p90** for the `-x-player-cv` rows — one flag, restores the 08-17 behaviour, which is flat but not impossible. **(2) CORRECT: rescale the weekly ratio to a season ratio**, `season_ratio ≈ 1 + (weekly_ratio − 1)/√G`. For Gibbs that is **1 + 0.916/√17 = 1.222**, i.e. a ceiling of **367** rather than 679 — plausible, and still per-player, which keeps the 4j repair. √G is an UPPER bound on the reduction (weekly outcomes correlate through role and injury) so it should be fitted, not assumed — but any √G-scaled version beats the unscaled one by a wide margin. **ADD A SANITY BOUND EITHER WAY: no ceiling may exceed the best season ever recorded at that position. Nothing asserted that, which is why this shipped.** unblocked by nothing, owner A, recheck **08-19** (before the draft). **RESOLVED 08-18 (A, ruled option 2 + the rail, same day):** the applied multiplier is now `1+(f_weekly−1)/√G` on BOTH tails (ceiling multiplied, floor divided — projections.py), which keeps the per-player 4j repair and kills every impossible number (Gibbs-class case pinned <1.2× his cell constant); AND the sanity rail is in: no ceiling may exceed the best realized season at the position in our own 2023-25 stores ×1.20 declared headroom (`PLAUSIBILITY_CEILING`, bases regenerated from the stores by a pin so the constant cannot rot). Both pins behavioral in test_player_volatility_tails.py. √G remains an upper bound on the shrink — fitting the true exponent is the post-draft item and rides 4t's merge. Board rebuild dispatched on the fix. |
| 36 | 🟢 **LIFECYCLE CLOSURE (Cory's resource review, 08-18): `advanced_stats_*` (air yards/EPA/CPOE) stops at step 5 WITH a reason now written down** — the 08-16 study's §6 is an honest preregistered NULL for the one construction tested, so the store stays captured (capture bar) and unwired (prediction bar), re-test trigger: a second graded season OR a new preregistered construction. The 08-17 lifecycle audit called this stop 'UNEXAMINED' only because nobody had linked the study to the store — linked now. | **A** | ✅ RESOLVED 08-18 | No action; the null + trigger IS the answer. recheck 09-15 (post-season-start, when week-1 data enables the second construction). |
| 4y | 🟡 **THE ONE UNEXAMINED STORE WITH REAL PRE-DRAFT-CLASS EDGE: `routes_*` (route participation 2021-25, captured weekly, reaches NOTHING — lifecycle stop 4).** Route participation LEADS target share — a rising route rate is the earliest public signal a young WR's role is expanding, which is a breakout leading-indicator the board cannot currently see (opportunity_z is prior-season usage, a veteran bonus). TOO BIG to wire honestly before 08-22 and deliberately NOT attempted. | **A** preregs · **D** grades | 🔴 OPEN | **THE POST-DRAFT #1 STUDY, dated:** prereg by 08-25 — does week-N route-rate delta predict week-N+1..N+4 target share and points, 2021-25, walk-forward, shuffle null — and if it clears, it feeds the WAIVER/START tooling where the in-season edge actually lives. owner A, recheck 08-26. |
| 4z | 🟢 **POST-DRAFT STUDY, FILED FROM Q3'S ONE UNAVERAGED THREAD: do the PROVEN rookie evaluators' DISAGREEMENTS WITH THE CROWD flag rookie busts/breakouts?** Q3 measured (expert_skill_grading.json): rookie-evaluation skill persists both transitions (rho .19/.32, both clear the null) — but slice-WEIGHTING ties flat, because the crowd average already contains the specialists. The untested residue is their DEVIATION: when the top-decile rookie evaluators (by 2023-24 rookie skill) rank a rookie far off consensus, does the deviation's direction predict his outcome vs ECR? Walk-forward, shuffle null, min-N declared before the run. | **A** preregs · **D** grades | 🔴 OPEN | Prereg by 08-27 (after 4y's routes prereg); runs on the committed captures, no egress. Not a draft-week item — filed so the thread Cory was promised cannot evaporate. owner A, recheck 08-28. |
| 5a | 🟡 **A KICKER'S COMPOSITE SCORE RESPONDS TO ROSTER STATE, AND NO COMPONENT SHOULD BE DOING THAT.** Found by the v25 re-pin sweep: in composite_roster_blindness's harness, filling QB+TE moves exactly one row on SCORE membership of the top 70 — Jason Sanders (K) — while the need term is proven multiplied away (weight 0) and K has no measured ceiling cell. Suspect: VONA context propagation reading my-roster state somewhere it should read only room state. Reproduction: the suite's EMPTY vs FILLED states, scoreNames diff. Not draft-blocking (one boundary kicker, tiny delta) but a component is roster-aware that claims not to be — the exact class of invisible coupling this register exists for. | **A** | 🔴 OPEN | Diagnose which component moves Sanders's score between the two states; either name it as designed (and document where) or fix the leak. The suite bounds the movement at ≤1 row so growth is loud. owner A, recheck 08-21. |
| 5b | 🟢 **THE UNGRADED USE OF EXPERT CONSENSUS CORY IS RIGHT TO PUSH ON: EXPERTS vs THE MARKET.** Re-weighting experts against each other measured a tie (expert_skill_grading.json) — but the consensus's classic edge use was never that; it is the GAP between what experts think (rank_ecr, committed 2023-26) and what drafters pay (ADP). Where ECR says round 4 and the room pays round 6, the consensus hands you a value pick; where ADP runs ahead of ECR, it flags a market overpay. STUDY, walk-forward: per season 2023-25, does the preseason ECR-vs-ADP gap predict realized points vs draft-slot expectation? Shuffle null; rookie slice separate; report by gap direction. DATA: expert ranks committed all four years; historical ADP is ONE CI fetch (FFC year endpoint, archived_adp.py/external_adp_historical.py already written — commit the fetched stores so this never depends on egress again). If it grades positive, the board surfaces the LIVE 2026 gap (both inputs already on the board today) as a value flag — a direct draft-day edge Saturday. | **A** | 🔴 OPEN | Dispatch the historical ADP capture, prereg the gap study, grade before 08-21 if the capture lands clean — the live-gap display needs no grading to EXIST (both numbers are published facts) but needs it to be CAPTIONED as an edge. owner A, recheck 08-20. |
| 5c | 🔴 **TE IS THE BOARD'S LARGEST AHEAD-OF-MARKET TILT AND Q12'S SEPARATING TEST COULD NOT RULE — measured live 08-18 answering Cory's direct question:** top-150 mean displacement vs market rank: **TE +49.8** (LaPorta 37 vs market 67), QB +13.6 (partly legitimate — our 6pt pass TD vs half-PPR ADP), WR +4.8, **RB −23.1**. The board systematically fades RBs to promote TEs in a 10-team ONE-TE league where streaming works — the K/DEF onesie logic exists for exactly this and TE was deliberately excluded from it. Q12's n=2 grading could not separate error from edge. | **A** | ✅ RESOLVED 08-18 | **BY 08-21 (pre-draft):** counterfactual rebuild offline — TE replacement set from the market's own behavior (TE drafted ~10th at the position ≈ the streaming bar) vs our formula; if the tilt collapses, it is a replacement artifact → either extend the onesie demotion to TE-depth or caption the tilt on the war room like the single-source list; if it survives, it is a scoring-driven edge and gets SAID so Cory can lean into it deliberately. Either way Cory drafts Saturday KNOWING which. owner A, recheck 08-21. **RESOLVED SAME NIGHT (A) — the counterfactual ran and the verdict is DELIBERATE BET, not error:** (1) replacement-artifact theory DEAD — our TE replacement (136.4) equals the market's own streaming bar (its TE10, Kelce, 136.4) to the decimal, so the counterfactual rebuild is a no-op; (2) on pure VORP the TE tilt is only +9.7 — the +50 decomposes into the SOURCE (Sleeper prices these TEs above ADP-herd willingness; LaPorta 38 vs market 67 on value alone) and the CEILING WEIGHT (TE mid-bands carry the board's largest measured p90 multipliers, 1.81-1.87, so Cory's 0.45 ruling promotes mid-TEs hardest); both drivers are measured constructions working as specified. DRAFT-DAY FRAMING, told to Cory: the board deliberately pays for TE upside — believe the bet and falling TEs are the board paying off, or trim the exposure via the ceiling weight (C1's post-draft refit, where position-normalizing the ceiling term is now the named question). |
| 5d | 🔴 **THE OLD 2025 WEEKLY-POINTS STORE CARRIED PLAYOFF WEEKS AS SEASON DATA, AND EVERY '2025 SEASON TOTAL' THIS WEEK INCLUDED THEM.** Found closing row 33: the fetcher-written store had 22 weeks (19-22 = playoffs, 109/76/34/17 player-rows) while the components-derived builder writes 18 — two writers, two week conventions, the two-derivations-drift class again. Rebuilt 2025 from the component store (the builder first reproduced committed 2023 EXACTLY, 5371/5371): +53 restored tail players (row 33's gap), 108 players' regular-season sums corrected (≤10 pts), playoff weeks gone. BLAST RADIUS: everything that summed 2025 weeks this session — the calibration's 4s recovery path, the plausibility-rail bases, the age-curve fit, both expert gradings' realized(2025). | **A** | 🔴 OPEN | Chain, in order: (1) corrected store committed; (2) rail bases + age curve regenerated offline; (3) expert-grading verdicts re-checked against corrected actuals (both were NULLs — a null that flips would be loud); (4) calibration regeneration + board rebuild dispatched; (5) 2021-24 stores audited for the same phantom weeks. owner A, recheck 08-19. |
| 4u | 🟠 **A WAR-ROOM COLUMN IS ABOUT TO SHOW A NUMBER THAT WAS MEASURED NOT TO PREDICT ANYTHING.** `expert_spread_2026.json` (400 players) shipped at `d83ffc35` as war-room display data, routed to B. **The data is sound** — expert disagreement is genuinely per-player (365 distinct spreads, ρ(ECR,spread) 0.855, `rank_std` 0.7→85.4), and Fable's own skill grading at `424b2347` independently reproduced the relay's P2/P3 result (*skill persists but is too weak to exploit; top-10 loses on identical players*). **The problem is what a reader will take the number to MEAN.** Graded on 1,111 player-seasons 2023–2025 (prereg §10, ledger P4): within ECR band, P(top-12 finish at position) moves **−0.0134 for spread (28th percentile of a 400-draw shuffle), −0.0467 for most-bullish-expert (15th), −0.0297 for the bullish decile (19th) — all three negative, all three BELOW the shuffle median**; the realized-p90 cut agrees at the 72nd percentile of its own null. **Disagreement is real information about the ROOM and no measured information about the PLAYER'S outcome.** At 8s/pick a column captioned 'upside' beside a value number reads as a recommendation. | **B** renders · **relay** routed | 🟠 OPEN | **Caption it as disagreement, never as upside/ceiling/breakout. NOT A VETO** — the context is genuinely interesting and the data is good; the ask is only that the label match the measurement. **DEFAULT: ship captioned as disagreement only.** unblocked by nothing, owner B, recheck 08-21 (before the draft). |
| 4t | 🔴🔴🔴 **WHAT WE CALL `proj_ceiling` IS NOT A CEILING. Cory, 2026-08-18: *"this isn't a ceiling…"* — he is right, and it is the cleanest statement of this defect anyone has made.** A ceiling is a property of **one player's** outcome distribution. What we compute is **`(that player's projection) × (the 90th-percentile realized/projected ratio of a COHORT he is bucketed into)`** — a scaled cohort quantile. It is not his ceiling and it is not anyone's ceiling. **AND THE COHORTS ARE MOSTLY NON-PARTICIPANTS, WHICH CHANGES WHAT THE NUMBER MEANS ENTIRELY.** Measured on the 3-season fit: `RB|33+` **p10 0.021 · p50 0.345 · p90 1.434**; `QB|33+` p50 **0.165**; `WR|33+` p50 0.419. Compare `RB|1-3`: p10 0.692, p50 1.233 — a normal distribution around 1. **The deep cells are collapsed toward zero because most of that population never got a real role.** So the p90 of the whole cohort lands near a TYPICAL OUTCOME FOR A PLAYER WHO SIMPLY PLAYED, not near a breakout. **For late picks we have been labelling a middling participation outcome as a best case.** **THE CONSEQUENCE, on the live board:** every RB from projection rank 33 down carries the IDENTICAL ratio 1.794 — Hubbard, White, Vidal, Ford, one number for all of them — so rank-60 Kimani Vidal's stated best case is **95.1 points**. A real late-round RB breakout scores 250+. **The board is structurally incapable of saying a late pick could win the league**, which is the single thing Cory drafts late for. **IT IS ALSO THE ONE ROOT CAUSE UNDER EVERY OTHER CEILING FINDING THIS WEEK:** the inverted ratio slope (4p), the ceiling weight moving nothing (4m), the tiebreak comparing calibration constants (E15a), dispersion carrying no player-specific information (4j), and `RB|33+` swinging 0.36 when one season drops (4s). **All five are a group statistic, dominated by non-participants, applied to individuals.** | **C** fetches · **A** wires | 🔴 OPEN | ✅ **CORY RULED 2026-08-18: *"ceiling is a projected score that we will have to get from outside source."* THAT SUPERSEDES EVERY DERIVED APPROACH BELOW, INCLUDING MY OWN RECOMMENDATION.** A ceiling is a PROJECTION someone publishes, not a statistic we infer from realized/projected ratios. Every variant we have tried — Gaussian, constant multiple, measured p90, band-split, player-specific term — is a different way of INFERRING a quantity that a source simply states. **STOP DERIVING IT.** **⚠️ AND C WAS RIGHT WHILE I CORRECTED IT.** C reported *"there is no way to get projected ceiling"* and I told it `proj_ceiling` already exists on every board row. **`proj_ceiling` exists; a projected CEILING does not.** C was asking about a real published number and I answered about our derived cohort statistic — the exact conflation this row is about. The relay's correction was wrong in substance and it cost C time. **WHAT WE ACTUALLY CAPTURE FROM FANTASYPROS TODAY:** `_FP_STAT_MAP` maps stat CATEGORIES (`pass_yds`, `rush_td`, …) which we then score through our own table. **There is no high/low/ceiling/floor field captured anywhere** — register **22** already says we keep one scalar and discard the rest, and this is what that costs. **SUPERSEDED, kept for the record:** my recommendation was `P(gets a real role)` × `upside GIVEN a role`. The second is the number a drafter actually wants and we have never computed it. **`UPSIDE-PLAYER-SPECIFIC-PREREG.md` should be RE-POINTED at this** — it currently asks whether a player-specific TERM improves the existing quantity, which is the wrong question if the quantity itself is not a ceiling. **NOTHING SHIPS BEFORE 08-22** — this is a redefinition, not a patch, and a third variant of a broken quantity before the draft is worse than the honest one. **FOR DRAFT DAY: the ceiling column is the one part of the board to ignore outright; timing, survival and keepers are sound.** unblocked by nothing, owner A, recheck 08-24. **🔎 CORY ASKED 2026-08-18: *"We will probably have to get from fantasy pro api don't think we ever got"* — WE DID GET IT, AND IT DOES NOT PUBLISH ONE.** `draft/audit/proj_correctness_evidence_2026-08-16.json` holds a real capture of `api.fantasypros.com/v2/json/nfl/2026/projections?position=ALL&scoring=HALF&week=draft` from **08-16: 596 raw rows, full UNFILTERED key census, 40 stat keys.** Every one is a point estimate (`points`, `points_half`, `points_ppr`, `pass_yds`, `rush_tds`, …) and **there is no `ceiling`/`floor`/`high`/`low` key at any depth.** FP's projections API serves a consensus MEAN and nothing around it. **AND THE PROBE THAT SAID SO COULD NOT HAVE SAID ANYTHING ELSE:** `fp-projections-probe.yml` pipes the payload through `_FP_STAT_MAP`, a 9-key whitelist, before anything inspects it — a literal `ceiling: 441.8` would have been dropped on the way in and the job would still have printed its green *"FantasyPros DOES serve projections"* verdict. **Another check that cannot fail, reported as a check that passed.** **SO THE QUESTION IS NOT CLOSED, ONLY THAT ENDPOINT IS.** `draft/backtest/discovery_ceiling_sources.py` + `ceiling-source-probe.yml` (shipped 08-18, **50 tests, known-positive control that refuses to trust a null from a census which cannot find a planted ceiling**) probe the four endpoints nobody has tried: **ECR `type=draft`**, **ECR `&experts=show`** (the per-expert ranks BEHIND the consensus — a fully observed per-player distribution rather than a summary), **FP WEEKLY projections**, and **Sleeper's projections endpoint unfiltered.** ⚠️ **THE CLOSEST REAL THING WE ALREADY DISCARD:** FP's rankings family publishes **`rank_min` / `rank_max` / `rank_std` per player** — confirmed present in `discovery_fp_adp_field_census.json`, which filed them under `unclassified` and printed the verdict **"NULL"**. That is genuine outside-sourced PER-PLAYER dispersion, in RANK space, thrown away for six days. It is not a points ceiling until mapped through a rank→points curve, but it is the only per-player upside signal we have that is neither derived nor synthetic — unlike `adp_sd`, which is **475 of 693 `fallback-clamped` / `clamped-linear`, i.e. mostly not a published number at all.** **✅ PROBE RUN 2026-08-18 (`32087333128`) — THE NULL IS NOW MEASURED ACROSS ALL SIX ENDPOINTS, AND ONE OF THEM CARRIES SOMETHING BETTER THAN A STATED CEILING.** `fp_projections_season` (238KB/58 keys), `fp_projections_week1` (262KB/50), `sleeper_projections` (2.9MB/124): **NULL — a mean and nothing around it.** `fp_ecr_draft` / `fp_adp`: `rank_min`/`rank_max`/`rank_std` only. **Zero endpoints unreachable, so every one of these is a real null and not a 403.** **🎯 BUT `consensus-rankings?type=draft&experts=show` RETURNS EACH INDIVIDUAL EXPERT'S RANK PER PLAYER — 91 of them — AND WE HAVE BEEN DISCARDING IT.** Not FP's three-number summary; the observed ranks themselves, one per human. **CAPTURED AND COMMITTED** (`fp_expert_ranks_2026.json`, run `32087530275`): **788 players, 100% carrying expert ranks, 0 missing, 365 DISTINCT rank spreads** — against 4j's *0 of 535 differ*. **Coverage is 100% with ≥20 experts through ECR 200, the entire draftable range of a 192-pick draft;** it thins only past pick 200. **ALL THREE PREREGISTERED KILL SCREENS CLEARED** (`EXPERT-SPREAD-CEILING-PREREG.md` §6): Spearman(ECR, spread) **0.855** — correlated but not deterministic; per-player `rank_std` **0.7→85.4, median 25.8** — 91 experts who genuinely disagree, not one counted ninety times; and **the 4j test, which is the headline: at ECR 80-99 Kyle Pitts' spread is 55 and Jonathon Brooks' is 176; at 120-139 Tyrone Tracy 69 and Malik Willis 411. Same draft neighbourhood, three to six times the disagreement — and the board hands each pair the IDENTICAL ceiling multiplier.** That is Cory's *"this goes against every fantasy footbal theory ever"*, measured. ⚠️ **GRADING HAS NOT RUN** — it needs 2023/2024/2025 captures and the prereg fixes the arms (BASE/ECR-MIN/ECR-Q10/ECR-SPREAD) and the **SHUFFLE null** before any of them was scored. **THE SHIP DATE DOES NOT MOVE: nothing reaches the board before 08-22, and the ceiling column stays the one part of the board to ignore on draft day.** | **🧷 A, 08-18, RECONCILING THE TWO CORY RULINGS ON THIS FIELD:** the prereg-§2 per-player volatility term SHIPPED today (`player_volatility_in_tails`) executing his EARLIER explicit order ('fix!!!! floors and ceilings need to be corrected like I have agreed to') — it makes the derived tails per-player (floors included, which the outside-source path does not yet cover) and directly answers his 'ceiling should be different depending on the player'. It does NOT claim to be the redefinition this row is about: Cory's newer 'get it from an outside source' stands, the expert-spread capture is the successor, and its preregistered grading (BASE/ECR-MIN/ECR-Q10/ECR-SPREAD + shuffle null) is the gate. Until that grades, the board's ceiling column is the least-wrong derived number we can print: measured cell level × the player's own realized volatility, weight still 0. The deep-cohort criticism (a p90 dominated by non-participants) is TRUE of the cell level and untouched by the player term — 4q's role-conditioning and this row's outside-source path are the same question and should merge post-draft. **🏁 A, 08-18 LATE — THE OUTSIDE-SOURCE CEILING IS NOW FULLY GRADED AND THE ANSWER IS NO, TWICE:** C ran the preregistered arms on all three seasons (nothing excluded): ECR-MIN / ECR-Q10 / ECR-SPREAD each beat BASE on **0 of 3 seasons** (§4 condition 1 fails; conditions 2 and 4 pass — the signal is real and per-player, it just does not predict realized dispersion better than the cohort baseline), consistent with 4u's independent null on top-12 hits. Rookie slice honestly underpowered (7/2/0 graded rookies), exactly as §8 predicted it might be. **PER THE PREREG'S OWN RULE: NO SHIP — the ceiling column stays v25's measured-cell × player-volatility construction, the expert-split badge stays captioned as ROOM disagreement, and this line is closed with a re-test trigger (a second capture generation, 2027).** Two graded nulls beat an ungraded hope; the model keeps the honest number.
| 4b | 🔴 **Cory rejected the war-room design outright** — *"too busy and wordy… looks like an amateur pile of shit"* — and says it does not match reference screenshots he was sent. Draft is 08-22. The surface Cory actually drafts on is the one surface no measurement covers. | **B** | 🔴 OPEN | Get the screenshots from Cory FIRST; do not redesign from taste. Standard is *lots of data, cleanly presented* — fix by hierarchy and density, NOT by deleting data. Coordinate with A: do not describe `proj_mean` as a blend (rows 21/21b). **recheck 08-19** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** |
| 4g | 🟠 **A'S INBOX — FIRST PASS DONE, THE REAL CUT STILL OWED.** A is the gatekeeper and is meant to answer in one word; it had **63 open items**, and boot cost measures **~135k tokens** across `ROUTES.md` (58,427 words), `STATUS.md` (27,834), `SESSION-A.md` (14,968). **This is not abstract: a two-file CI fix sat unmerged for an hour today while `main` went red four more times, five days before the draft, and I ended up merging it myself rather than waiting.** **⚠️ AND I HAVE BEEN MAKING IT WORSE WHILE THE ROW SAT OPEN** — several of the longest items in that file are mine, filed today. **DONE 08-17:** a `🎯 DECISION QUEUE` box now sits at the top of A's section listing the **seven** things that actually need A (D1–D7), with everything else marked context; and three items were closed **verified against live state, not against the date on the line** — the CI fix (green on run `32064471617`), the `matchup_placed_bet`/`trashtalk` P0 (both suites PASS today), and the repo_parity gate ruling (shipped in `ci.yml`). **SECOND PASS 08-17:** the three `worktree-agent-*` MERGE-READY handoffs are CLOSED, each verified by ancestry (`git merge-base --is-ancestor <tip> origin/main`) and, for the one whose branch was gone and whose `git log --merges` showed only merges INTO it — which reads exactly like work that never landed — **verified a second way by checking its named deliverables on `origin/main` before closing.** They were all already in `main`. **A: 63 → 57 open.** ⚠️ **A MIS-TARGETED CLOSURE WAS CAUGHT AND REVERTED BEFORE PUSH:** the first script matched a branch name where it appeared in a QUOTED SUMMARY and walked back to the nearest preceding bullet, closing *the blend-question item* instead. Same family as every join defect this week — a lookup that returns confidently and returns the wrong row. Redone line-exact and diffed. **THIRD PASS 08-17:** five more closed as INFORMATIONAL — each self-declared as needing no decision **and** checked for a buried ask first (`ASK:` / `THE ONE DECISION` / `MERGE-READY` / `DECISIONS-NEEDED` / *your call*). **Two siblings that read exactly the same way DID carry a buried ask and were deliberately left open**, which is why this is a check and not a headline sweep. **A: 63 → 52 open** (−11 tonight). Nothing deleted; closed items move to `ROUTES-ARCHIVE.md`. **STILL OWED:** the remaining ~30 need reading, not pattern-matching — the two saved siblings prove a regex cannot finish this job. | relay | 🟠 IN HAND | Walk A's remaining open items and sort each into DECISION (stays), HANDOFF (moves to the lane that can act) or DONE (closed against live state, never against its date). **Archive closed material out of `ROUTES.md` into `ROUTES-ARCHIVE.md` — Cory's rule is write MORE down, not less, so the fix is where it lives, not whether it exists.** recheck 08-18. |
| 4 | **`main` CI red** on `matchup_placed_bet` (3) and `trashtalk` (3), since 08-16 23:01. A red main means the gate has confirmed nothing for anybody. | **B** | OPEN | Verify whether B's `/matchup` fix cleared it; if not, fix. **recheck 08-19** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** **✅ VERIFIED CLEARED — relay, 2026-08-18, doing precisely what this row's next action asks.** Both suites PASS on `origin/main` today: `matchup_placed_bet.test.js` **6/6**, `trashtalk.test.js` **27/27**, run against a tree byte-identical to `origin/main` for those files and their subjects. **AND THE ROW THAT NAMED THE FIX IS ALSO NOT WHAT IT SAYS:** B's `11e253bb` (subject *"P0 CI red (matchup_placed_bet/trashtalk)"*) does not repair them — it ADDS 22 lines to each. Whatever cleared this main red, it was not that commit, and I could not identify what did. **Left OPEN for B rather than closed by me**, because the owner should confirm; but the premise *"main CI red since 08-16"* is FALSE as of today and should not be costing anyone attention four days out. |

## 🔬 E — RED-TEAM FINDINGS (merged from the red-team lane 2026-08-18)

**Union-merged, not chosen.** Main carried 27 rows this branch did not and the
branch carried these 15 that main did not. Taking either side would have
silently dropped the other's findings — the merge-drop defect this repo has
already been bitten by, which is why both sets are here in full.

| # | what | owner | status | next action |
|---|---|---|---|---|
| E1 | 🔴 **Nine top-50 players read the wrong dispersion cell** — band assignment uses a rank that disagrees with the band the calibration was fitted on. Worth $47.6 of spread between St. Brown and Jefferson. | **A** | 🔴 **AWAITING A'S APPROVAL — fix prepared, deliberately NOT applied** | **THE FIX IS WRITTEN AND SENT FOR APPROVAL — `E1_proposed_fix_for_approval_2026-08-17.md`.** Two edits: give `blend()` an optional `rank_pool_ids` (inert until passed), and hoist the keeper map above `build.py:721` so the cell is read against the ordering the board actually publishes. **NOT APPLIED because (b) moves `_assess_keeper_slate`, a NETWORK read, earlier in the build pipeline five days before the draft — a gatekeeper call, not a red-team one.** No one-liner exists: `kept_ids` is not known until `build.py:1646`, and using `cfg`'s raw keepers instead fails because the slate is partial (4 of 10 designated), so that set and the published-withheld set are different populations. **Blast radius: dispersion fields only — order does not move.** **Re-check after keeper lock 08-20 — and the magnitude is now computed, not guessed: 46 players change calibration cell when the other teams' keepers leave the pool, 37 of them inside the top 150**, with James Cook, CeeDee Lamb, Ashton Jeanty and A.J. Brown all moving into the `1-3` band. Since sweep 7 established `proj_ceiling` is `proj_mean × a per-cell constant`, **cell membership IS the dispersion figure** — so every one of those 46 gets a materially different floor and ceiling on 08-20. **E1 does not fix itself and does not worsen; it changes IDENTITY** — the mismatch is governed by Cory's three keepers, who stay in `kept_players`, so it stays ~3 slots wide but lands on a different nine. **Fix the mechanism, not the named rows — now DEMONSTRATED, not argued: re-running E1's own comparison on the post-lock pool gives 11 misreads against today's 9, and ONLY Chase Brown is on both lists.** Eight of today's nine drop out; ten new players appear, five inside the top 50 (Pickens 17, Waddle 28, Kyren Williams 32, Etienne 37, McLaurin 47). So the defect gets slightly wider, not identical — **my own earlier "stays ~3 slots wide" was directionally right and numerically wrong, corrected in the audit.** Directional only: uses `predicted_keepers`, which is MOCK-ONLY by its own header, and 4 of 10 teams have designated against 14 predicted. `keeper_lock_shifts_the_cells_2026-08-17.md` `band_edge_misread_2026-08-17.md` |
| E2 | 🔴 **`proj_ceiling` is STILL rank-identical to `proj_mean` inside every cell** — Spearman exactly 1.000000, 16/16; within-cell cv 6.3e-04 worst. The §1 defect signature, unchanged. `constant_multiple_sweep` flags it on today's board with its known-positive control firing. **Cory 08-17: "FIX!!!"** | **A** | ✅ RESOLVED 08-18 | The construction is already written: `VOLATILITY-WIRING-PREREG.md` §2, on `main`, with `weekly_volatility.json` committed. Prereg says run after 08-22; **Cory has now asked for it sooner, so the date is his to move, not the prereg's.** Blast radius is bounded: `MEASURED_WEIGHTS.ceiling = 0`, so it moves the **bench branch** (`engine.js:1166`, ranks on `proj_ceiling − proj_mean`) and **`playerDollars`** (row 8a), NOT the composite board ranking. `ceiling_is_still_a_cell_constant_2026-08-17.md` **BLAST RADIUS NOW MEASURED — `ceiling_fix_blast_radius_2026-08-17.md`:** the change introduces **~1.4x-2.0x within-cell spread** where there is now exactly 1.0x. **One thing to re-derive FIRST:** the prereg's coverage counts (131/157, 26 without, 8 rookies) **do not reproduce** on the published board — closest is 94 of 101 skill-only/2025-only — though its four named players (Evans, G.Wilson, Nabers, Daniels, +Reed unnamed) do miss 2025 exactly. **All four have a cv in some OTHER season, so `2025-only` vs `any-season` must be decided before wiring**: under any-season the absent rule stops protecting the injury-return group it was written for. **RESOLVED 08-18 (A):** wired exactly as prereg §2 (`player_volatility_in_tails`, ON in league_config under Cory's explicit override): ceiling = mean × p90[cell] × f, floor = mean × p10[cell] / f, f = own realized 2025 weekly cv over cell median, mean-normalized — the §2 gate is a pinned test, absent players keep the cell constant with their original `_source` stamp, carriers stamp `-x-player-cv`. **Coverage fork ruled 2025-ONLY** (persistence is measured on one-year transitions; any-season would hand the injury-return group a stale reading — E's own warning). Within-cell Spearman(ceiling, mean) is no longer 1.0 by construction; the constant_multiple_sweep flag should clear on the next board. The ceiling WEIGHT stays 0 — that is C1, post-draft. |
| E3 | **The composite's only per-player term saturates at its cap across the top 50** — 30 players at exactly `opportunity_adj 0.15`, 22 of them top-50, underlying `opportunity_z` spanning 2.00–3.77 flattened to one number. `proj_mean = proj_sleeper × 1.1500` exactly for 21 of the top 24. | **A** | OPEN | Rule on whether the cap should bind where it binds. Invisible to `constant_multiple_sweep` (cells of 2–8 rows against `MIN_CELL = 12`). `opportunity_cap_saturation_2026-08-17.md` |
| E4 | **The opportunity adjustment is not mean-preserving and excludes QB/K/DEF entirely** — pool level shift RB +7.60%, WR +7.04%, TE +6.81%, QB **+0.00%**; one distinct value (0.0) across all 88 QBs. Since the board ranks across positions on vorp, it is a positional thumb on the scale: Josh Allen ovr 16 → ~10 under symmetric treatment. | **A** | OPEN | Decide whether an adjustment that is also a +7% level shift may apply asymmetrically across positions that are then ranked against each other. `VOLATILITY-WIRING-PREREG` already fixes mean-preservation for the next feature. |
| E5 | **That same term is a veteran bonus** — median `opportunity_adj` +0.0783 / +0.1166 / +0.1454 / +0.1377 by experience band; 0 of 13 young top-150 players reach the cap against 26 of 66 established. Two top-150 rookies with `wopr: null` get exactly 0.0000. **Supplies the mechanism for the rookie gap the 08-16 drafter study measured at +25.1 = 38% of Cory's pooled gap.** | **A** | OPEN | Consider together with the already-queued rookie-prior decision in `DECISIONS-NEEDED.md`, not as a separate item. `opportunity_is_a_veteran_bonus_2026-08-17.md` |
| E6 | ✅ **FIXED 2026-08-17 (Cory: "act on, fix and continue").** ~~The board's "single-source" caveat marked the wrong 255 players.~~ `app.js:642` gates it on `proj_fantasypros` being ABSENT, so the 427 rows where FP exists and is ignored render with NO caveat — and its absence asserts a second opinion that does not exist. 96 of the top 150 are unmarked; all 682 are Sleeper-only. | **B** (fixed by E on Cory's instruction — **B please review**) | ✅ **FIXED — verify** | **DONE:** `app.js:662` now marks EVERY row and splits the per-player fact that actually varies — **¹ = a second source EXISTS and is UNUSED (127 rendered), ² = no second source exists (61 rendered)**. **Verified three ways:** the live screen reports **0 of 188 rows unmarked, 127 ¹ with 0 wrong, 61 ² with 0 wrong** (was 127 unmarked, all single-source); `proj_source_authority.test.js` gains two pins with a **proven fail arm** (restoring the old function fails them 2/2); and a behavioural rehearsal `rehearsal-board-truth.js` is wired into `rehearsals.yml`. JS suite **313/313 files pass**; Python shows the same 9 pre-existing failures as a clean tree at HEAD. **B: this is a LABEL change — no number moved.** The original ask was: What the mark could usefully say is the thing that IS per-player: whether a second source exists and was not used. Register 21b with the mechanism located. `proj_source_mark_inverted_2026-08-17.md` **NOW OBSERVED ON THE LIVE SCREEN, not just read in the code** (`warroom_truth_check_2026-08-17.md`): driving the real war room, **127 of 127 unmarked rows are provably Sleeper-only** (`proj_baseline == proj_sleeper`) and **65 marked rows all genuinely lack FP** — the mark is internally consistent with its own condition and the condition is the wrong one. **Everything else on that screen is truthful**: 188 players checked, `overall_rank` and `proj_mean` both **0 discrepancies** against the artifact. A ~40-line DOM-vs-artifact assertion would pin this against regression; recorded in the audit, not shipped, because adopting it is B's or A's call. |
| E7 | **`STREAMABLE_LATE` is computed, tested, and read by nothing.** `engine.js:2167` defines it; `engine.test.js:475` pins it; `applyFormatDefaults` copies only `BENCH_DISCOUNT`. Observable consequence at **TE** (+12.3 top-150, +16.7 in the 70–150 band, six board-highest players in that band all TEs) and **absent at QB**. | **A** | OPEN | Wire it or delete it — either is defensible. What must not persist is a league-shape judgement that runs every build, is pinned by a test, and reaches no decision. If deleted, delete the test with it. `streamable_late_is_never_read_2026-08-17.md` |
| E8 | **Live PUP/IR status reaches no availability number.** Kittle (PUP) carries his position's healthy `games_expected`; `bench_mv.js` turns that into a constant `injuryWeeks` — every RB simulated as missing 1.8 weeks, healthy or not. Kittle is ranked ~15 picks ahead of market while on PUP. | **A** | OPEN | Low urgency — `risk` and `bye` are both weighted 0.0, so it reaches no composite recommendation. `bench_mv.js`'s ledger already states the "no history" half; extend it to "no current status". `sweep3_flags_that_died_2026-08-17.md` **⭐ MEASURED WIDER, 2026-08-18 (relay), and the row is bigger than the Kittle case:** `games_expected` takes **SIX distinct values across all 693 players** — 15.0 (240 players), 14.2 (156), 14.8 (131), 15.5 (90), 16.5 (44), 17.0 (32). It is a **position-band constant carrying essentially no player-specific information**, which is the identical shape to the dispersion defect that was 08-17's headline (*'every dispersion field was proj_mean x a per-band constant — zero player-specific information'*). PUP status not reaching it is one symptom; **nothing player-specific reaches it at all.** And it is not inert the way `risk` and `bye` are: `projections.py:599` computes **`weekly_sd = season_sd / sqrt(games)`**, so a banded `games` propagates into every per-week dispersion number the in-season tools read. **A CANDIDATE INPUT NOW EXISTS:** Kalshi lists free, unauthenticated per-player availability markets — *'Will &lt;player&gt; play in a game in every week (excluding byes) of the 2026 season'* — which is a market-implied durability number per player (`free_market_census.json`). Gate it like any arm before wiring: does it beat the band constant out of sample. **recheck 09-12** — date assigned by the relay 08-18; the owner may change it, and changing it with a reason clears the check. |
| E9 | **Source-ruling evidence for A2 / row 21** — the choice changes the ORDER, not just the level: top-150 FP/Sleeper cv 0.072, within-position Spearman 0.890–0.935, **25 of 39 top-150 WRs move 3+ spots**. On the QB board FP and the market agree against Sleeper on Daniels and Hurts. | **A** | OPEN | Input to the source ruling A2, not a recommendation — row 20 says the measurement that would settle it does not exist. |
| E10 | **Register row 2 has a named candidate without a fresh board** — `adjusted_adp = c × pool_rank` in **10/10 cells** (cv 0.012430), in no dispersion relationship, so a genuinely separate instance. | **A** | OPEN | Check `adjusted_adp` first when the fresh 693-row board is built. Narrows row 2; does not close it. |
| E15 | ✅ **FIXED 2026-08-17 — the QB over-recommendation Cory hit live had TWO causes and both are repaired.** (1) **The ceiling tiebreak decided on a calibration constant**: at pick 88 with Burrow rostered the engine promoted Bo Nix (proj 335.72) over Brock Purdy (proj 350.2) and labelled it *"on upside"* — Nix's ceiling is bigger only because `QB|9-16` carries a 1.426 multiplier against `QB|4-8`'s 1.316. `moreUpsideThanTheCellExplains()` now refuses a swap justified only by the two players' cells; **score inversions across the list went 16 → 0**. (2) **The seat plan asserted a QB starter seat at pick 73 and was roster-blind** — solved once pre-draft from keepers alone, never re-solved, with a `fallback_rule` steering to *"the best remaining player ELIGIBLE FOR QB"*. `renderSeatPlan` now asks the engine's own `mandatoryGaps()` and marks the seat **SEAT ALREADY FILLED** when the slot is held. | **A** (engine) / **B** (surface) — fixed by E on Cory's instruction, **please review** | ✅ **FIXED — verify** | Both guards self-release: the tiebreak starts firing again the day `proj_ceiling` carries per-player information (VOLATILITY-WIRING-PREREG §2), and the seat notice tracks the live roster. **Verified:** JS **315/315 files**, Python 3312 passed (same 9 pre-existing failures as a clean tree), war-room rehearsal **19/19**, board-truth **6/6**, plus two new test files with controls. Two existing controls in `engine_ablation` and `rec_rows` legitimately inverted and were **re-aimed, not deleted** — both keep their ability to fail. `ceiling_is_still_a_cell_constant_2026-08-17.md` |
| E13 | **`rookie_affinity` is 0.0 for all ten managers and could not have been anything else.** `manager_profiles.league_average.rookie_rate = 0.0` across ~450 picks in 3 drafts. Cause: `managers.py:122` reads `years_exp` from **today's** Sleeper payload while `position` and `team` in the same dict prefer contemporaneous `meta` — so a 2023 rookie has `years_exp` 3 today and is never counted, and only a 2026 rookie could qualify. **The `"chases rookies"` clause (`rate > league_rate*1.5 and rate > 0.08`, both 0.0) is unsatisfiable and fires 0 of 10 times.** The module header names rookie affinity as one of three metrics that *carry* the profile. **Contradicts the 08-16 drafter study, where the rookie prior cleared its bar at +25.1.** | **A** | OPEN | Derive rookie status from the draft season minus rookie season (`league_history.json` carries the season on every row), or emit ABSENT with coverage reported — the treatment `position` already gets via `"?"` — rather than 0.0, which reads as a measured *"nobody drafts rookies"*. **Not draft-critical**: feeds opponent summaries, not the board ranking; the softmax reaching survival is driven by `bpa_rate`. Post-08-22 unless A disagrees. `rookie_affinity_cannot_vary_2026-08-17.md` **SCOPE LIMIT CLOSED:** the other profile metrics are NOT degenerate — `homer_index.rate` 7 distinct, `homer_index.team` 9 distinct, `bpa_rate` all 10 distinct, positional consistency on 10/10, and summaries discriminate in plain language. **Three legs carry weight and one is sawn off**, so the fix is to one metric, not the module. |
| E14 | **An unverified assumption sits under three manager metrics.** `managers.py` reads `meta.get("position") or info.get("position")` — contemporaneous first, today as fallback — and **nothing confirms `meta` is ever populated.** The only inspectable copy of historical picks, `league_history.json`, has `metadata` **empty on 480 of 480**. If that also holds on the live path, `homer_index` measures *which teams a manager's old picks play for NOW*, and the `"loves <TEAM>"` line in every opponent summary is wrong. **NOT filed as a defect:** the production path reads live Sleeper (`build.py:1493` → `si.all_drafts` → `fetch_draft_picks`), which returns the raw payload and probably does carry metadata — **and Sleeper is HTTP 000 from this sandbox, measured 2026-08-17**, so this is unverified rather than refuted. | **A** | OPEN | **One line for anyone with egress:** `picks = si.fetch_draft_picks("<completed draft_id>"); count how many carry `metadata.team`.` `0 of N` means homer_index is wrong; `N of N` closes the row and leaves only E13. `rookie_affinity_cannot_vary_2026-08-17.md`, appendix. |
| E12 | **The draft-day runbook's one irreversible step rests on a false premise.** `DRAFT-WEEK-BRIEF.md` §4 says the freeze *"is from 08-14 and is missing fourteen declared fields"*; it was re-taken in `60f3487` and its payload sha is **byte-identical** to a freeze generated from today's board (`98f580261a2ea898…`), 0 of 44 declared fields missing, `test_freeze_not_stale.py` **3/3 green** including its `repo_parity` node. The brief's *"two deliberate red flags"* is one. | **relay** | OPEN | **The ACTION stays — re-take after the final board build, because the board rebuilds nightly.** Replace the stale justification with the durable one, and correct the closing paragraph. A `rm` justified by a premise the reader can falsify is a step someone skips. `freeze_is_not_stale_2026-08-17.md` |
| E11 | ✅ **Register row 3 does NOT reproduce on the published board** — 0 adjacent pairs where a lower-ranked player carries higher vorp; `overall_rank` is a clean 1..682. | **A** | **ANSWERED for the published board** | Row 3 is specific to the fresh 693-row board and is **not in front of Cory this week**. Does not close the row; takes it off the draft-week critical path. |

## 🟠 CORRECTNESS — known wrong, deliberately held

| # | what | owner | status | next action |
|---|---|---|---|---|
| 5 | **`MEASURED_WEIGHTS.ceiling = 0` is contradicted by measurement — AND THE BLAST RADIUS IS NOW MEASURED, small.** Three preregistered runs, two independent seed sets, every value 0.15–0.65 beats zero, 3/3 separable. **Through `recommend()` at 0 vs 0.45 (relay, 08-17): 0 of top-60 move at pick 30 and at pick 80; 8 move (max 3 spots) at pick 100; 35–37 move (max 5) at picks 120–140; the TOP RECOMMENDATION NEVER CHANGES at any pick.** The harness has its own control — it returns movement late, so the early zeros are a finding, not a dead call. Cause: the term is position-normalised (`engine.js:1217`) and gated by `CEILING_LATE_FROM = 0.6`. | **Cory** | WAITING | Cory rules. **The honest framing has changed: this is a late-round bench-ordering change, not a board-wide one.** See also row 25 — `autoWeights` already ships 0.45–0.8, so which system is live decides what this ruling even means. **recheck 08-24** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** |
| 6 | **ADP-sd ratchet fired** — shipped rule is 1.39× FFC's published dispersion in the 50–100 band. Our constant did not drift; the market tightened. Blast radius: 1 player. | **Cory** | WAITING | Recommendation: leave it, revisit post-season. **recheck 08-24** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** |
| 7 | **`risk` weight is UNMEASURED** and the term is PARTIAL on backtest boards (age only, 6 of production's 11–13 distinct values). | **A** | OPEN | No date. Needs a prereg before any measurement, or an explicit "stays zero" ruling. **recheck 08-24** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** |
| 8a | **The dollar model was not re-derived after the ceiling changed.** `playerDollars`' `boom = ceiling − mean` moved: boom's share of a player's total fell **47.2% → 34.9%** (median), every total moved **−18.8%** median, **464 of 603 players moved >10%**. Coefficients 0.22/0.08/0.05 were chosen at the old scale. | **A** | OPEN | Re-derive, post-08-22. Until then the figures are RELATIVE ONLY — which the code says and every surface must keep saying. `ceiling_change_downstream_2026-08-17.md` **recheck 08-24** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** |
| 8b | **The board carries TWO ceiling constructions** — 530 `measured-2023-25-p90`, 73 `gaussian_z` — and `playerDollars` reads the field, not the `proj_ceiling_source` stamp, so it treats them identically. | **A** | OPEN | Decide whether the 73 should be excluded from dollar figures or the difference surfaced. **recheck 08-24** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** **✅ RE-MEASURED 2026-08-18 ON THE 05:33 BOARD, AND THE SCOPE IS WIDER THAN THIS ROW SAYS — extending 8b/28 rather than filing a new row.** **(1) The counts moved.** `proj_ceiling_source`: **268 `measured-2023-25-p90-x-player-cv` · 267 `measured-2023-25-p90` · 161 `gaussian_z`** (8b recorded 530/73). `proj_floor_source`: identical 268/267/161. `proj_sd_source`: **535 `measured-2023-25-error` · 161 `position_variance`** (28 recorded 530/152). **161 players — 23% of the board — carry dispersion with no per-player measurement behind them.** **(2) IT IS NOT ONE CONSUMER IGNORING THE STAMP. IT IS ZERO CONSUMERS READING IT.** 8b names `playerDollars`; I grepped every surface — `public/js/`, `views/`, `src/` — and **`proj_ceiling_source`, `proj_floor_source`, `proj_sd_source` and `adp_sd_source` are read by NOTHING.** (`adp_source` is read by 3 files, `bye_source` and `variance_why` by 1 each, so the grep is not blind — those are the control.) **(3) WHY THAT MATTERS MORE THAN THE 161.** `draft/projections.py` states the purpose of these stamps in its own comment: *"Absent stays absent: an unmeasured band keeps the Gaussian rather than silently receiving a filled-in p90, and `proj_ceiling_source` is how a consumer tells the two apart — the same rule `proj_sd_source` follows, and **the rule whose absence let `0.25 * proj_mean` reach the board once**."* **The mechanism built to stop the worst bug this project has had from recurring invisibly has no reader.** It is a guard that is written but not wired. **Practical exposure for 08-22 is small and I will not inflate it** — in Cory's pick window (ADP 27-160) only 9 of 132 are `gaussian_z` and all nine are K/DEF (row 4v). **The cost is standing, not immediate: the next time a band goes unmeasured, nothing on any screen will say so.** |
| 8c | **`champodds`, the bench ceiling floor, and every `proj_floor` consumer** were calibrated against the pre-08-17 distribution and have not been re-checked. The floor has had LESS attention than the ceiling, not more. | **A** | OPEN | Same rule: anything reading proj_ceiling/floor/sd/weekly_sd is suspect until re-measured. Measuring is not shipping. **recheck 08-24** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** |
| 8 | **own_v6 — the live model — has 22.7% of its forecasts excluded from its own accuracy score** (115 of 506; walk_forward 211 of 737). MAE is optimistic by an unmeasured amount. | **A** | WAITING (after 08-22) | Prereg written: `SURVIVORSHIP-BOUND-PREREG.md`. Headline is ranking stability, not a corrected MAE. **recheck 08-24** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** |
| 37 | **FB-VORP defect in backtest bundles (found by the live-edge engine replay).** `cli.py` bundles keep Sleeper's raw `FB` position; `vorp.replacement_levels` returns 0.0 for a position with no starter slots, so the top fullback's VORP equals his gross projection — the engine drafted Juszczyk r2-2024 and r8-2025. The LIVE board carries no FB, so this corrupts only bundle-driven backtests (`results.json`, `strategy-results.json`, `engine_seat_replay.json` all carry it). | **A** | OPEN | Map FB→RB in `build_bundle.build` before `apply_vorp`, re-dispatch `backtest.yml`, re-grade. Post-08-22; mechanism routed in ROUTES, no constant touched. **recheck 08-24.** |
| 38 | **The bundle projection layer, not the engine machinery, dominates the engine replay's losses.** On era bundles B2 (pure VORP over the same board) is the WORST of the four policies (value-cut −197.9 vs B0 −14.5, B3 −109.7); the engine's bleed is rounds 4-12; the status-filter diagnostic arm moves the optimal median only −281.0→−190.4 and flips no seat. `lab_projections.walk_forward` prices stale/backup QBs (Wentz-2024 r3) and carries no role context — a weaker construction than the graded own_v6_nomarket the proxy replay builds walk-forward for the same seasons. | **A** | OPEN | Prereg (post-08-22): drive bundles with own_v6_nomarket boards so engine and proxy are measured over the SAME board and the delta isolates the machinery. `draft/data/engine_seat_replay.json` honesty list names the limit. **recheck 08-24.** |
| 39 | **The regenerated projection-error calibration measures RB p90_ratio FLAT across rank bands** (4-8: 1.812 / 9-16: 1.785 / 17-32: 1.768 / 33+: 1.794, n up to 151) where the superseded artifact carried band separation, and RB|33+ (1.794) now EXCEEDS the Gaussian construction (1.762), inverting the measured-ceiling audit's deep-band claim for that one cell. Rule-3d screen passed (QB monotone 1.59→1.22, WR/TE structured, n varies per cell — inputs vary, not the constant defect), so this reads as a real property of RB outcome variance — but the regeneration used C's new `projection_error.regenerate()` (rest-of-season grading via cli.py machinery) and nobody has diffed its grading basis against the artifact it replaced. | **C** | RESOLVED | Cross-check regeneration method vs the superseded artifact's derivation (population join, grading window, season weighting) and either corroborate RB flatness or name the method delta. **Before 08-19** — this feeds every live RB ceiling at the next board build. **recheck 08-19.** **RESOLVED 2026-08-17 23:51 — the cross-check found the method delta: the regeneration was fitted on punters, DBs and linebackers polluting every cell (commit 88b58a5d revert, 79a51073 proper fix + population guard). RB flatness was the pollution, not football; the corrected artifact restores band structure (RB|33+ 1.434) and measures the 1-3 bands honestly. The Rule-3d screen I ran (positions vary, n varies) was necessary but not sufficient — cross-position variation survived a within-position population defect; the guard commit is the durable fix.** |
| 40 | **Drill-down VONA/composite render only for the engine's shortlist-depth slice** — a rail/board click outside it drew a bare em-dash indistinguishable from zero/broken (B's rehearsal find). Captioned honestly 2026-08-18 ('not scored this pick — outside the engine's shortlist depth'). The real fix is on-demand scoring for any clicked player. | **A** | OPEN | Build on-demand per-click scoring post-08-22 (re-runs the engine per click mid-draft — deliberately not rushed at draft-4). **recheck 08-24.** |

## 🟡 DATA — stores and coverage

| # | what | owner | status | next action |
|---|---|---|---|---|
| 41 | 🟡 **FROM WEEK 1, "no change" ON A WEEKLY CAPTURE IS A SILENT HOLE IN THE SERIES, AND FOUR WORKFLOWS TREAT IT AS BENIGN ALL YEAR.** `weekly-snap-counts.yml`, `weekly-proj-snapshot.yml`, `weekly-routes.yml` and `external-adp-capture.yml` all end with `git diff --cached --quiet && echo "no change" && exit 0`. **The reasoning is right today and stops being right on 09-10.** `weekly-snap-counts.yml` states the stakes itself: *"the value of this feed is `share_volatility` — how much a player's snap share MOVES week to week. That is a quantity you can only build out of a week-by-week series, and a series with holes in it measures a different thing than one without."* **Pre-season, nothing staged means the season is unpublished — correct, and going red weekly until September would train us to ignore the job. In-season, nothing staged means one of three things and the workflow cannot tell them apart: the week was already captured (fine), the source has not published yet (retry), or the fetch/join silently returned nothing (a HOLE, and it is the one that matters).** *"I could not look"* rendered as *"fine"* — `commitments_check.js` calls that BLIND IS NOT QUIET and refuses it; these do not. **Found from the pattern, not from a breakage** (ledger P69's question applied to the scheduled jobs): 24 workflows carry a `schedule:`, **10 have no test that references them at all**, and these four are the subset whose silence would cost a series rather than a run. Nothing is broken today — week 1 is 09-10. | **A** · **D** | OPEN | **A owns the workflows; D owns the in-season series that would carry the hole.** **Make "no change" season-aware before 09-10.** Minimum viable: once the current NFL week is ≥ 1, an empty stage is a WARNING with the week number named, and an empty stage on the SAME week two runs running is a failure. The retry/hole distinction needs the week number, which every one of these fetchers already knows. **Recheck 2026-09-03** — a week before the opener, so a fix has somewhere to land. **⚠️ CORRECTION TO THIS ROW, SAME DAY, AND IT CHANGES THE RECOMMENDATION RATHER THAN THE DEADLINE.** I measured "is this workflow tested?" as *does any file under `draft/tests/` mention its name* — a ONE-DIRECTIONAL PROXY, and it was wrong in both directions. **`standing-check.yml` was on my no-test list and should not have been: it runs `pytest draft/tests/test_sleeper_trending.py` and `node draft/tests/component_write.test.js` INSIDE ITSELF, under the step names "Test the capture and the check before trusting either" and "Test the writer before trusting what it writes".** The relationship runs workflow→test and my grep only looked test→workflow. Re-measured both ways: **24 scheduled workflows · 13 have a test that mentions them · 6 INVOKE a test suite before trusting their own output · 10 have neither** (`data-inventory`, `external-ingest-run`, `inbox-health`, `kalshi-capture`, `mutation-manifest`, `odds-capture`, `sleeper-active-probe`, `weekly-proj-snapshot`, `weekly-routes`, `weekly-snap-counts`). **And "a test mentions the name" is not evidence either** — it is the same proxy pointing the other way. Only the 6 that RUN a suite have anything watching them. **SO THE FIX IS ALREADY HOUSE STYLE AND I DO NOT NEED TO INVENT IT:** `draft-data` (8 suites), `lab` (12), `rehearsals` (5), `self-audit` (4), `standing-check` (2) and `market-capture` (1) all test before they trust. **The recommendation for the four capture workflows is that pattern plus the week-aware empty check — not a new mechanism.** **AND THE SHARPER CUT, which is what is now RATCHETED in `draft/tests/test_every_gate_can_fail.py`:** the question that matters is not *is the workflow mentioned in a test* but *does anything verify what it commits before it commits it*. **24 scheduled workflows · 18 COMMIT data · 5 run a test suite first · 2 delegate to a self-checking script (`own-weekly-proj`, `weekly-grade`) · 11 commit with NOTHING verifying.** Ratcheted at 11 rather than failed, because eleven new reds four days before the draft get switched off by Saturday. **Stated limit, in the test itself:** script detection is a regex over `run:` blocks and misses inline heredocs, so `data-inventory` and `external-ingest-run` read as unverified because they invoke no NAMED script. That may be unfair to those two — a limit of the measurement, not a finding about them — and a test asserts the reason still applies, so if either grows a named script the count gets re-checked rather than silently inherited. **🔴 ESCALATED 2026-08-18 — THIS ROW NOW PROTECTS THE ONLY EXPERIMENT THAT CAN SETTLE THE SOURCE RULING, AND THE FAILURE HAS ALREADY HAPPENED ONCE.** Tracing P39 today established that **we have never measured our model against Sleeper on any season** — the promotion bar is *"beat BOTH NAIVE baselines"*, `api.sleeper.app: "no route"`, and `model_accuracy_v5` says the comparison *"first becomes measurable at the January 2027 grade of the frozen 2026 proj_series"*. **`weekly-proj-snapshot.yml` produces that series. It is not an input to the experiment; it IS the experiment**, and it is the only thing that can ever answer `CORY-ASKS` A2. **AND IT HAS ALREADY LOST A DAY, SILENTLY.** `draft/data/proj_series.json` holds 10 captured dates and **2026-08-10 has a `sleeper` row and NO `fantasypros` row** — FP capture rate 9/10. **Nothing reported it.** That happened in the easiest possible conditions: preseason, daily cadence, no game-week timing pressure. **Retroactive fetches leak (exp33), so it can never be backfilled.** **GUARDED AS OF TODAY:** `draft/tests/test_proj_series_completeness.py` (5 checks) ratchets incomplete dates at the known 1, **names the known hole so a swap cannot hide behind the same count**, carries a fail arm, and states its own limit — it compares sources WITHIN a captured date, so **a day the job never ran at all leaves no row and is still invisible.** That gap needs an expected-cadence field and belongs with the workflow fix, which is this row. **Priority is no longer routine in-season hygiene.** **✅ THE LIMIT I SHIPPED THIS CHECK WITH IS NOW CLOSED, AND IT DID NOT NEED THE FIELD I SAID IT NEEDED.** I wrote that a day the job NEVER RAN leaves no row and is invisible, and that catching it *"needs an expected-cadence field the series does not carry"*. **Wrong — the cadence is knowable from the workflows.** `draft-data.yml` runs `0 8 * * *`, DAILY, and is what appends these rows (`weekly-proj-snapshot.yml` adds the Sunday one). So the expectation is simply contiguity. **MEASURED: 2026-08-09 → 2026-08-18, ten calendar days, ten captured, ZERO whole days missing.** The only defect remains the source-level hole on 08-10. **AND A THIRD CHECK, because the first two together still pass on a DEAD series:** if the capture stopped today, tomorrow's run would find the span perfectly contiguous and perfectly complete while nothing was being written at all. A freshness bound (newest date ≤7 days old) is what makes the other two mean anything. **Seven days is deliberately generous for a daily job** — enough to catch a dead capture inside a week, loose enough that one missed run or a weekend of CI trouble cannot make it cry wolf. **7 checks, all three arms mutation-tested on synthetic input.** The workflow fix in this row is still owed — a check that notices a hole is not a job that stops making them. **🛠️ PARTIAL FIX SHIPPED ON `claude/fantasy-football-research-926y6z` — the detection half, not the enforcement half, and the split is deliberate.** `draft-data.yml` now carries a step *"Is today's projection snapshot COMPLETE?"* that reads `proj_series.json` after the build and emits a GitHub Actions **::warning::** when today is PARTIAL (naming the missing source), when today has NO row at all, or when the file is missing entirely — and writes the same line to the run summary. **It WARNS and does not FAIL, and that is a judgement I want on the record rather than buried:** refusing to publish a whole draft board because one projection provider was unreachable trades a real immediate cost for a speculative one, and a gate that blocks the board on a third party's uptime gets switched off in a week. **What was missing on 08-10 was not enforcement — it was anyone knowing.** Whether it should block is A's ruling and stays open in this row. **Exercised, not assumed — I extracted the step's `run:` block and ran it as CI would, against four fixtures:** complete day → clean line; **partial day (sleeper only, the exact 08-10 shape) → warns and names `fantasypros`**; no row for today → warns; missing file → warns. All exit 0, so it cannot break the board. **The heredoc indentation trap was the thing to check and it is fine — the YAML block scalar strips the common indent, so the Python arrives at column 0.** **⚠️ CORRECTION TO WHAT I JUST TOLD A — I UNDERSTATED MY OWN WORK AND LEFT A QUESTION HALF-ANSWERED.** I said the warning risks being *"a better-instrumented silence"* if nobody reads run summaries. **The enforcement half already exists and I built it an hour earlier without connecting the two.** `ci.yml:197` runs `python -m pytest draft/tests`, which collects all 7 checks in `test_proj_series_completeness.py` — none is `repo_parity`-marked — **so a new hole turns the BUILD RED, which does not depend on anyone reading anything.** **PROVEN END-TO-END, not inferred:** I injected a partial `2026-08-19` (sleeper only) into the real series and ran it — `FAILED ... 2 captured dates are missing a source (baseline 1): 2026-08-10 has only ['sleeper'], 2026-08-19 has only ['sleeper']` — then restored the file byte-identical (`git diff` clean). **SO THE ACCURATE PICTURE IS THREE LAYERS, NOT TWO:** ① same-day, attention-dependent — the workflow `::warning::` in the run that caused it · ② next-commit, ENFORCED — CI red, naming the date, no attention required · ③ **still uncovered: the capture job commits the partial day.** CI catches it one commit later; it does not prevent it. **THAT MAKES A'S RULING SMALLER THAN I FRAMED IT.** The question is not *"should this be enforced at all"* — it is enforced. It is *"is preventing the bad commit worth blocking the board on a provider outage, when CI already refuses the next commit?"* **My own answer, offered rather than assumed: probably not.** |
| 9 | **2025 cannot be graded** — lateral handling in the pbp rebuild. The 2-point gap is fixed (mean_abs 0.489 → 0.149); laterals are what still blocks. | **A** | OPEN | Unlocks a second grading fold. Post-draft. **recheck 08-31** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** |
| 10 | **STALE ROW — BOTH STORES ALREADY EXIST (found 08-17).** `nflverse_weekly_points_2021.json` (5,401 player-weeks, 18 weeks, `complete: true`) and `_2022.json` (5,351, 18, `complete: true`) are committed. **So the first clause is false and the diagnosis with it.** The real constraint is the **scoring-fingerprint boundary**: 2021-22 carry `220bf4c671786351`, 2023-25 carry `bd8f3e50bd67a9ce` — different tables, so they cannot be POOLED. That is exactly why `weekly_volatility` refused those seasons. **C is assigned to build a store that exists**, the same "check what exists before fetching" trap the relay flagged to C on 08-17. | **C** | OPEN | **Do not build it — it is there.** Routed to C. **unblocked by** a re-score of 2021-22 under the current table OR a fold design that stays inside one fingerprint; **owner C**, **recheck 08-24.** What is genuinely open is narrower: a second graded fold needs a **re-score of 2021-22 under the current table**, or fold designs that stay inside one fingerprint — which `routes_tprr_study.py` demonstrates works and which gave it 3 folds instead of 1. |
| 11 | **Board-derived artifacts are not in the freshness registry** — 13 entries, none of them the ones that refuse publication. This is the whack-a-mole. | **A** | OPEN | Register only the genuinely derived ones. **Never** auto-regenerate a board assertion (rows 2, 3). **recheck 08-31** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** |
| 13 | ✅ **MEASURED 08-17 — snap share is a NULL, and it is the SECOND opportunity metric to die the same way.** Preregistered (`SNAP-SHARE-PREREG.md`, committed first), graded (`snap_share_arm.json`, `clears: false`): partial ρ +0.140 / +0.060 / **−0.020** / +0.139 over four folds, 2 of 4 beating the null. **Cause, named in the prereg before the run: ρ(snap share, prior-season points) = 0.81–0.84 every fold** — four-fifths redundant with the points it was meant to complement. Raw snap share also never beats carry-forward (0.60–0.70 vs `naive_prev`'s 0.71–0.77). **THE GENERALISATION: TPRR died at 0.74–0.82 vs targets, snap share at 0.81–0.84 vs points — season-grain opportunity metrics here are ~80% redundant with volume. Measure the collinearity first; it costs one line and calls the result in advance.** `draft/audit/snap_share_row13_2026-08-17.md` | **D** | CLOSED | **Weekly job keeps running (Rule 3c); nothing installs.** Trigger, declared pre-run: **weekly grain** (`share_volatility` is already in the store and snap share's *variance* is the untested part) or **per-position** (RB committee splits). **unblocked by nothing — the data is on disk; owner D, recheck post-08-22.** |
| 14 | **MEASURED 08-17 — season-grain TPRR is a real null, and the reason is collinearity.** Preregistered (`ROUTES-TPRR-PREREG.md`, committed first in `891c9e6`), graded (`routes_tprr_study.json`, `clears: false`). **E1 persistence is strong** (+0.671/+0.670/+0.653/+0.686 over four transitions, null p95 ≈ +0.13) **but is largely inherited**: targets carry at +0.673/+0.730/+0.689/+0.694, near-identical. **E2 increment fails the bar** — partial ρ (TPRR given targets) +0.128 / +0.171 / **+0.026**, and the first beats its null by 0.0009, i.e. noise. **Mechanism, measured: ρ(TPRR, targets) = +0.74…+0.82 every season, while ρ(TPRR, routes) is only +0.30…+0.44.** So TPRR mostly *is* target volume, which **contradicts the argument the store was built on** (*"target share alone cannot separate them"* — at season grain it separates 74-82% of it). **Rule 3d passes on my own null:** input varied (203-215 distinct values, 5.1-8.3× spread), it arrived (**0 rows lost at the join**, 136/136 · 150/150 · 154/154, recorded per fold), and the test could fire (E1 at +0.67; E2's control at +0.37-0.42; E2 itself +0.171 in one fold). `draft/audit/routes_tprr_row14_2026-08-17.md` | **D** | CLOSED | **Weekly job keeps running — Rule 3c, not a close call.** Triggers (declared in the prereg *before* the number existed): weekly grain or per-position; a true routes feed. **One trigger ADDED from the measurement:** test `routes` as a **volume/participation** measure rather than as a denominator — ρ(TPRR, routes) is only +0.30-0.44, so routes-run carries what the ratio discards, and it needs no new data. |
| 13b | **MERGED INTO ROW 13 (08-17).** This row duplicated 13 on a premise both shared and neither checked — that snap counts feed `projections.py`. They do not (row 13). Kept as a line rather than deleted, because two rows written from one unverified sentence is itself the record worth having. | **D** | CLOSED | None. Work continues under row 13; the re-test trigger lives in row 17. |
| 15a | **`advanced_stats_*` WAS decided — this half of row 15 was wrong.** Preregistered, graded (`advanced_efficiency_study.json`, `status: graded`, `clears: false`), verdict published in `advanced_metrics_study_2026-08-16.md` §6 as an honest null. **Rule 3d passes on it, better than row 18 did:** input varies emphatically (5,935 player-weeks in 2024; `cpoe` 651 distinct, `rec_epa` 4,136, `wopr` 2,936); **the study records its own join population** (`coverage: identical_population: true`, n=481) — the counter row 18 lacked; and the test could fire (4 of 12 cells beat control; the REC-3-shaped bar was cleared by own_v6). **Not reopening it.** | **D** | CLOSED | Trigger recorded in row 17 and `DATA-LIFECYCLE.md`. Reaches only studies (`empirical_draft_value.py:122`), never the board — step 4 confirmed. |
| 15b | ✅ **CLOSED 08-17 — the paid props data is GRADED for the first time.** `props_season_projection.py` re-run (module unmodified); the refusal naming three files that exist is gone. **Verdict `clears: false`** — props lose MAE at RB (+12.19), TE (+3.29) and WR (+0.93), winning only at QB (−33.00). ⚠️ **THE SPEARMAN FIGURES MUST NEVER TRAVEL ALONE: props 0.93-0.97 vs own_v6 0.66-0.74 is NOT projection skill.** The arm sums prop lines from **all 18 in-season weeks** (`season_implied_totals`, `:199`), so a week-17 line knows the season and an injured player simply has no rows — while own_v6 is a preseason forecast. **The module preregistered this** (`in_season_information_asymmetry`), so it is a correctly-labelled asymmetry, not a leak. **Coverage:** 424 forecasts, 4 unmatched names. `draft/audit/props_first_grade_2026-08-17.md` | **D** | CLOSED | **THE FAIR TEST EXISTS, IS PAID FOR AND HAS NEVER RUN:** `historical_props_week1_*.json` (2,283 player-weeks) — week-1 lines close before any game, the project's own leak-free rule for season-total features. Needs a prereg and a new code path in A's module, so **parked to A**. Also: 2023 and 2024 remain ungraded (`GRADED_SEASON = 2025` only) — two more folds on disk. Asymmetry pinned by `test_props_asymmetry_is_declared.py`. **unblocked by** a prereg for the week-1 arm; **owner A**, **recheck post-08-22.** |
| 15c | 🟡 **THE WEEK-1 PROPS STORES CARRY `any_td` AS DECIMAL ODDS, AND IT IS THE BINDING CONSTRAINT ON THE ONLY LEAK-FREE PROPS ARM.** 2025 values span **0.80-4.21, median 2.69**; `line_to_points` prices each at 6.0, projecting Jalen Hurts at **786.9** for the season. The fetcher's `AMERICAN_IMPOSSIBLE_BAND` guard exists — **the committed stores predate it**, exactly as `empirical_draft_value.props_ordering()` documents. **Consequence:** the week-1 arm must drop the column and therefore carries **no rushing or receiving touchdowns** — a stated LOWER BOUND that penalises RB (goal-line) and QB hardest, which are the two positions it loses. It **beats own_v6 on both metrics at WR and TE** (MAE 40.30 vs 41.45, ρ 0.632 vs 0.591; 28.91 vs 35.13, 0.706 vs 0.541) — **but Amendment 2 killed that as evidence: props beat NEITHER `naive_prev` NOR `recency_blend` at ANY position in EITHER valid season (0 of 2; the prereg's rule was 1 of 3 kills it).** A win against one model while losing to the trivial baseline that model beats is a statement about the comparison, not the input. own_v6 itself DOES beat both baselines at RB/WR/TE, so it is not broken; the §1 effect is a POPULATION effect — own_v6 is materially weaker on the ~230 heavily-market-covered players this arm scores (WR ρ 0.591 vs 0.702 on the wider set), which is a question for A about own_v6. `draft/audit/props_week1_arm_2026-08-17.md` | **C** | OPEN | **Re-fetch the three week-1 slates with the guard in place — ~48 odds calls**, against 16,320 for one full season. Cheapest high-value fetch left in the props agenda. **It unblocks three things:** this arm at full strength, `empirical_draft_value.props_ordering()` (a lower bound today for the same reason), and any TD-aware props work. **unblocked by nothing — the guard already exists; owner C, recheck 08-20.** |
| 16 | **ONLY ONE COPY WAS EVER COMMITTED — the diff has no second input.** Searched every committed JSON for `spread_line`: exactly one file. The pbp copy (C's *3 seasons / 854 games*) lives only inside a live `import_pbp_data()` call and appears nowhere in the repo but the relay's paraphrase. So the truer statement is not *"nobody has checked they agree"* but **"one copy was never retained, so nobody could."** **NOT a data gap:** nflverse pbp probed 2026-08-17 → **HTTP 200**. **DONE meanwhile:** the reconciler is built and proven (`draft/tests/test_vegas_lines_reconcile.py`, joins on (season,week,home,away), refuses to inner-join, 3 controls incl. one perturbing a real row of the 1,426); and the committed copy is validated — counts match provenance, all 205 drops explained as unlined 2026 fixtures, 0 duplicate fixtures, 0 team-week collisions, REG-only holds, sign convention holds, no line stored as 0. **2022's 271 games is CORRECT** — BUF@CIN wk17, the Hamlin cancellation — now pinned so nobody re-investigates it. `draft/audit/vegas_lines_row16_2026-08-17.md` | **D** | WAITING (on C) | **Fetch spec parked in `ROUTES.md` → TO: C** — commit the pbp lines as `draft/backtest/vegas_lines_pbp.json`. The skipped test turns green-or-red automatically the moment it lands; nothing else is needed from anyone. If C has not produced it by 08-22, it is post-draft work — the diff changes no number Cory drafts from. |
| 24 | 🟢 **THE "SLEEPER HAS NO HISTORY" BLOCKER IS FALSE — AND THE PROOF SAT IN AN ACTIONS LOG FOR A DAY.** `sleeper-hist-proj.yml` ran 08-16, succeeded, and found Sleeper serves historical projections for 2023/24/25. **2025 passed every leak gate (`clean`)**; 2023/24 correctly failed on `leaked_markers`. The verdict never committed: the run was dispatched from a worktree branch and the guard blocks feature-branch pushes to `main`. | **A** | 🔴 OPEN | (1) Re-dispatch the workflow FROM `main` so the verdict commits. (2) Write the three-way prereg the probe demands. (3) Run Sleeper vs FP vs blend on 2025, **matched population**. **Verdict committed `0f9ecbe2` — the refusal is OVERTURNED; unblocked by the per-player fetch, owner C, recheck 08-18.** `draft/audit/sleeper_hist_proj_verdict_recovered_2026-08-17.md` |
| 20b | **Row 20's premise is superseded.** `proj_mean_blend.json` refused with `no_control` because Sleeper had no per-player history. It does — 2025, gate-clean. The refusal was correct on the evidence it had. | **A** | OPEN — **unblocked by the per-player fetch (row 24), owner C, recheck 08-18** | Revisit, do not delete. The blend arm is constructible now. |
| 23 | **A spent fetch whose answer lives only in a log is a fetch paid for twice.** The workflow prints its verdict then exits 0 when dispatched off `main`, so nothing surfaces that anyone must act on. | relay | OPEN | Upload the verdict as a run artifact a `main` job can commit, or make the off-`main` path end loudly enough to be retrieved. **recheck 08-31** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** |
| 30 | **`weekly_volatility` is the FIRST metric to survive the collinearity diagnostic — but only as `cv`.** ρ(sd, mean) = **+0.83…+0.86**, ρ(cv, mean) = −0.66…−0.74: neither is orthogonal. **The decisive test — does persistence survive removing the mean — separates them:** `cv` partial ρ **+0.133 / +0.264**, both beating null (2 of 2) — **though +0.133 sits on the ±0.13 noise floor and is weak alone; +0.264 carries it**; raw `sd` **+0.025 / +0.084**, both BELOW null (0 of 2). **So there is a real level-independent persistent volatility trait, and the board's existing `weekly_sd` is not it** — that field's persistence is entirely inherited from the mean, so wiring it would re-introduce the projection under a new name. **Wire `cv`.** Effect is real but modest. `draft/audit/volatility_collinearity_2026-08-17.md` | **A** | OPEN | Fold into `VOLATILITY-WIRING-PREREG.md` before it ships (A's file, untouched). **Also: its artifact refuses 2021-22 as "different scoring table" — that is the float32 artifact (row 27b), so 4 transitions are available, not 2.** **unblocked by nothing — measured; owner A, recheck post-08-22.** |
| 27b | 🔴 **AND THE BOUNDARY THAT WOULD STILL HAVE SEPARATED THEM IS NOT REAL EITHER — the scoring-fingerprint split is a float32/float64 ARTIFACT.** 2021-22 (`220bf4c671786351`) and 2023-25 (`bd8f3e50bd67a9ce`) were scored under the **same table**: 44 identical keys, three values differing only as float32-vs-float64 renderings of 0.04/0.1/0.1. **Rounding the older table to 6dp reproduces the newer fingerprint exactly**; max distortion from pooling is **<5×10⁻⁶ points on a season total**. The fingerprint is a sha256 of the *serialised* dict, so representation alone changes it — it answers *"byte-identical?"* and was read as answering *"same rule?"*. **Cost so far:** `weekly_volatility` refused 2021-22 (2 of 4 transitions — the brief calls its coefficient *"directional rather than precise"* for this reason), the pace study's registered second fold, and a fold in D's own routes study, refused this morning on the same false premise and now restored (verdict unchanged, `clears: false` on 4 folds). `draft/audit/scoring_fingerprint_artifact_2026-08-17.md` | **D** | IN HAND | **Routed to A** — `weekly_volatility` gains two seasons and is the top post-draft wiring item (brief §7.1); the pace fold is now fully clear (row 17's option 3 answered). **Not reached into: both are other lanes' files.** ⚠️ **The rebuild licence stays open and separate** — 2021-22 are `rebuilt_offline: true`; this shows the TABLE matches, not that the offline rebuild matches a live capture. Lean on `position_weight_transfer.py`'s exact-2023-reproduction check for that, which D has **not** re-verified. **unblocked by nothing — the measurement is done; owner D, recheck 08-19.** |
| 27 | 🔴 **"WE CAN ONLY GRADE ONE SEASON" IS FALSE — ALL FIVE REALIZED STORES ARE POPULATED.** Measured today: **2021: 611 players / 18 weeks · 2022: 589 / 18 · 2023: 586 / 22 · 2024: 603 / 22 · 2025: 585 / 22.** Register row 10 ("no weekly-points store for 2022/2021") is stale — C built them and nobody closed the row. **This is the THIRD time a stated limit turned out to be false** (Sleeper history, spread_line, now this), and n=1-season is the caveat attached to nearly every conclusion we hold. | **E** | 🔴 OPEN | **Re-examine every study whose stated limit is sample size.** The blend, the adjuster fit and the survivorship bound all cite n as the binding constraint. **unblocked by nothing — the data is on disk, owner E, recheck 08-19.** |
| 28 | 🟠 **`proj_sd` HAS TWO CONSTRUCTIONS TOO — 530 `measured-2023-25-error`, 152 `position_variance`.** Same shape as the ceiling defect (row 8b) in a different field, and `proj_sd` feeds survival odds, `adp_sd` consumers and every confidence interval on the board. | **A** | OPEN | Decide whether the 152 are excluded from dispersion-dependent surfaces or the difference is surfaced. **unblocked by the row-8b ruling, owner A, recheck 08-19.** **✅ RE-MEASURED 2026-08-18 ON THE 05:33 BOARD, AND THE SCOPE IS WIDER THAN THIS ROW SAYS — extending 8b/28 rather than filing a new row.** **(1) The counts moved.** `proj_ceiling_source`: **268 `measured-2023-25-p90-x-player-cv` · 267 `measured-2023-25-p90` · 161 `gaussian_z`** (8b recorded 530/73). `proj_floor_source`: identical 268/267/161. `proj_sd_source`: **535 `measured-2023-25-error` · 161 `position_variance`** (28 recorded 530/152). **161 players — 23% of the board — carry dispersion with no per-player measurement behind them.** **(2) IT IS NOT ONE CONSUMER IGNORING THE STAMP. IT IS ZERO CONSUMERS READING IT.** 8b names `playerDollars`; I grepped every surface — `public/js/`, `views/`, `src/` — and **`proj_ceiling_source`, `proj_floor_source`, `proj_sd_source` and `adp_sd_source` are read by NOTHING.** (`adp_source` is read by 3 files, `bye_source` and `variance_why` by 1 each, so the grep is not blind — those are the control.) **(3) WHY THAT MATTERS MORE THAN THE 161.** `draft/projections.py` states the purpose of these stamps in its own comment: *"Absent stays absent: an unmeasured band keeps the Gaussian rather than silently receiving a filled-in p90, and `proj_ceiling_source` is how a consumer tells the two apart — the same rule `proj_sd_source` follows, and **the rule whose absence let `0.25 * proj_mean` reach the board once**."* **The mechanism built to stop the worst bug this project has had from recurring invisibly has no reader.** It is a guard that is written but not wired. **Practical exposure for 08-22 is small and I will not inflate it** — in Cory's pick window (ADP 27-160) only 9 of 132 are `gaussian_z` and all nine are K/DEF (row 4v). **The cost is standing, not immediate: the next time a band goes unmeasured, nothing on any screen will say so.** |
| 25 | 🔴 **TWO WEIGHT SYSTEMS DISAGREE ABOUT UPSIDE, AND WHICH ONE RUNS DEPENDS ON A TOGGLE IN CORY'S BROWSER.** `MEASURED_WEIGHTS.ceiling = 0` (`engine.js:567`). But `autoWeights()` — the round-aware path — ships **ceiling 0.45 in Anchor, 0.6 in Build, 0.8 in Fill** (`engine.js:3386-3396`). So the claim 'the model ignores upside entirely' is true only with Auto OFF. State lives in `localStorage` (`app.js:3040`), so **the weights Cory drafts under depend on a browser toggle nobody has audited.** | **A** | 🔴 OPEN | Decide which is authoritative, and surface WHICH IS ACTIVE on the war room. This reframes register row 5 (the ceiling ruling): it is not zero-vs-non-zero, it is *which of two systems is live*. **recheck 08-31** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** |
| 26 | 🟠 **THE ADJUSTERS HAVE NEVER BEEN TUNED, AND THE STATED BLOCKER IS A CHOICE RATHER THAN A LIMIT.** `autoWeights()` docstring: *"WHAT THIS IS NOT: backtested. Three prior drafts is not enough to fit weights against."* **But `PRE-REGISTRATION-three-season-replay.md:362` says the n=3 is `one league, ONE SEAT` — and line 396 lists the fix in its own extensions: `SEAT — 10 managers, not 1. Run the tools from every seat, not only [one]`.** Ten seats × three seasons is ~30 draft-seasons, not three. The replay harness already drives the REAL `engine.js` rather than a re-implementation, which is the hard part and it is done. | **A** | 🔴 OPEN — **this is Cory's FIX order** | Fit the phase constants (need 0.35/0.9/1.45, tier 1.35/1.2/1.0, ceiling 0.45/0.6/0.8, the 2/6/10 round boundaries) against the ALL-SEATS replay, not against Cory's seat alone. `POLICY-TOURNAMENT.md` already carries the small-n guard for conditional rules. **Post-08-22** — this is a fit, not a hotfix, and fitting weights during draft week is how a guard becomes a rubber stamp. **recheck 08-31** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** |
| 21 | 🔴 **THE BOARD DOES NOT BLEND ANYTHING. `proj_mean` IS SLEEPER, SCALED.** `proj_baseline == proj_sleeper` for **422 of 422** players with both — exactly, no exceptions — and `build.py:1003` declares the formula itself: `"sleeper_baseline * (1 + opportunity_adj)"`. **`proj_fantasypros` (422 players) and `proj_ownmodel` (416) are carried on the board and DISPLAYED, but neither enters `proj_mean`.** So "Sleeper vs FantasyPros vs blend" was never decided — we defaulted to Sleeper. FP is the only source with measured skill (12/12 cells beating naive baselines, bias sign transferring out-of-fold) and it is the one not used. | **A** | 🔴 OPEN | **This is Cory's question and it is a ruling, not a measurement.** Decide the source policy. **Unblocked by the per-player fetch (row 24), owner C, recheck 08-18.** Note row 20: the blend study refused (`no_control`) for want of Sleeper history, so "we measured and kept Sleeper" is NOT what happened — nothing was measured against Sleeper at all. **⭐ RE-MEASURED ON THE 05:38 REBUILT BOARD, 2026-08-18 (relay), AND THE HEADLINE NOW UNDERSTATES IT.** This row says *"Sleeper, SCALED"*. Since Cory's 08-17 ruling 1 set `opportunity_cap` 0.15 → **0.0** (`8b95d92a`), the formula `sleeper_baseline × (1 + opportunity_adj)` evaluates with `opportunity_adj == 0` for **all 696 players** — `max(-0, min(0, (z/2)×0))`, which is why the field's maximum is literally `-0.0`. **So `proj_mean` is no longer Sleeper scaled. It is Sleeper, UNMODIFIED, to the cent: `proj_mean == proj_sleeper` for 609 of 609 players carrying both** (this row measured `proj_baseline` on 422; this is the field that actually drives the board, on a larger current population). **Meanwhile 498 players carry a computed `proj_ownmodel` that reaches `proj_mean` for none of them**, and `opportunity_z` is still alive and non-zero on 377 — the inputs are all being computed and then multiplied away. **NOT A REGRESSION AND NOT A DROPPED RULING:** the cap zero is Cory's own ruling, and `proj_mean_blend.json` carries `ship: REFUSE` with the reason this row's note already gives — the control arm is unmeasurable because Sleeper has no per-player history. **The consequence is unchanged and now total:** every projection Cory drafts on Saturday is Sleeper's, and the unblock is rows 22/24 (per-player FP capture), not a new study. **📐 NEW EVIDENCE FOR THIS RULING, 2026-08-18 (relay) — the disagreement is POSITION-SHAPED, and RB is the outlier by 3-5×.** Measured in Cory's actual pick window (ADP 27-160, the 123 players there carrying BOTH sources; `proj_mean == proj_sleeper` for **123 of 123**): **RB n=37 — median FantasyPros − Sleeper = +21.4 pts (+15.3%), FP higher for 33 of 37.** WR n=48 → +6.1 (+4.3%). QB n=21 → +4.8 (+1.4%). TE n=17 → +4.2 (+3.0%). **So "Sleeper vs FantasyPros" is not one decision — at WR/TE/QB the two sources are within a few percent and the choice barely moves a pick; at RB they differ by a sixth of a season and it moves ranks by dozens of slots.** **AND THIS IS THE MECHANISM UNDER REGISTER ROW 2c** (*high-upside young RBs are under-ranked*), which I re-measured today and confirmed TRUE: RJ Harvey board **144** vs ADP **83**, Bhayshul Tuten board **94** vs ADP **56**. Their Sleeper numbers are 125.6 and 158.8; FantasyPros says **145.2** and **192.5**. Both sit below the RB replacement level of 179.3 on Sleeper's number, which is exactly why they rank where they do. **The market is closer to FantasyPros on these players.** **⚠️ WHAT THIS IS NOT: EVIDENCE THAT FANTASYPROS IS RIGHT.** `ROUTES.md:1662` states the binding constraint — *"Sleeper/FantasyPros past accuracy is unmeasurable — 2026 is the first gradeable season."* This measures the SIZE and SHAPE of the disagreement, not which side of it is correct, and no amount of staring at it will settle that before 08-22. **WHAT IT DOES CHANGE: the ruling can be POSITION-SCOPED.** A blanket source choice pays the RB cost or takes the RB risk across the whole board; ruling RB separately is a much smaller commitment, and RB is where Cory's picks 33-93 mostly land. **⚠️ CORRECTION TO THE EVIDENCE ABOVE, SAME DAY, BEFORE ANYONE RULES ON IT — I TRIANGULATED AGAINST OUR OWN MODEL AND IT DOES NOT SUPPORT THE READING.** The RB gap above is real, but I let it drift toward *"Sleeper under-projects RBs and the market agrees with FantasyPros"*. **Our own model — the source P39 grades BEST at RB (ρ .7968 vs FP .7649 vs Sleeper .7538) — sits BELOW Sleeper, not above: median `proj_ownmodel − proj_sleeper` = −20.1, higher for only 11 of 36.** FantasyPros is above Sleeper for 33 of 37; our model is below it for 25 of 36. **The two non-Sleeper sources disagree with Sleeper in OPPOSITE DIRECTIONS — only 11 of 36 RBs (31%) have both above it, and 21 of 36 are split.** **THE CRUX, AND IT IS A CATEGORY ERROR I NEARLY SHIPPED: ρ IS AN ORDERING STATEMENT AND MY +15.3% IS A LEVEL STATEMENT.** Spearman is rank-based and therefore scale-invariant — *"we are better at RB"* means our model ORDERS RBs better and says nothing about whose point totals are right. **Those are different currencies and they do not add up to "Sleeper is low at RB".** **WHAT SURVIVES:** the source rankings genuinely REVERSE between RB (ours .7968 > FP .7649 > Sleeper .7538) and WR (Sleeper .7724 > ours .7634 ≈ FP .7621) — **that is graded, and it is the real argument for ruling per position** — and the RB disagreement is far larger than any other position's, so RB is where a ruling is worth making. **WHAT DOES NOT SURVIVE: any claim about the DIRECTION Sleeper is wrong at RB.** **✅ AND HERE IS THE SAME QUESTION ASKED IN THE RIGHT CURRENCY — ORDERING, not level — which is what ρ actually measures. This is the version to rule on.** Ranking the 36 RBs in Cory's window by each source: **Spearman(Sleeper order, our order) = 0.727, with 11 of 36 moving 8+ slots.** The orderings genuinely differ, and **P39's graded numbers say ours is the better one at RB (.7968 vs Sleeper's .7538).** No level claim is involved — this is rank against rank. **WHERE IT MOVES A PICK CORY CAN MAKE (Sleeper-rank → our rank):** Zach Charbonnet **33 → 14** (ADP 137.7) · Woody Marks **30 → 16** (ADP 154.3) · Rico Dowdle **20 → 9** (ADP 79.3) · RJ Harvey **24 → 13** (ADP 83.0). **Every one is available inside his pick range (33-148), and the board is ranking them by the ordering that grades worse.** In the other direction: Bhayshul Tuten **12 → 34** (ADP 56.3) · Rhamondre Stevenson **13 → 26** · David Montgomery **8 → 17**. **THIS ALSO HALF-CORRECTS REGISTER 2c.** That row names Harvey AND Tuten as under-ranked young RBs. **Our best-grading RB source agrees on Harvey (24→13, we like him more) and flatly disagrees on Tuten (12→34, we like him much less).** 2c should be re-scoped to the players our own ordering actually supports. **AND THE WR CONTRAST IS THE STRONGEST ARGUMENT FOR RULING PER POSITION:** at WR the two orderings are **ρ 0.294** — 29 of 48 move 8+ slots, near-total disagreement — **but at WR Sleeper is the source that grades BEST (.7724 vs ours .7634), so the board's Sleeper-only choice is already the right one there.** Same board policy, opposite correctness, adjacent positions. **🛑 SECOND CORRECTION, AND IT PULLS MY OWN RECOMMENDATION — I WENT TO THE ARTIFACT BEHIND ρ AND IT DOES NOT SAY WHAT P39'S PROSE SAYS.** **(1) P39's per-source numbers are UNTRACEABLE.** `.7538` (Sleeper RB), `.7649` (FP RB), `.7724` (Sleeper WR), `.7621` (FP WR) **appear in no committed artifact** — every grep hit is an unrelated `rec_epa`/`wopr`/`rush_epa` coincidence. **(2) THE COMMITTED ARTIFACT CONTRADICTS THEM AT WR.** `draft/backtest/model_accuracy_v6.json` reports `spearman_beats_both = TRUE at ALL FOUR positions` — QB .7225 vs .7213 · RB .7968 vs **.7682** · **WR .7634 vs .7344** · TE .7987 vs .7871. P39 says we LOSE at WR to Sleeper's .7724; the artifact says we beat the best baseline there by .029. **They cannot both be right.** **(3) THE ARTIFACT NEVER NAMES ITS BASELINES** — zero occurrences of `sleeper` or `fantasypros` anywhere in it, only `best_baseline_spearman`. **So "our model beats Sleeper at RB" is not a claim this artifact can support: it does not record which source the baseline was.** **(4) AND ITS OWN HONESTY NOTE SHOULD DECIDE THIS BEFORE 08-22:** `graded_season: 2025` — **ONE season** — and *"third candidate graded against 2025 in this lineage (v4, v5, v6)... **2025 has now been read three times; the January 2027 grade is the first untouched evaluation**"*. **SO I WITHDRAW THE PER-POSITION RECOMMENDATION ABOVE.** I told A to order RB by `proj_ownmodel` and keep Sleeper at WR *"because that is what the graded ρ says"*. **The artifact says our model beats both baselines at EVERY position, which removes the asymmetry the recommendation was built on** — and it rests on one season, read three times, with unnamed baselines. **Not a foundation for changing the value term four days before the draft.** **WHAT STANDS, because it needs no ρ:** the orderings differ materially (RB ρ 0.727 Sleeper-vs-ours, WR ρ 0.294) and the named RB movers are facts about the board. **What I no longer claim is that we know which ordering is better.**  **🔑 RESOLVED, 2026-08-18 — AND THE ANSWER IS THAT THE COMPARISON HAS NEVER BEEN MADE.** I traced P39's claim to its artifacts. **(1) THE PROMOTION BAR IS NOT ABOUT SLEEPER.** `model_accuracy_v5/v6.promotion_bar.bar` reads *"beat BOTH **naive** baselines at ALL four positions on BOTH metrics, shared population, leak-free walk-forward — REC-3, Cory-ratified"*. So `spearman_beats_both = True` means our model beat **two naive baselines** (a recency blend and its sibling). **It says nothing whatsoever about Sleeper.** That is why no version of the artifact names Sleeper: Sleeper was never in the comparison. **(2) SLEEPER HAS NEVER BEEN GRADED AGAINST OUR MODEL ON ANY SEASON.** v5 records `api.sleeper.app: "no route"` and `api.fantasypros.com: "no route"` (probed 08-16), and states the consequence outright: *"Arm B is ungradeable on any past season; its bar — beat FP alone — **first becomes measurable at the January 2027 grade** of the frozen 2026 proj_series, which prices Sleeper, FP and own_v5 on the same season."* **(3) THE FP CELLS THAT DO EXIST ARE ON A DIFFERENT POPULATION.** v5's `fp_bar` has real FP 2025 numbers — RB ρ **.7795**, WR **.7423**, QB **.768**, TE **.7892** — against ours of .7968 / .7634 / .7188 / .7987. But its own caveat: *"FP cells are graded on FP's OWN shared population (n=57/88/141/83), v5's on OURS (n=58/99/150/84)... **Per-player FP comparison is impossible from committed disk** — the archive rows were never committed and api.fantasypros.com is unreachable."* **And these are not P39's numbers either** (P39 cites FP RB .7649 / WR .7621; v5 says .7795 / .7423), so P39's figures remain untraceable to anything. **SO THE HONEST ANSWER TO CORY'S QUESTION — *"if we are better at RBs shouldn't we use our projections?"* — IS: WE HAVE NEVER MEASURED OURSELVES AGAINST SLEEPER.** Not once, at any position, on any season. **This is not "one season read three times" as I said an hour ago. It is zero seasons.** The first comparison that can settle it is the January 2027 grade, and the artifact said so before I got here. **WHAT THAT SETTLES:** the source ruling cannot be made on accuracy evidence, because none exists for Sleeper. **It can only be made on judgement, and it should therefore hold through 08-22 under the no-change rule.** **WHAT IT MAKES URGENT INSTEAD:** the 2026 `proj_series` freeze is what makes January 2027 gradeable at all — **that capture is the whole experiment, and it is a weekly job (`weekly-proj-snapshot.yml`) that register row 41 flags as treating a missed week as a benign no-op.** A hole in that series does not just cost a week; it costs the only measurement that can ever answer this. **REVISED RECOMMENDATION: hold source policy through 08-22 under the no-change rule** — the same call A already made for the durability prior — **and make the first post-draft job naming the baselines in `model_accuracy_v6.json`, because a promotion bar that cannot say what it beat is not a bar.** |
| 21b | **Cory's 08-16 blend ruling was never executed, and the board does not say so.** Ruling: *"not replace Sleeper with own_v6 … but blend."* The blend study returned `no_control` and wired nothing; `proj_mean` still reads Sleeper-only. The refusal is recorded inside `proj_mean_blend.json`, but every surface shows a `proj_mean` that looks like a consensus. | relay | OPEN — **unblocked by A's source ruling (row 21), owner A, recheck 08-18** | Surface the refusal where the number is shown, or the board implies a blend that does not exist. Routed to A and B. |
| 35 | 🔴 *(renumbered from 34 at merge — 34 was already the zero-information-adjuster row)* **THE BOARD PUBLISHES WITHOUT ITS UPSTREAM INPUTS, AND NOTHING TRIGGERS THE REBUILD.** Twice on 08-18: A's keeper-vorp fix landed `073aadfc` 03:20:28Z against a board built 02:03:30Z (resolved only because an unrelated rebuild fired at 03:49); and the calibration table was regenerated `f6acbe76` **03:59:04Z** against that same 03:49:25Z board — 9m39s stale — by a commit stating it *"moves every proj_ceiling/proj_floor/proj_sd on the board."* **152 of 535 banded rows now disagree with the table**, and because `MEASURED_WEIGHTS.ceiling` ships at 0.45 this feeds a weighted term in `recommend()`, not just bench-seat option value. **The individual staleness is a symptom; the defect is that no mechanism ties a board rebuild to a change in what the board is built FROM.** `TODO #46` recorded a central freshness registry as the permanent fix for exactly this class and it is not catching these. | **A** | 🔴 OPEN | Rebuild the board before keeper lock (08-20). Then make the trigger real: any commit touching `projection_error_calibration.json`, `build.py`'s inputs, or the vorp path should either rebuild or fail a parity check that names the stale artifact. **recheck 08-21** — date assigned by the relay 08-18 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** |
| 22 | **We capture ONE SCALAR from FantasyPros — total projected points.** `proj_series.json` stores `{player_id: points}` and nothing else, for both sources. FP publishes far more: per-stat projections (which our HISTORICAL grader already parses — `exp_fp_hist_proj` runs `grading_mode: statline`, coverage 1.0) plus **best/worst-case ranges and expert-consensus dispersion**. | **D** | 🔴 OPEN | **The range fields are a PER-PLAYER upside signal, and this project just spent a week discovering it had none** — `ceiling` was `proj_mean × constant` for every player, and 73 still fall back to `gaussian_z`. Walk FP through the eight questions; step 2 evidence is below. **recheck 08-31** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** |
| 22b | **Sandbox egress to `api.fantasypros.com` is BLOCKED** — measured today, `CONNECT tunnel failed, response 403` via the agent proxy. Not a dead end: `fp-projections-probe.yml` reaches it from GitHub Actions, which is how every FP artifact we hold was fetched. | relay | **ANSWERED** | Recorded per Cory's rule — the response code from an attempt made today, not "we can't get it." Any FP field expansion runs as a workflow, not in-session. **recheck 08-31** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** |
| 19 | 🔴 **THE FP-vs-FFC ADP COMPARISON NEVER HAPPENED — IT COMPARED FANTASYPROS TO FANTASYPROS.** `exp_fp_board_coverage.py` builds a map it *names* `ffc_rank` from the board's `raw_adp`, then intersects it with freshly-fetched FP. But at that probe's own commit the board carried **334 players `adp_source: fantasypros` and 4 `ffc`** — `raw_adp` was already FP. Hence ρ = **exactly 1.0000**, and `coverage_overall` 0.191 ≈ the FP-sourced count, not a real overlap. Same defect class as `ceiling`: a field correlated against a copy of itself, reported as a finding. **The go/no-go that wired FP as the board's primary ADP anchor rests on this number.** | **A** | 🔴 OPEN | Rename the variable to what it holds, re-run against a genuine FFC pull, and re-decide the anchor on a real comparison. **recheck 08-31** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** |
| 19b | **That same verdict cannot fail.** The f-string emits `"ρ={rho} vs FFC means the swap MOVES picks (not cosmetic)"` for ANY ρ — and the reading is backwards regardless: ρ=1.0 is *identical ordering*, i.e. maximally cosmetic. | **A** | OPEN | Four-branch `verdict_for(rho)` like `stack_sweep.verdict_for`, plus the known-positive control. **recheck 08-31** — date assigned by the relay 08-17 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** |
| 18b | ✅ **MEASURED 08-18 — the grid edge was real, the ceiling barely moved, and the ASYMMETRY is the finding.** Preregistered (`ORACLE-LAMBDA-PREREG.md`, committed first in `ae2bb60`), graded (`oracle_lambda_sweep.json`). **Verdict `INTERIOR`, λ\* = 0.60, agreed independently by both seasons** — so the optimum was outside the old two-point grid and 18b's defect is confirmed. **The VOIDING reproduction control passed exactly on all six checks** (2,179 / 2,259 rows; ΔMAE 0.1412 / 0.2422 / 0.1219 / 0.2138, read from A's artifact, not from a copy). **But the correction is small: the ceiling moves +0.228 → +0.2379, +4.3%.** Anyone holding "+0.23" was approximately right. **And 18b's second claim — that an oracle needing damping means the form is mis-specified — is REFUTED as stated:** the curve is smooth, unimodal, and turns sharply negative above λ=1. **🔑 THE FINDING IS THE ASYMMETRY.** Split by multiplier side: a **dud** game is worth **+0.55 / +0.33 ΔMAE at λ=0.80**; a **shootout** is worth **+0.06 / +0.13 at λ=0.25–0.50**. **Knowing a game will be LOW-scoring is worth 2.3–7.2× (**corrected 08-18 net of a 40-draw placebo; raw was 5–10×** — register 33) more than knowing it will be high, and wants nearly twice the damping.** **⚠️ AMENDED 08-18: net of placebo the dud side is +0.4514/+0.2967, and the shootout side's placebo is EXACTLY 0.0000 — the free lunch exists only in the shrink direction. The finding survives (p=0.024, every side beats every draw); only the ratio moved.** λ\*=0.60 is not an optimum — it is a compromise between two regimes, and one scalar hides both. Mechanism: a shootout concentrates in one or two players, so scaling a whole roster up over-credits; a dud suppresses everyone roughly uniformly, which is what a multiplier models. **Also: `dropped: 0`** — every eligible row had a team and a line, so absent-stays-absent and A's `m = 1.0` default coincide here. `draft/audit/oracle_lambda_row18b_2026-08-18.md` | **D** | ✅ CLOSED (graded) | **Two things handed on, neither reached into.** (1) **A:** `exp_weekly_env.py:50` still ships `DAMPENING = (1.0, 0.5)` — TERRITORY: A, routed not edited; the published +0.228 should read +0.2379. (2) **The design instruction, which is the valuable half:** every game-environment feature here has been graded as *does the signal work*; this says the failure may be in the APPLICATION — **apply it asymmetrically, fully when the environment points down and weakly when it points up.** Costs no new data, applies directly to the Vegas store (register 18), needs a preregistered arm. **⚠️ CALIBRATION THAT BOUNDS ALL OF IT:** at ΔMAE +0.2379 a PERFECT game-total oracle sits at **77% of the replay's ±0.310 detection floor** (register 31) — the most this channel can ever be worth is smaller than the smallest thing our edge instrument can measure. Register 18's team arm (+0.008) is 30× below even this ceiling. **unblocked by nothing — graded; owner A for the constant, D for the asymmetric arm, recheck post-08-22.** |
| 20 | **"Sleeper vs FantasyPros vs blend" is UNANSWERABLE, because nobody captured Sleeper.** `proj_mean_blend.json` gate = `no_control`: Sleeper has no per-player history for 2023/24/25, so the control arm does not exist. The prereg correctly refused rather than substituting a different source. **This is the retention rule's canonical case — un-backfillable, and it cost us the answer to a question Cory is asking during draft week.** | relay | **MITIGATED — unblocked by Sleeper 2025 rows (row 24), owner C, recheck 08-18** | `proj_series.json` now snapshots BOTH sources (fantasypros + sleeper, 15 entries since 08-09, append-only, deduped). First real answer: **Jan 2027.** Nothing to fix; recorded so the cost is visible. |
| 18 | ✅ **CLOSED 08-17 — Q2 ANSWERED: the join was 100%.** The team-level implied-total arm register 18 asked for was run (`vegas_team_arm.json`, preregistered first, no egress — all inputs committed). **Join survival 2,179/2,179 and 2,259/2,259, zero dropped for no team or no line**, so the original null was NOT a wiring failure. The harness reproduces `exp_weekly_env`'s eligible counts and baseline MAE (5.6729 / 5.7369) exactly from an independent path — the control that makes the comparison mean something. **And the real, correctly-aimed line buys ~nothing: best ΔMAE +0.0016 and +0.0079.** ΔMAE falls monotonically with λ (λ=1.0 worst at −0.15/−0.10), so the optimum is at or below 0.15 — no-op — which says the multiplicative rescale is the wrong transform. **The store's `_note` is still wrong about what +0.23 bounds; the correctly-labelled measurement is simply also thin.** ⚠️ **My bar was badly designed and the artifact says `clears: true` on +0.002** — I required only ΔMAE > 0 with no magnitude. In substance this is a null. `draft/audit/vegas_team_arm_2026-08-17.md` | **D** | CLOSED | **Trigger:** a team-level ORACLE, to separate "weak line" from "wrong transform" (confounded here); and any non-multiplicative form. **`_note` correction still parked to C.** **unblocked by nothing — measured; owner D, recheck post-08-22.** |
| 17 | **COMPLETE 08-17 — every recorded null now carries a re-test trigger.** Vegas (team-level implied-total arm with a join counter, row 18) · snap_counts (prereg a feature, row 13) · advanced_stats (a different construction or a 4th fold, row 15a) · routes (weekly grain / true feed / **routes-as-volume**, row 14) · **pace (a SECOND graded fold — §3 below)** · weekly_volatility (already dated, `VOLATILITY-WIRING-PREREG.md`) · historical_props (no trigger needed — it was never graded, row 15b). **The pace trigger turned up the 2022-store claim in its third home**, and there it cost the most: `pace_arm.json`'s `leak_protocol` marks the REGISTERED selection fold UNAVAILABLE because *"Only 2023, 2024 and 2025 exist"*, so the pace null rests on **one** graded fold. `season_totals(2022)` loads cleanly today (verified read-only). **But the fold is not automatically recoverable** — its priors are 2022+2023, which straddle the fingerprint boundary, so the recorded reason is false while the conclusion may survive on the correct one, which nobody has evaluated. `draft/audit/row17_triggers_and_the_2022_claim_2026-08-17.md` | **D** | CLOSED | **Routed to A** (`pace_arm.py`/`pace_study.py` are TERRITORY: A, untouched) with three options: re-score 2021-22 under the current table · redesign the fold to stay inside one fingerprint (`routes_tprr_study.py` shows this works, 3 folds) · or measure whether a differently-scored prior distorts the feature enough to matter, and record it either way. |
| 29 | **`component_stats_*` is the LAST store stopping without a recorded reason — and unlike rows 13/14 it genuinely does predict.** It feeds `own_model_v6` (the live model) and is graded, but only *through* weekly points, so its own contribution is never isolated: we cannot say whether the component arms help, hurt or do nothing. That is the shape row 13 was wrongly filed as, true here. Filed 08-17 by D rather than started, because isolating it means an **ablation of the live model** — build own_v6 with and without the component arms and grade both — which is `own_model_v6.py`'s territory, needs a prereg, and would read 2025 for the **fifth** time (`pace_arm.json`'s multiplicity note already flags four). Starting it late on 08-17 and leaving it half-done is worse than naming it precisely. `draft/audit/row17_triggers_and_the_2022_claim_2026-08-17.md` §4 | **A** | OPEN | Prereg the ablation before running it, and declare the 2025 re-read count in the prereg. Post-08-22. |
| 33 | **`component_stats_2025.json` and `nflverse_weekly_points_2025.json` DO NOT COVER THE SAME PLAYERS, and 2025 is the only season where they diverge.** **1,067 of 5,752** player-games in the 2025 component store have no score in the 2025 weekly-points store — **347 distinct players**, WR/TE-heavy (160 WR, 96 TE, 78 RB, 13 QB), most missing 1-4 weeks rather than a whole season. The identical join on **2024 drops exactly 0**. Both stores report themselves healthy: `coverage.complete: true`, field population 100% on every field. **Nothing is wrong with either file's own self-check — the gap only exists BETWEEN them, which is precisely the place no artifact-level check looks.** Found 08-18 by `opponent_strength.py` counting `no_opponent` and `no_points` separately instead of as one `unjoined` total; the same run also found and fixed a three-way team-code disagreement (BDL `WSH`/`LAR` vs components `WAS`/`LA`) that was dropping two entire offences, 614 player-games across 2024-25. | **C** | 🔴 OPEN | Establish WHICH store is short and why 2025 only — most likely the weekly-points fetch applies a usage/roster threshold the component fetch does not. Then either backfill or record the truncation in both artifacts so downstream studies can see it. **Every 2025 study joining these two is silently working on 81% of the population, and the missing 19% is the low-usage tail — not a random sample**, so any per-player rate, floor or replacement level built from them is computed on a truncated distribution. **recheck 09-05** — date assigned by the relay 08-18 so this row is covered by `register_recheck_check.js`; **the owner may change it, and changing it with a reason is a legitimate way to clear the check.** |
| 12 | **`config_confirmed` and every local ruling were being wiped on each build.** `build.py --league-id` rebuilt the config from Sleeper and kept only two keys. | relay | **CLOSED 08-17** | `preserve_local_rulings()` + 6 tests. Verified: the shipped config still carries the ruling. |

## 🟢 CHECKED AND CLEAN — recorded so nobody re-investigates

| what | finding |
|---|---|
| **Do the model-scoreboard actuals inherit the weak `import_weekly_data` coverage?** (611 of 1,708 on-field players classified) | **No.** `fetch_component_stats.py` pulls the nflverse `player_stats` release files directly by URL and never calls `import_weekly_data`. The exclusions resolve to row 8, not a new defect. Checked 2026-08-17. |
| **Are per-model exclusion counts (115 vs 211) an unfair comparison?** | **No.** `model_accuracy_backtest.py` already computes `head_to_head_shared_population` over the intersection. The ranking that decides which model ships is on a matched denominator. |
| **Do other interval labellers carry the "CI includes $0" bug?** | Swept at artifact level. Three instances found and fixed (`frontier`, `cory_conditional`, `stack_sweep`); 42 co-located pairs across 6 artifacts now consistent. Scope limit stated in the test. |
| **Are the snap-count stores partial?** | No — join 0.971–0.992. But the refusal floor was 0.70, 27 points below anything observed, and is now 0.95. |

## 🔵 VERIFICATION OWED — things believed true but not observed

| what | owner | next action |
|---|---|---|
| **Does a kicker ever reach Cory's visible top options?** `demoteFlaggedOnesies` IS called in `recommend()`, but it demotes K/DEF only when **a rail fired on that player** or he is **`capped`** — not for being a kicker. Pre-draft, with no K rostered and no rail, a kicker with positive VORP is not demoted by that path. Raw `vorp` order puts them at board 105–150 against ADP 323–336. | **B** | **Observe it, do not infer it.** Open the war room at picks 1, 40 and 100 and look. If no kicker appears, this closes and the raw-order drift is cosmetic. If one does, it is draft-critical. **The relay read the code and deliberately did NOT call this a defect** — three over-claims today came from exactly that inference. |
| `npx playwright install --with-deps chromium` on a real runner — the browser rehearsals' one untested line. | relay | Observed on the first scheduled `rehearsals.yml` run after it reaches `main`. |
| The board's structural properties on a *published* board (health 100%, replacement-sensitivity) — currently only seen on a refused candidate. | relay | **unblocked by the board publishing (row 1), owner A, recheck 08-18** — confirm on the first successful publish. |

---

**Rule for adding a row:** anything that could change a number Cory drafts or
starts on. When in doubt, add it — a row costs a line, a missed defect costs a
season.

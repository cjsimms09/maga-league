# DEFECT REGISTER — every open data/logic concern, with an owner

**Cory, 2026-08-17:** *"it's also your job to make sure nothing gets left behind
or not chased down, especially potential data or logic errors or anything that
messes with our models draft or inseason tools."*

Every row has an **owner** and a **next action**. A row with neither is a defect
in this file. `draft/tests/test_defect_register.py` fails if one appears.

**This file tracks what is BROKEN. `CORY-ASKS.md` tracks what CORY ASKED FOR and
whether he has it** — a defect row and a promise row are different things, and an
ask with no defect behind it used to fall between them.

**Status words mean one thing each:** `OPEN` = nobody has looked since it was
filed · `IN HAND` = someone is on it now · `WAITING` = blocked on a named person
or date · `CLOSED` = fixed and verified, kept for the record.

---

## 🔴 BLOCKING — draft-critical, this week

| # | what | owner | status | next action |
|---|---|---|---|---|
| 1 | **Board has not published since 08-15.** Gate refuses the fresh board. | **A** | IN HAND | Build fresh board; confirm rows 2 and 3 below before regenerating anything. |
| 2 | **A new field joined the constant-multiple family** on the fresh board. This is the ceiling-defect class by name — a field that is a rescaled copy of another cannot be weighted independently. | **A** | OPEN | Run `test_constant_multiple_sweep` on a fresh board; NAME the field. Do not regenerate to green. |
| 3 | **Board's published ranks disagree with its own vorp ordering** (`test_A_ZERO_BONUS_REPRODUCES_THE_BOARDS_OWN_RANKS`). No artifact involved — internal inconsistency in the board Cory drafts from. | **A** | OPEN | Same fresh board; identify which players' ranks disagree. |
| 4b | 🔴 **Cory rejected the war-room design outright** — *"too busy and wordy… looks like an amateur pile of shit"* — and says it does not match reference screenshots he was sent. Draft is 08-22. The surface Cory actually drafts on is the one surface no measurement covers. | **B** | 🔴 OPEN | Get the screenshots from Cory FIRST; do not redesign from taste. Standard is *lots of data, cleanly presented* — fix by hierarchy and density, NOT by deleting data. Coordinate with A: do not describe `proj_mean` as a blend (rows 21/21b). |
| 4 | **`main` CI red** on `matchup_placed_bet` (3) and `trashtalk` (3), since 08-16 23:01. A red main means the gate has confirmed nothing for anybody. | **B** | OPEN | Verify whether B's `/matchup` fix cleared it; if not, fix. |

## 🟠 CORRECTNESS — known wrong, deliberately held

| # | what | owner | status | next action |
|---|---|---|---|---|
| 5 | **`MEASURED_WEIGHTS.ceiling = 0` is contradicted by measurement.** Three preregistered runs, two independent seed sets, every value 0.15–0.65 beats the shipped zero, 3/3 separable. | **Cory** | WAITING (after 08-22) | Cory rules. Framing: the model ignores upside entirely; the exact amount hardly matters. |
| 6 | **ADP-sd ratchet fired** — shipped rule is 1.39× FFC's published dispersion in the 50–100 band. Our constant did not drift; the market tightened. Blast radius: 1 player. | **Cory** | WAITING | Recommendation: leave it, revisit post-season. |
| 7 | **`risk` weight is UNMEASURED** and the term is PARTIAL on backtest boards (age only, 6 of production's 11–13 distinct values). | **A** | OPEN | No date. Needs a prereg before any measurement, or an explicit "stays zero" ruling. |
| 8a | **The dollar model was not re-derived after the ceiling changed.** `playerDollars`' `boom = ceiling − mean` moved: boom's share of a player's total fell **47.2% → 34.9%** (median), every total moved **−18.8%** median, **464 of 603 players moved >10%**. Coefficients 0.22/0.08/0.05 were chosen at the old scale. | **A** | OPEN | Re-derive, post-08-22. Until then the figures are RELATIVE ONLY — which the code says and every surface must keep saying. `ceiling_change_downstream_2026-08-17.md` |
| 8b | **The board carries TWO ceiling constructions** — 530 `measured-2023-25-p90`, 73 `gaussian_z` — and `playerDollars` reads the field, not the `proj_ceiling_source` stamp, so it treats them identically. | **A** | OPEN | Decide whether the 73 should be excluded from dollar figures or the difference surfaced. |
| 8c | **`champodds`, the bench ceiling floor, and every `proj_floor` consumer** were calibrated against the pre-08-17 distribution and have not been re-checked. The floor has had LESS attention than the ceiling, not more. | **A** | OPEN | Same rule: anything reading proj_ceiling/floor/sd/weekly_sd is suspect until re-measured. Measuring is not shipping. |
| 8 | **own_v6 — the live model — has 22.7% of its forecasts excluded from its own accuracy score** (115 of 506; walk_forward 211 of 737). MAE is optimistic by an unmeasured amount. | **A** | WAITING (after 08-22) | Prereg written: `SURVIVORSHIP-BOUND-PREREG.md`. Headline is ranking stability, not a corrected MAE. |

## 🟡 DATA — stores and coverage

| # | what | owner | status | next action |
|---|---|---|---|---|
| 9 | **2025 cannot be graded** — lateral handling in the pbp rebuild. The 2-point gap is fixed (mean_abs 0.489 → 0.149); laterals are what still blocks. | **A** | OPEN | Unlocks a second grading fold. Post-draft. |
| 10 | **No weekly-points store for 2022 / 2021.** The single reason every own-model artifact grades exactly one season. | **C** | IN HAND | Build `nflverse_weekly_points_2022.json`. |
| 11 | **Board-derived artifacts are not in the freshness registry** — 13 entries, none of them the ones that refuse publication. This is the whack-a-mole. | **A** | OPEN | Register only the genuinely derived ones. **Never** auto-regenerate a board assertion (rows 2, 3). |
| 13 | **`snap_counts` feeds `projections.py` and NOTHING grades its contribution.** We cannot say whether it helps, hurts, or does nothing — the same shape as a weight set by an experiment that could not fail. | **D** | OPEN | Grade the snap-share contribution, or state why it is unmeasurable. `DATA-LIFECYCLE.md` step 6. |
| 14 | **`routes_*` is captured weekly and reaches no prediction.** The WIRING is undecided. The capture is not in question. | **D** | OPEN | Prereg a routes feature and measure it. **The weekly job keeps running regardless** — 2026 cannot be re-fetched next year. If the measurement is null, attach a re-test trigger and keep capturing. |
| 13b | **`snap_counts` — same shape, same answer.** Feeds `projections.py`, ungraded. | **D** | OPEN | Grade the contribution. A null grades the *wiring*, never the store. |
| 15 | **`advanced_stats_*` (air yards / EPA / CPOE) and `historical_props_*`** — studies ran, wiring was never decided either way. | **D** | OPEN | Record a decision with a measurement behind it, the way pace and Vegas were closed — **plus a re-test trigger**, which neither of those has. |
| 16 | **TWO unreconciled copies of the same Vegas lines.** `vegas_lines_2021_2026.json` (nflverse *schedules*, 6 seasons, 1,426 games, closing) and the `spread_line`/`total_line` columns inside the pbp pull (3 seasons, 854 games incl. post). Nobody has checked they agree. | **D** | OPEN | Join on (season, week, home, away) and diff. **This check CAN fail** — unlike constant-within-game, which is true by construction. A disagreement means one copy feeding something is wrong. |
| 21 | 🔴 **THE BOARD DOES NOT BLEND ANYTHING. `proj_mean` IS SLEEPER, SCALED.** `proj_baseline == proj_sleeper` for **422 of 422** players with both — exactly, no exceptions — and `build.py:1003` declares the formula itself: `"sleeper_baseline * (1 + opportunity_adj)"`. **`proj_fantasypros` (422 players) and `proj_ownmodel` (416) are carried on the board and DISPLAYED, but neither enters `proj_mean`.** So "Sleeper vs FantasyPros vs blend" was never decided — we defaulted to Sleeper. FP is the only source with measured skill (12/12 cells beating naive baselines, bias sign transferring out-of-fold) and it is the one not used. | **A** | 🔴 OPEN | **This is Cory's question and it is a ruling, not a measurement.** Decide the source policy. Note row 20: the blend study refused (`no_control`) for want of Sleeper history, so "we measured and kept Sleeper" is NOT what happened — nothing was measured against Sleeper at all. |
| 21b | **Cory's 08-16 blend ruling was never executed, and the board does not say so.** Ruling: *"not replace Sleeper with own_v6 … but blend."* The blend study returned `no_control` and wired nothing; `proj_mean` still reads Sleeper-only. The refusal is recorded inside `proj_mean_blend.json`, but every surface shows a `proj_mean` that looks like a consensus. | relay | OPEN | Surface the refusal where the number is shown, or the board implies a blend that does not exist. Routed to A and B. |
| 22 | **We capture ONE SCALAR from FantasyPros — total projected points.** `proj_series.json` stores `{player_id: points}` and nothing else, for both sources. FP publishes far more: per-stat projections (which our HISTORICAL grader already parses — `exp_fp_hist_proj` runs `grading_mode: statline`, coverage 1.0) plus **best/worst-case ranges and expert-consensus dispersion**. | **D** | 🔴 OPEN | **The range fields are a PER-PLAYER upside signal, and this project just spent a week discovering it had none** — `ceiling` was `proj_mean × constant` for every player, and 73 still fall back to `gaussian_z`. Walk FP through the eight questions; step 2 evidence is below. |
| 22b | **Sandbox egress to `api.fantasypros.com` is BLOCKED** — measured today, `CONNECT tunnel failed, response 403` via the agent proxy. Not a dead end: `fp-projections-probe.yml` reaches it from GitHub Actions, which is how every FP artifact we hold was fetched. | relay | **ANSWERED** | Recorded per Cory's rule — the response code from an attempt made today, not "we can't get it." Any FP field expansion runs as a workflow, not in-session. |
| 19 | 🔴 **THE FP-vs-FFC ADP COMPARISON NEVER HAPPENED — IT COMPARED FANTASYPROS TO FANTASYPROS.** `exp_fp_board_coverage.py` builds a map it *names* `ffc_rank` from the board's `raw_adp`, then intersects it with freshly-fetched FP. But at that probe's own commit the board carried **334 players `adp_source: fantasypros` and 4 `ffc`** — `raw_adp` was already FP. Hence ρ = **exactly 1.0000**, and `coverage_overall` 0.191 ≈ the FP-sourced count, not a real overlap. Same defect class as `ceiling`: a field correlated against a copy of itself, reported as a finding. **The go/no-go that wired FP as the board's primary ADP anchor rests on this number.** | **A** | 🔴 OPEN | Rename the variable to what it holds, re-run against a genuine FFC pull, and re-decide the anchor on a real comparison. |
| 19b | **That same verdict cannot fail.** The f-string emits `"ρ={rho} vs FFC means the swap MOVES picks (not cosmetic)"` for ANY ρ — and the reading is backwards regardless: ρ=1.0 is *identical ordering*, i.e. maximally cosmetic. | **A** | OPEN | Four-branch `verdict_for(rho)` like `stack_sweep.verdict_for`, plus the known-positive control. |
| 18b | **The Vegas oracle's optimum sits at the EDGE of a two-point grid.** `DAMPENING = (1.0, 0.5)`, and λ=0.5 wins (+0.23 pooled vs +0.13 at λ=1.0). 0.5 is the grid **minimum**, so the true optimum is unmeasured below it and the "+0.23 ceiling" is a floor on the ceiling. Worse: **an ORACLE that improves when shrunk toward no-op indicates the multiplicative form is mis-specified**, not that game environment is weak — perfect information should not need damping. | **D** | OPEN | Extend the grid below 0.5 (0.35/0.25/0.15) to a real interior optimum. nflverse egress verified reachable today (HEAD 200), so the re-run is cheap. Same edge-of-grid defect fixed in `exp_ceiling_replicate.py` on 08-17. |
| 20 | **"Sleeper vs FantasyPros vs blend" is UNANSWERABLE, because nobody captured Sleeper.** `proj_mean_blend.json` gate = `no_control`: Sleeper has no per-player history for 2023/24/25, so the control arm does not exist. The prereg correctly refused rather than substituting a different source. **This is the retention rule's canonical case — un-backfillable, and it cost us the answer to a question Cory is asking during draft week.** | relay | **MITIGATED, dated** | `proj_series.json` now snapshots BOTH sources (fantasypros + sleeper, 15 entries since 08-09, append-only, deduped). First real answer: **Jan 2027.** Nothing to fix; recorded so the cost is visible. |
| 18 | **REOPENED: the Vegas +0.23 null is implausible on its face and was never treated as a suspected bug.** It was a PERFECT-FORESIGHT test — hand the model the true game environment and weekly MAE improves by a quarter point. Knowing every game's real total in advance should be worth far more than that. A near-zero oracle result usually means the oracle never reached the model, not that the input is worthless. Cory, 08-17: *"Do we really think it makes sense vegas odds didnt move a single thing?"* | **D** | OPEN | Before re-running anything: answer Rule 3d's three questions on the ORIGINAL run — did the line input vary (distinct values), did it survive the join to the player-week rows (count), and could the test have fired (control). **The +0.23 is quoted in `vegas_lines_2021_2026.json`'s `_note` as settled context, so it is propagating.** |
| 17 | **Every recorded null lacks a re-test trigger.** Pace (published null) and Vegas (+0.23 perfect-foresight ceiling) are filed as closed; both were measured at one point in time on limited seasons. | **D** | OPEN | Attach a trigger to each — season count, event, method, or date. Per `DATA-LIFECYCLE.md`: a null with no trigger is abandoned, not answered. |
| 12 | **`config_confirmed` and every local ruling were being wiped on each build.** `build.py --league-id` rebuilt the config from Sleeper and kept only two keys. | relay | **CLOSED 08-17** | `preserve_local_rulings()` + 6 tests. Verified: the shipped config still carries the ruling. |

## 🟢 CHECKED AND CLEAN — recorded so nobody re-investigates

| what | finding |
|---|---|
| **Do the model-scoreboard actuals inherit the weak `import_weekly_data` coverage?** (611 of 1,708 on-field players classified) | **No.** `fetch_component_stats.py` pulls the nflverse `player_stats` release files directly by URL and never calls `import_weekly_data`. The exclusions resolve to row 8, not a new defect. Checked 2026-08-17. |
| **Are per-model exclusion counts (115 vs 211) an unfair comparison?** | **No.** `model_accuracy_backtest.py` already computes `head_to_head_shared_population` over the intersection. The ranking that decides which model ships is on a matched denominator. |
| **Do other interval labellers carry the "CI includes $0" bug?** | Swept at artifact level. Three instances found and fixed (`frontier`, `cory_conditional`, `stack_sweep`); 42 co-located pairs across 6 artifacts now consistent. Scope limit stated in the test. |
| **Are the snap-count stores partial?** | No — join 0.971–0.992. But the refusal floor was 0.70, 27 points below anything observed, and is now 0.95. |

## 🔴 E'S SWEEP — filed 2026-08-17, every row with an owner and a next action

**Session E swept the published board (682 rows, built 08-16T14:10Z) eight times
on 2026-08-17 and filed eleven findings. They were routed in `ROUTES.md` and had
no register rows until now — which is exactly the gap Cory named: a lane label
in an inbox is not ownership.** Working files are under `draft/audit/`.

| # | what | owner | status | next action |
|---|---|---|---|---|
| E1 | 🔴 **Nine top-50 players read the wrong dispersion cell** — band assignment uses a rank that disagrees with the band the calibration was fitted on. Worth $47.6 of spread between St. Brown and Jefferson. | **A** | 🔴 **AWAITING A'S APPROVAL — fix prepared, deliberately NOT applied** | **THE FIX IS WRITTEN AND SENT FOR APPROVAL — `E1_proposed_fix_for_approval_2026-08-17.md`.** Two edits: give `blend()` an optional `rank_pool_ids` (inert until passed), and hoist the keeper map above `build.py:721` so the cell is read against the ordering the board actually publishes. **NOT APPLIED because (b) moves `_assess_keeper_slate`, a NETWORK read, earlier in the build pipeline five days before the draft — a gatekeeper call, not a red-team one.** No one-liner exists: `kept_ids` is not known until `build.py:1646`, and using `cfg`'s raw keepers instead fails because the slate is partial (4 of 10 designated), so that set and the published-withheld set are different populations. **Blast radius: dispersion fields only — order does not move.** **Re-check after keeper lock 08-20 — and the magnitude is now computed, not guessed: 46 players change calibration cell when the other teams' keepers leave the pool, 37 of them inside the top 150**, with James Cook, CeeDee Lamb, Ashton Jeanty and A.J. Brown all moving into the `1-3` band. Since sweep 7 established `proj_ceiling` is `proj_mean × a per-cell constant`, **cell membership IS the dispersion figure** — so every one of those 46 gets a materially different floor and ceiling on 08-20. **E1 does not fix itself and does not worsen; it changes IDENTITY** — the mismatch is governed by Cory's three keepers, who stay in `kept_players`, so it stays ~3 slots wide but lands on a different nine. **Fix the mechanism, not the named rows — now DEMONSTRATED, not argued: re-running E1's own comparison on the post-lock pool gives 11 misreads against today's 9, and ONLY Chase Brown is on both lists.** Eight of today's nine drop out; ten new players appear, five inside the top 50 (Pickens 17, Waddle 28, Kyren Williams 32, Etienne 37, McLaurin 47). So the defect gets slightly wider, not identical — **my own earlier "stays ~3 slots wide" was directionally right and numerically wrong, corrected in the audit.** Directional only: uses `predicted_keepers`, which is MOCK-ONLY by its own header, and 4 of 10 teams have designated against 14 predicted. `keeper_lock_shifts_the_cells_2026-08-17.md` `band_edge_misread_2026-08-17.md` **MAGNITUDE EXTENDED 2026-08-17 (sweep 15): E1 hits the FLOOR as well as the ceiling, and proportionally harder.** Band-to-band p10 spans are 78.5× at QB and 37.2× at RB against p90 spans of only 1.4×/1.3× — so which cell a player lands in decides his floor far more than his ceiling. **The absolute-points conclusion runs the other way, stated rather than the flattering half: summed over the nine, 373.5 pts of floor error against 535.1 pts of ceiling error**, because ceilings are ~3× larger numbers. **E1 is now VISIBLE IN THE WAR ROOM** rather than only in this file — `showWhy` tells the reader when a player's spread was priced off a different cohort than his rank (E16), and the browser-side detector recovers exactly these nine using board data alone. `floor_is_a_cohort_not_a_forecast_2026-08-17.md` |
| E2 | 🔴 **`proj_ceiling` is STILL rank-identical to `proj_mean` inside every cell** — Spearman exactly 1.000000, 16/16; within-cell cv 6.3e-04 worst. The §1 defect signature, unchanged. `constant_multiple_sweep` flags it on today's board with its known-positive control firing. **Cory 08-17: "FIX!!!"** | **A** | 🔴 OPEN — Cory has called for the fix | The construction is already written: `VOLATILITY-WIRING-PREREG.md` §2, on `main`, with `weekly_volatility.json` committed. Prereg says run after 08-22; **Cory has now asked for it sooner, so the date is his to move, not the prereg's.** Blast radius is bounded: `MEASURED_WEIGHTS.ceiling = 0`, so it moves the **bench branch** (`engine.js:1166`, ranks on `proj_ceiling − proj_mean`) and **`playerDollars`** (row 8a), NOT the composite board ranking. `ceiling_is_still_a_cell_constant_2026-08-17.md` **BLAST RADIUS NOW MEASURED — `ceiling_fix_blast_radius_2026-08-17.md`:** the change introduces **~1.4x-2.0x within-cell spread** where there is now exactly 1.0x. **One thing to re-derive FIRST:** the prereg's coverage counts (131/157, 26 without, 8 rookies) **do not reproduce** on the published board — closest is 94 of 101 skill-only/2025-only — though its four named players (Evans, G.Wilson, Nabers, Daniels, +Reed unnamed) do miss 2025 exactly. **All four have a cv in some OTHER season, so `2025-only` vs `any-season` must be decided before wiring**: under any-season the absent rule stops protecting the injury-return group it was written for. |
| E3 | **The composite's only per-player term saturates at its cap across the top 50** — 30 players at exactly `opportunity_adj 0.15`, 22 of them top-50, underlying `opportunity_z` spanning 2.00–3.77 flattened to one number. `proj_mean = proj_sleeper × 1.1500` exactly for 21 of the top 24. | **A** | OPEN | Rule on whether the cap should bind where it binds. Invisible to `constant_multiple_sweep` (cells of 2–8 rows against `MIN_CELL = 12`). `opportunity_cap_saturation_2026-08-17.md` |
| E4 | **The opportunity adjustment is not mean-preserving and excludes QB/K/DEF entirely** — pool level shift RB +7.60%, WR +7.04%, TE +6.81%, QB **+0.00%**; one distinct value (0.0) across all 88 QBs. Since the board ranks across positions on vorp, it is a positional thumb on the scale: Josh Allen ovr 16 → ~10 under symmetric treatment. | **A** | OPEN | Decide whether an adjustment that is also a +7% level shift may apply asymmetrically across positions that are then ranked against each other. `VOLATILITY-WIRING-PREREG` already fixes mean-preservation for the next feature. |
| E5 | **That same term is a veteran bonus** — median `opportunity_adj` +0.0783 / +0.1166 / +0.1454 / +0.1377 by experience band; 0 of 13 young top-150 players reach the cap against 26 of 66 established. Two top-150 rookies with `wopr: null` get exactly 0.0000. **Supplies the mechanism for the rookie gap the 08-16 drafter study measured at +25.1 = 38% of Cory's pooled gap.** | **A** | OPEN | Consider together with the already-queued rookie-prior decision in `DECISIONS-NEEDED.md`, not as a separate item. `opportunity_is_a_veteran_bonus_2026-08-17.md` |
| E6 | ✅ **FIXED 2026-08-17 (Cory: "act on, fix and continue").** ~~The board's "single-source" caveat marked the wrong 255 players.~~ `app.js:642` gates it on `proj_fantasypros` being ABSENT, so the 427 rows where FP exists and is ignored render with NO caveat — and its absence asserts a second opinion that does not exist. 96 of the top 150 are unmarked; all 682 are Sleeper-only. | **B** (fixed by E on Cory's instruction — **B please review**) | ✅ **FIXED — verify** | **DONE:** `app.js:662` now marks EVERY row and splits the per-player fact that actually varies — **¹ = a second source EXISTS and is UNUSED (127 rendered), ² = no second source exists (61 rendered)**. **Verified three ways:** the live screen reports **0 of 188 rows unmarked, 127 ¹ with 0 wrong, 61 ² with 0 wrong** (was 127 unmarked, all single-source); `proj_source_authority.test.js` gains two pins with a **proven fail arm** (restoring the old function fails them 2/2); and a behavioural rehearsal `rehearsal-board-truth.js` is wired into `rehearsals.yml`. JS suite **313/313 files pass**; Python shows the same 9 pre-existing failures as a clean tree at HEAD. **B: this is a LABEL change — no number moved.** The original ask was: What the mark could usefully say is the thing that IS per-player: whether a second source exists and was not used. Register 21b with the mechanism located. `proj_source_mark_inverted_2026-08-17.md` **NOW OBSERVED ON THE LIVE SCREEN, not just read in the code** (`warroom_truth_check_2026-08-17.md`): driving the real war room, **127 of 127 unmarked rows are provably Sleeper-only** (`proj_baseline == proj_sleeper`) and **65 marked rows all genuinely lack FP** — the mark is internally consistent with its own condition and the condition is the wrong one. **Everything else on that screen is truthful**: 188 players checked, `overall_rank` and `proj_mean` both **0 discrepancies** against the artifact. A ~40-line DOM-vs-artifact assertion would pin this against regression; recorded in the audit, not shipped, because adopting it is B's or A's call. |
| E7 | **`STREAMABLE_LATE` is computed, tested, and read by nothing.** `engine.js:2167` defines it; `engine.test.js:475` pins it; `applyFormatDefaults` copies only `BENCH_DISCOUNT`. Observable consequence at **TE** (+12.3 top-150, +16.7 in the 70–150 band, six board-highest players in that band all TEs) and **absent at QB**. | **A** | OPEN | Wire it or delete it — either is defensible. What must not persist is a league-shape judgement that runs every build, is pinned by a test, and reaches no decision. If deleted, delete the test with it. `streamable_late_is_never_read_2026-08-17.md` |
| E8 | **Live PUP/IR status reaches no availability number.** Kittle (PUP) carries his position's healthy `games_expected`; `bench_mv.js` turns that into a constant `injuryWeeks` — every RB simulated as missing 1.8 weeks, healthy or not. Kittle is ranked ~15 picks ahead of market while on PUP. | **A** | OPEN | Low urgency — `risk` and `bye` are both weighted 0.0, so it reaches no composite recommendation. `bench_mv.js`'s ledger already states the "no history" half; extend it to "no current status". `sweep3_flags_that_died_2026-08-17.md` |
| E9 | **Source-ruling evidence for A2 / row 21** — the choice changes the ORDER, not just the level: top-150 FP/Sleeper cv 0.072, within-position Spearman 0.890–0.935, **25 of 39 top-150 WRs move 3+ spots**. On the QB board FP and the market agree against Sleeper on Daniels and Hurts. | **A** | OPEN | Input to the source ruling A2, not a recommendation — row 20 says the measurement that would settle it does not exist. |
| E10 | **Register row 2 has a named candidate without a fresh board** — `adjusted_adp = c × pool_rank` in **10/10 cells** (cv 0.012430), in no dispersion relationship, so a genuinely separate instance. | **A** | OPEN | Check `adjusted_adp` first when the fresh 693-row board is built. Narrows row 2; does not close it. |
| E15 | ✅ **FIXED 2026-08-17 — the QB over-recommendation Cory hit live had TWO causes and both are repaired.** (1) **The ceiling tiebreak decided on a calibration constant**: at pick 88 with Burrow rostered the engine promoted Bo Nix (proj 335.72) over Brock Purdy (proj 350.2) and labelled it *"on upside"* — Nix's ceiling is bigger only because `QB|9-16` carries a 1.426 multiplier against `QB|4-8`'s 1.316. `moreUpsideThanTheCellExplains()` now refuses a swap justified only by the two players' cells; **score inversions across the list went 16 → 0**. (2) **The seat plan asserted a QB starter seat at pick 73 and was roster-blind** — solved once pre-draft from keepers alone, never re-solved, with a `fallback_rule` steering to *"the best remaining player ELIGIBLE FOR QB"*. `renderSeatPlan` now asks the engine's own `mandatoryGaps()` and marks the seat **SEAT ALREADY FILLED** when the slot is held. | **A** (engine) / **B** (surface) — fixed by E on Cory's instruction, **please review** | ✅ **FIXED — verify** | Both guards self-release: the tiebreak starts firing again the day `proj_ceiling` carries per-player information (VOLATILITY-WIRING-PREREG §2), and the seat notice tracks the live roster. **Verified:** JS **315/315 files**, Python 3312 passed (same 9 pre-existing failures as a clean tree), war-room rehearsal **19/19**, board-truth **6/6**, plus two new test files with controls. Two existing controls in `engine_ablation` and `rec_rows` legitimately inverted and were **re-aimed, not deleted** — both keep their ability to fail. `ceiling_is_still_a_cell_constant_2026-08-17.md` |
| E13 | ✅ **FIXED 2026-08-17.** ~~`rookie_affinity` is 0.0 for all ten managers and could not have been anything else.~~ `manager_profiles.league_average.rookie_rate = 0.0` across ~450 picks in 3 drafts. Cause: `managers.py:122` reads `years_exp` from **today's** Sleeper payload while `position` and `team` in the same dict prefer contemporaneous `meta` — so a 2023 rookie has `years_exp` 3 today and is never counted, and only a 2026 rookie could qualify. **The `"chases rookies"` clause (`rate > league_rate*1.5 and rate > 0.08`, both 0.0) is unsatisfiable and fires 0 of 10 times.** The module header names rookie affinity as one of three metrics that *carry* the profile. **Contradicts the 08-16 drafter study, where the rookie prior cleared its bar at +25.1.** | **A** — fixed by E, review | ✅ **FIXED — verify** | **DONE, and it needed no new data.** `years_exp` counts seasons since debut, so a rookie at that draft satisfies `years_exp == season_now - draft_season`. **Both terms were already present**: the row has always carried `season`, and **`season_now` had been a `build_profiles` parameter since it was written — declared, never read, never passed.** `build.py` now supplies it. **Measured on the real historical picks: 409 of 438 derivable (93% coverage), 45 rookies, rate 0.110, spread 13/15/17 across 2023/24/25 — was pinned at 0.000.** An underivable row is now **ABSENT rather than False** (the `"?"` treatment `position` already gets), with `rookie_rate_coverage` and per-manager `derivable_picks` shipped beside the rate so a low rate cannot be confused with a thin one. **`"chases rookies"` is satisfiable again** — at a league rate of 0.110 a manager needs ~0.17. **10 new tests with a proven fail arm** (restoring the old predicate fails 8 of 10, including the known-positive control). Python **3322 passed**, same 9 pre-existing failures as a clean tree. ~~Original ask:~~ (`league_history.json` carries the season on every row), or emit ABSENT with coverage reported — the treatment `position` already gets via `"?"` — rather than 0.0, which reads as a measured *"nobody drafts rookies"*. **Not draft-critical**: feeds opponent summaries, not the board ranking; the softmax reaching survival is driven by `bpa_rate`. Post-08-22 unless A disagrees. `rookie_affinity_cannot_vary_2026-08-17.md` **SCOPE LIMIT CLOSED:** the other profile metrics are NOT degenerate — `homer_index.rate` 7 distinct, `homer_index.team` 9 distinct, `bpa_rate` all 10 distinct, positional consistency on 10/10, and summaries discriminate in plain language. **Three legs carry weight and one is sawn off**, so the fix is to one metric, not the module. |
| E19 | ✅ **FIXED 2026-08-17.** **Three suites graded a board no surface renders.** `app.js:52` initialises from `MEASURED_WEIGHTS` (five of eight terms ZERO); 8 of the 28 engine-driving suites never reference it. **`ui_fidelity_verdict.test.js` §2, titled "REAL ENGINE OUTPUT ON THE SHIPPED BOARD", scored `DEFAULT_WEIGHTS`: the verdict WORD differs at 4 of 12 of Cory's picks and the backed PICK at 8 of 12 — at pick 33, his first, it validated LOCK Zay Flowers g=14.3 while the app shows LEAN Colston Loveland g=2.9, the SAME pair `rec_rows.test.js` records for this defect.** Its assertions were never wrong (they are self-referential); the AIM was — and it matters because the verdict word is a function of the GAP, which the production vector compresses: the arm produced LOCK at 10 of 12 and **never once a LEAN**. **`sanity-sweep.test.js` — the file built to catch "QB2 in round 9 with a starter rostered", verbatim Cory's Bo Nix complaint — had never scored the vector that produced it**, and it already documents TWO earlier fixture defects of the same class (roster quality, board depletion). **Correcting it is a NEGATIVE result, recorded as one: all four enforced checks pass under both vectors; only the reported open finding moves, 47 → 50 bye-stacks, coherent since `bye` is weighted 0.0.** `taken_ids_replay.test.js` carried the latent `MEASURED || DEFAULT` shape. **All three now REFUSE rather than fall back**, per `rec_rows`: a suite that cannot find the production weights must stop, not guess. **Checked and CORRECT, not everything is a victim:** `engine.test.js` parameterises weights for mechanism pins, `composite_roster_blindness` falls back TO measured, and `live_context.test.js` actively ASSERTS the measured default. **PART 1 flag died:** the only live production fallback is `age || peak` (61 rows) and it reaches nothing draftable — 32 DEF, 2 K, and 27 skill players of whom 25 project 0.0 with ADP past 690. `suites_grading_weights_nobody_runs_2026-08-17.md` | **A** — fixed by E, review | ✅ **FIXED — one judgement for A** | Whether `roster: []`/`currentKeepers: []` should also be corrected across the nine suites passing them. I did NOT touch it — `rec_rows` names it as having no obvious right value — but it is the last fixture dimension between these suites and production. |
| E18 | ✅ **FIXED 2026-08-17 (fix + document moved together, on Cory's override).** **The keeper bar RANKED rows it cannot value, so the screen asserted a candidate beats a player it has no projection for.** Sibling of E17: that fixed the seam, this the reader. `nextYearVorp` reads `(player.vorp || 0)`, so any roster entry without `vorp` scored as ZERO not unknown; at `ranked[slots-1]` it dragged the bar negative and `max(0, raw−bar)` ADDED to every candidate. Two live paths create such rows **by design and correctly** — `recordManualPick` and the Sleeper off-board stub (**3.3% expected, 14% upper bound**). Measured at pick 33 on a 2-keeper + 1-stub roster: **three KEEPER TARGET badges reading "beats &lt;stub&gt; by 12 pts"**, the stub wearing its real Sleeper name. **The badge is not the defect** — a VALUED weak third candidate correctly gives "beats Jordan Mason by 13". **Inert for a 3-keeper slate (bar identical at all 12 picks); binds below `slots` valued entries (2 keepers + 1 stub → −7.71 vs +2.54).** Fixed as a **contract violation**: `keeperOptionValue`'s own docstring already said *"with fewer incumbents than slots there is a free slot, so the bar is zero"* — a row we cannot value is not an incumbent. ⚠️ **THIS MOVED A PUBLISHED CLAIM IN `WAR-ROOM-SURFACE-CONTRACT.md` (`TERRITORY: A`) AND THE EDIT IS STAMPED AS AN OVERRIDE FOR A'S REVIEW.** The keeper term's 14.3% share **was almost entirely this defect** — valued correctly it is **0.2%** and is now the SMALLEST term (value 63.1 / onesie 25.2 / stack 11.6 / keeper 0.2). I first held the fix and routed `NO DEFAULT — BLOCKED`; Cory overrode (*"Fix and continue"*). **The order is now MORE stable, not less** — measured across 3/2/1/0-keeper rosters it never moves, so the document's old *"middle ranks are board-dependent"* caveat is retired and the full order is pinned again. VONA's share updated 59%→63% in two other places. **E17 and E18 are independent and neither subsumes the other:** E18 alone would have said *"3 keeper slots still open"* while Cory holds three — pinned as a check. **Also fixed: `surface_contract.test.js` built its roster from raw `kept_players`** and was green only because it graded a keeper term the app stopped producing when E17 landed — the stale-fixture class `rec_rows.test.js` records. `draft/tests/keeper_bar_ignores_what_it_cannot_value.test.js` (13 checks). `keeper_bar_ranks_what_it_cannot_value_2026-08-17.md` | **A** — fixed by E, review | ✅ **FIXED — DOCUMENT OVERRIDE NEEDS A'S REVIEW** | A: if you would have re-derived §1's table differently, change it. **If Cory locks FEWER THAN THREE keepers on 08-20, E re-checks that day.** |
| E17 | ✅ **FIXED 2026-08-17.** 🔴 **The war room told Cory that Zay Flowers beats Ja'Marr Chase for his last keeper slot.** `kept_players` is a different population from `players` and lacks `vorp`; `populateKeepers` copied the row verbatim into `state.myRoster`, so `ctx.currentKeepers` held three players with `vorp === undefined`, and `composite.js:nextYearVorp` reads `(player.vorp || 0)` — **absent became a confident ZERO**, the same shape as the `|| echo` and `|| undefined` this repo has already removed twice. The keeper term is `max(0, raw − bar)` where the bar is the weakest incumbent, so a zeroed bar went **NEGATIVE (−31.86 in round 1, −11.42 in rounds 5-6) and ADDED to every candidate**. Ramp is 1.0 through round 6, i.e. live where Cory picks. **4 KEEPER TARGET badges fire across his 12 picks, each naming a keeper the candidate does not beat; with the keepers valued, 0 do.** **ORDERING IS UNAFFECTED, MEASURED NOT ASSUMED: 0 of 120 name slots move** — the bar is constant across candidates, so it shifts every keeper term equally and can only reorder through the clamp. **So this corrected a FALSE ON-SCREEN CLAIM, not a ranking.** (My first measurement used an empty `taken` set and said '1 of 12'; that was not a real draft state, corrected before filing.) **Fix derives `vorp` from the artifact's OWN formula** — `vorp == round(proj_mean − replacement_points[pos], 2)` holds 682/682 — and **absent stays absent**: unknown position or no projection leaves it undefined rather than reaching for a fallback. Two flags died: keeper dispersion cells are CORRECT (Chase→WR|1-3, Henry→RB|4-8, Walker→RB|9-16), and `pool_rank` is written by `keepers.js` which deliberately excludes keepers. **`rec_rows.test.js` passes `roster: []`/`currentKeepers: []` and says so in its own header — no suite had ever exercised the keeper path with real keepers.** `draft/tests/keeper_seeded_with_a_value.test.js` (17 checks, 2 known-positives, fail arm — which was wrong on first run and the test caught it). `keeper_seeded_without_a_value_2026-08-17.md` | **A** — fixed by E, review | ✅ **FIXED — ARTIFACT QUESTION OPEN FOR A** | Rule whether `build.py` should emit `vorp` on `kept_players` directly; the UI derivation no-ops if it does. |
| E16 | ✅ **FIXED 2026-08-17 (label only).** **The war room printed cohort statistics as player forecasts.** `proj_floor`/`proj_ceiling` are `proj_mean × the measured p10/p90 ratio of the player's (position, rank-band) CELL` (`projections.py:423-437`), so every player in a cell shares one multiple. Applying a STEP over bands to a CONTINUOUS rank puts a cliff at every edge: **QB16 Jaxson Dart floor 87.29 vs QB17 Jordan Love floor 2.45 — 35.6× across a 6.0-point projection gap, both in their CORRECT cell** (not the E1 misread). Also WR31 68.43 vs WR32 8.23, TE32 11.61 vs TE33 2.11. `showWhy` said only "(floor 2, ceiling 479)". **SHIPPED: the caveat names the cohort, and NO NUMBER CHANGED — nothing reorders, no scoring path touched.** **My first version of the fix was itself a false label and its own test caught it:** it named the cohort from `pos_rank`, which is wrong for E1's nine; it now recovers the cohort from the ratio actually carried, so **E1 is visible at the point of use** ('priced off a DIFFERENT cohort than his rank'). The browser detector independently finds exactly those nine with no calibration file. A second defect fell out: the cohort cache survived a re-sync replacing `state.board` — now keyed on the board reference. **THE UNDERLYING QUESTION IS OPEN AND IS A'S** — step vs interpolate between band centres. I lean interpolate (nothing in football makes QB17 discontinuous with QB16) but it is a modelling decision, not a red-team one. **DEFAULT: nothing before 08-22** — this moves the SPREAD, not the ranking, and the ceiling weight is 0.0, so no recommendation moves today. `draft/tests/floor_is_a_cohort_not_a_forecast.test.js` (12 checks: known-positive on the live board, both branches exercised, fail arm). `floor_is_a_cohort_not_a_forecast_2026-08-17.md` | **A** — fixed by E, review | ⚠️ **LABEL FIXED — MODEL QUESTION OPEN FOR A** | Post-draft: decide step vs interpolate. The label fix stands either way. |
| E14 | **An unverified assumption sits under three manager metrics.** `managers.py` reads `meta.get("position") or info.get("position")` — contemporaneous first, today as fallback — and **nothing confirms `meta` is ever populated.** The only inspectable copy of historical picks, `league_history.json`, has `metadata` **empty on 480 of 480**. If that also holds on the live path, `homer_index` measures *which teams a manager's old picks play for NOW*, and the `"loves <TEAM>"` line in every opponent summary is wrong. **NOT filed as a defect:** the production path reads live Sleeper (`build.py:1493` → `si.all_drafts` → `fetch_draft_picks`), which returns the raw payload and probably does carry metadata — **and Sleeper is HTTP 000 from this sandbox, measured 2026-08-17**, so this is unverified rather than refuted. | **A** | OPEN | **One line for anyone with egress:** `picks = si.fetch_draft_picks("<completed draft_id>"); count how many carry `metadata.team`.` `0 of N` means homer_index is wrong; `N of N` closes the row and leaves only E13. `rookie_affinity_cannot_vary_2026-08-17.md`, appendix. |
| E12 | **The draft-day runbook's one irreversible step rests on a false premise.** `DRAFT-WEEK-BRIEF.md` §4 says the freeze *"is from 08-14 and is missing fourteen declared fields"*; it was re-taken in `60f3487` and its payload sha is **byte-identical** to a freeze generated from today's board (`98f580261a2ea898…`), 0 of 44 declared fields missing, `test_freeze_not_stale.py` **3/3 green** including its `repo_parity` node. The brief's *"two deliberate red flags"* is one. | **relay** | OPEN | **The ACTION stays — re-take after the final board build, because the board rebuilds nightly.** Replace the stale justification with the durable one, and correct the closing paragraph. A `rm` justified by a premise the reader can falsify is a step someone skips. `freeze_is_not_stale_2026-08-17.md` |
| E11 | ✅ **Register row 3 does NOT reproduce on the published board** — 0 adjacent pairs where a lower-ranked player carries higher vorp; `overall_rank` is a clean 1..682. | **A** | **ANSWERED for the published board** | Row 3 is specific to the fresh 693-row board and is **not in front of Cory this week**. Does not close the row; takes it off the draft-week critical path. |

**Two process notes E owes against its own work.** Three arithmetic or framing
errors were made and corrected the same day: the keeper `cost_round` label read
as a price (retracted in `keeper_surplus_read_2026-08-17.md`), an asymmetric
stress test in the Henry-vs-Olave comparison, and a distinct-ratio count used
where a cv was needed — the last being failure mode (1) in
`constant_multiple_sweep`'s own header. **A lane that audits others must show its
own misses**, and all three are in the audit files rather than quietly fixed.

**And the register itself was the ownership gap.** `DEFECT-REGISTER.md` is **not
on `main`** — it exists only on `claude/fantasy-football-research-926y6z`. The
file whose entire purpose is that nothing gets left behind, and whose test fails
on any row without an owner, is itself behind an unmerged branch. **That is the
mechanical cause of "no one owns anything", and only A can fix it by merging.**

## 🔵 VERIFICATION OWED — things believed true but not observed

| what | owner | next action |
|---|---|---|
| `npx playwright install --with-deps chromium` on a real runner — the browser rehearsals' one untested line. | relay | Observed on the first scheduled `rehearsals.yml` run after it reaches `main`. |
| The board's structural properties on a *published* board (health 100%, replacement-sensitivity) — currently only seen on a refused candidate. | relay | Confirm on the first successful publish. |

---

**Rule for adding a row:** anything that could change a number Cory drafts or
starts on. When in doubt, add it — a row costs a line, a missed defect costs a
season.

# DEFECT REGISTER — every open data/logic concern, with an owner

**Cory, 2026-08-17:** *"it's also your job to make sure nothing gets left behind
or not chased down, especially potential data or logic errors or anything that
messes with our models draft or inseason tools."*

Every row has an **owner** and a **next action**. A row with neither is a defect
in this file. `draft/tests/test_defect_register.py` fails if one appears.

**Status words mean one thing each:** `OPEN` = nobody has looked since it was
filed · `IN HAND` = someone is on it now · `WAITING` = blocked on a named person
or date · `CLOSED` = fixed and verified, kept for the record.

---

## 🔴 BLOCKING — draft-critical, this week

| # | what | owner | status | next action |
|---|---|---|---|---|
| 1 | **Board has not published since 08-15.** Gate refuses the fresh board. | **A** | IN HAND | Build fresh board; confirm rows 2 and 3 below before regenerating anything. |
| 2 | **A new field joined the constant-multiple family** on the fresh board. This is the ceiling-defect class by name — a field that is a rescaled copy of another cannot be weighted independently. | **A** | OPEN | Run `test_constant_multiple_sweep` on a fresh board; NAME the field. Do not regenerate to green. |
| 3 | **Board's published ranks disagree with its own vorp ordering** (`test_A_ZERO_BONUS_REPRODUCES_THE_BOARDS_OWN_RANKS`). No artifact involved — internal inconsistency in the board Cory drafts from. | **A** | OPEN | Same fresh board; identify which players' ranks disagree. |
| 4 | **`main` CI red** on `matchup_placed_bet` (3) and `trashtalk` (3), since 08-16 23:01. A red main means the gate has confirmed nothing for anybody. | **B** | OPEN | Verify whether B's `/matchup` fix cleared it; if not, fix. |

## 🟠 CORRECTNESS — known wrong, deliberately held

| # | what | owner | status | next action |
|---|---|---|---|---|
| 5 | **`MEASURED_WEIGHTS.ceiling = 0` is contradicted by measurement.** Three preregistered runs, two independent seed sets, every value 0.15–0.65 beats the shipped zero, 3/3 separable. | **Cory** | WAITING (after 08-22) | Cory rules. Framing: the model ignores upside entirely; the exact amount hardly matters. |
| 6 | **ADP-sd ratchet fired** — shipped rule is 1.39× FFC's published dispersion in the 50–100 band. Our constant did not drift; the market tightened. Blast radius: 1 player. | **Cory** | WAITING | Recommendation: leave it, revisit post-season. |
| 7 | **`risk` weight is UNMEASURED** and the term is PARTIAL on backtest boards (age only, 6 of production's 11–13 distinct values). | **A** | OPEN | No date. Needs a prereg before any measurement, or an explicit "stays zero" ruling. |
| 8a | **The dollar model was not re-derived after the ceiling changed.** `playerDollars`' `boom = ceiling − mean` moved: boom's share of a player's total fell **47.2% → 34.9%** (median), every total moved **−18.8%** median, **464 of 603 players moved >10%**. Coefficients 0.22/0.08/0.05 were chosen at the old scale. | **A** | OPEN | Re-derive, post-08-22. Until then the figures are RELATIVE ONLY — which the code says and every surface must keep saying. `ceiling_change_downstream_2026-08-17.md` |
| 8b | **The board carries TWO ceiling constructions** — 530 `measured-2023-25-p90`, 73 `gaussian_z` — and `playerDollars` reads the field, not the `proj_ceiling_source` stamp, so it treats them identically. | **A** | OPEN | Decide whether the 73 should be excluded from dollar figures or the difference surfaced. |
| 8c | **`champodds`, the bench ceiling floor, and every `proj_floor` consumer** were calibrated against the pre-08-17 distribution and have not been re-checked. The floor has had LESS attention than the ceiling, not more. | **A** | OPEN | Same rule: anything reading proj_ceiling/floor/sd/weekly_sd is suspect until re-measured. Measuring is not shipping. |
| 8 | **own_v6 — the live model — has 22.7% of its forecasts excluded from its own accuracy score** (115 of 506; walk_forward 211 of 737). MAE is optimistic by an unmeasured amount. | **A** | WAITING (after 08-22) | Prereg written: `SURVIVORSHIP-BOUND-PREREG.md`. Headline is ranking stability, not a corrected MAE. |

## 🟡 DATA — stores and coverage

| # | what | owner | status | next action |
|---|---|---|---|---|
| 9 | **2025 cannot be graded** — lateral handling in the pbp rebuild. The 2-point gap is fixed (mean_abs 0.489 → 0.149); laterals are what still blocks. | **A** | OPEN | Unlocks a second grading fold. Post-draft. |
| 10 | **STALE ROW — BOTH STORES ALREADY EXIST (found 08-17).** `nflverse_weekly_points_2021.json` (5,401 player-weeks, 18 weeks, `complete: true`) and `_2022.json` (5,351, 18, `complete: true`) are committed. **So the first clause is false and the diagnosis with it.** The real constraint is the **scoring-fingerprint boundary**: 2021-22 carry `220bf4c671786351`, 2023-25 carry `bd8f3e50bd67a9ce` — different tables, so they cannot be POOLED. That is exactly why `weekly_volatility` refused those seasons. **C is assigned to build a store that exists**, the same "check what exists before fetching" trap the relay flagged to C on 08-17. | **C** | OPEN | **Do not build it — it is there.** Routed to C. What is genuinely open is narrower: a second graded fold needs a **re-score of 2021-22 under the current table**, or fold designs that stay inside one fingerprint — which `routes_tprr_study.py` demonstrates works and which gave it 3 folds instead of 1. |
| 11 | **Board-derived artifacts are not in the freshness registry** — 13 entries, none of them the ones that refuse publication. This is the whack-a-mole. | **A** | OPEN | Register only the genuinely derived ones. **Never** auto-regenerate a board assertion (rows 2, 3). |
| 13 | **CORRECTED 08-17 — `snap_counts` does NOT feed `projections.py`, and the premise of this row was wrong.** Measured: **0 snap-derived fields on the board** (56 distinct keys, 682 rows); `own_model_v6.py` 0 mentions; `build.py` 19 mentions, all `snapshot`; `projections.py` names it only at `:131-136` to say it is **not computed anywhere**. So it stops at lifecycle step **4**, not 6 — **there is no contribution to grade.** And the stop is not unexamined: `capture_registry.py:138-144` records it as a deliberate WIRING gap, dated, *"a new input wired live five days before the draft is a worse instrument than a known one."* The store is healthy — 35,705 stored player-weeks 2021-25, join rate **0.9919**, `pct` 0.000-1.000 over 101 distinct values. `draft/audit/snap_counts_row13_2026-08-17.md` | **D** | IN HAND | **This row and row 14 are the same shape**: a step-4 store awaiting a preregistered feature, post-08-22. Pinned by `draft/tests/test_data_lifecycle_predicts_column.py` (3 checks, one a known-positive control). **Parked to A:** two stale claims in `projections.py` (`:5` still lists snap share as an engine nudge input; `:135` still says the source was *"never pulled"* — false since 08-17, and the identical sentence was already scrubbed from `capture_registry.py` by a test). |
| 14 | **MEASURED 08-17 — season-grain TPRR is a real null, and the reason is collinearity.** Preregistered (`ROUTES-TPRR-PREREG.md`, committed first in `891c9e6`), graded (`routes_tprr_study.json`, `clears: false`). **E1 persistence is strong** (+0.671/+0.670/+0.653/+0.686 over four transitions, null p95 ≈ +0.13) **but is largely inherited**: targets carry at +0.673/+0.730/+0.689/+0.694, near-identical. **E2 increment fails the bar** — partial ρ (TPRR given targets) +0.128 / +0.171 / **+0.026**, and the first beats its null by 0.0009, i.e. noise. **Mechanism, measured: ρ(TPRR, targets) = +0.74…+0.82 every season, while ρ(TPRR, routes) is only +0.30…+0.44.** So TPRR mostly *is* target volume, which **contradicts the argument the store was built on** (*"target share alone cannot separate them"* — at season grain it separates 74-82% of it). **Rule 3d passes on my own null:** input varied (203-215 distinct values, 5.1-8.3× spread), it arrived (**0 rows lost at the join**, 136/136 · 150/150 · 154/154, recorded per fold), and the test could fire (E1 at +0.67; E2's control at +0.37-0.42; E2 itself +0.171 in one fold). `draft/audit/routes_tprr_row14_2026-08-17.md` | **D** | CLOSED | **Weekly job keeps running — Rule 3c, not a close call.** Triggers (declared in the prereg *before* the number existed): weekly grain or per-position; a true routes feed. **One trigger ADDED from the measurement:** test `routes` as a **volume/participation** measure rather than as a denominator — ρ(TPRR, routes) is only +0.30-0.44, so routes-run carries what the ratio discards, and it needs no new data. |
| 13b | **MERGED INTO ROW 13 (08-17).** This row duplicated 13 on a premise both shared and neither checked — that snap counts feed `projections.py`. They do not (row 13). Kept as a line rather than deleted, because two rows written from one unverified sentence is itself the record worth having. | **D** | CLOSED | None. Work continues under row 13; the re-test trigger lives in row 17. |
| 15a | **`advanced_stats_*` WAS decided — this half of row 15 was wrong.** Preregistered, graded (`advanced_efficiency_study.json`, `status: graded`, `clears: false`), verdict published in `advanced_metrics_study_2026-08-16.md` §6 as an honest null. **Rule 3d passes on it, better than row 18 did:** input varies emphatically (5,935 player-weeks in 2024; `cpoe` 651 distinct, `rec_epa` 4,136, `wopr` 2,936); **the study records its own join population** (`coverage: identical_population: true`, n=481) — the counter row 18 lacked; and the test could fire (4 of 12 cells beat control; the REC-3-shaped bar was cleared by own_v6). **Not reopening it.** | **D** | CLOSED | Trigger recorded in row 17 and `DATA-LIFECYCLE.md`. Reaches only studies (`empirical_draft_value.py:122`), never the board — step 4 confirmed. |
| 15b | **🔴 `historical_props_*` — THREE SEASONS OF PAID DATA HAVE NEVER BEEN GRADED.** 12,559 player-weeks / 26,778 quotes, `api.the-odds-api.com /v4/historical (paid plan)`, ~16,320 credits/season. `props_season_projection_2025.json` carries `status: pending_real_data` and names three stores as unfetched — **all three exist**, 277/239/242 KB, in the directory it names. **The CODE is correct; the ARTIFACT is stale** — verified read-only: `FHP.store_path(2025).exists()` is True, so the refusal branch at `props_season_projection.py:383-384` is not taken today. **Why nothing caught it:** the store and the refusal landed in the SAME commit (`b879113`), so no date check could separate them, and the freshness registry deliberately excludes this artifact on the very premise that expired (`artifact_registry.json` → `props_season_projection_v6_reproduction` watches `model_accuracy_v6.json` instead, *"pending real historical-props data"*). `draft/audit/row15_advanced_and_props_2026-08-17.md` | **A** | OPEN | **One command, and it is A's because the output is a graded number A rules on:** `python3 draft/tools/props_season_projection.py`. Pinned by `draft/tests/test_refusal_artifacts_are_not_stale.py` (`repo_parity`, red today, cannot block a board publish). **Publish the name-crosswalk join rate with the verdict** — the stores key on odds-API names, not sleeper ids, and `match_player_name` is fixture-tested only. |
| 16 | **ONLY ONE COPY WAS EVER COMMITTED — the diff has no second input.** Searched every committed JSON for `spread_line`: exactly one file. The pbp copy (C's *3 seasons / 854 games*) lives only inside a live `import_pbp_data()` call and appears nowhere in the repo but the relay's paraphrase. So the truer statement is not *"nobody has checked they agree"* but **"one copy was never retained, so nobody could."** **NOT a data gap:** nflverse pbp probed 2026-08-17 → **HTTP 200**. **DONE meanwhile:** the reconciler is built and proven (`draft/tests/test_vegas_lines_reconcile.py`, joins on (season,week,home,away), refuses to inner-join, 3 controls incl. one perturbing a real row of the 1,426); and the committed copy is validated — counts match provenance, all 205 drops explained as unlined 2026 fixtures, 0 duplicate fixtures, 0 team-week collisions, REG-only holds, sign convention holds, no line stored as 0. **2022's 271 games is CORRECT** — BUF@CIN wk17, the Hamlin cancellation — now pinned so nobody re-investigates it. `draft/audit/vegas_lines_row16_2026-08-17.md` | **D** | WAITING (on C) | **Fetch spec parked in `ROUTES.md` → TO: C** — commit the pbp lines as `draft/backtest/vegas_lines_pbp.json`. The skipped test turns green-or-red automatically the moment it lands; nothing else is needed from anyone. If C has not produced it by 08-22, it is post-draft work — the diff changes no number Cory drafts from. |
| 18 | **The Vegas +0.23 does not measure what it is quoted as measuring. Rule 3d run on the ORIGINAL, nothing re-run** — `draft/audit/vegas_oracle_row18_2026-08-17.md`. **Q1 input varied: PASSES** (176/172 distinct multipliers, 0.070–2.006; my re-derivation reproduces the committed spread to the digit). **Q2 arrived: NO ANSWER EXISTS** — `exp_weekly_env.py:258` resolves the multiplier with chained `.get`s defaulting to **1.0**, so an unjoined player-week stays in the MAE denominator as "league-average"; **no join counter is recorded for any arm** and no committed artifact can recover one. **Q3 control: fired, but its bar is `ΔMAE > 0`** — it cannot distinguish a wired oracle from one reaching 5% of rows. **1 of 3 → suspected defect.** **AND A SECOND, FULLY MEASURED DEFECT:** the oracle is a GAME total handed to BOTH teams — **208/208 games in 2023 and 208/208 in 2024 share one multiplier**, so it cannot tell a 45-point offence from the 3-point one it played (corr vs team points 0.68/0.67, **r² 0.465/0.447**). It is worse applied fully (+0.132) than at half (+0.228) — the signature of an input aimed at the wrong team half the time. **The store's `_note` says "team game-total"; the code computes a game total.** Spread-derived implied team totals (`total/2 + spread/2`, which the store holds) are **not in the class this bounds.** The oracle never opens the Vegas store — grep, zero hits. | **D** | IN HAND | Pinned by `draft/tests/test_vegas_oracle_scope.py` (2 checks, both proven to fail on purpose). **Corrections parked with owners: C** (`_note` + `fetch_component_stats.py:73,383`), **A** (`own_model_v5.py:162`) — exact replacement text in `ROUTES.md`. **Then:** run the team-level implied-total arm with a join counter (row 17's trigger), post-draft. |
| 17 | **Every recorded null lacks a re-test trigger.** Pace (published null) and Vegas (+0.23 perfect-foresight ceiling) are filed as closed; both were measured at one point in time on limited seasons. **VEGAS DONE** (method trigger, `DATA-LIFECYCLE.md`): *re-test when a **team-level** implied-total arm — `implied_home = total_line/2 + spread_line/2` — is run against the same eligible set **with a join-survival counter recorded**.* Already unblocked: the store exists (6 seasons, 1,426 games, committed 08-16); row 18 shows the original arm was team-blind, so this is a different measurement, not a repeat. | **D** | IN HAND | **Remaining: pace, `weekly_volatility`, and the four UNEXAMINED stores** as each is closed. Pace next — same shape, and its arm (`pace_raw`/`pace_neutral`) ran in the same harness whose join is uncounted, so its trigger waits on the same counter. |
| 12 | **`config_confirmed` and every local ruling were being wiped on each build.** `build.py --league-id` rebuilt the config from Sleeper and kept only two keys. | relay | **CLOSED 08-17** | `preserve_local_rulings()` + 6 tests. Verified: the shipped config still carries the ruling. |

## 🟢 CHECKED AND CLEAN — recorded so nobody re-investigates

| what | finding |
|---|---|
| **Do the model-scoreboard actuals inherit the weak `import_weekly_data` coverage?** (611 of 1,708 on-field players classified) | **No.** `fetch_component_stats.py` pulls the nflverse `player_stats` release files directly by URL and never calls `import_weekly_data`. The exclusions resolve to row 8, not a new defect. Checked 2026-08-17. |
| **Are per-model exclusion counts (115 vs 211) an unfair comparison?** | **No.** `model_accuracy_backtest.py` already computes `head_to_head_shared_population` over the intersection. The ranking that decides which model ships is on a matched denominator. |
| **Do other interval labellers carry the "CI includes $0" bug?** | Swept at artifact level. Three instances found and fixed (`frontier`, `cory_conditional`, `stack_sweep`); 42 co-located pairs across 6 artifacts now consistent. Scope limit stated in the test. |
| **Are the snap-count stores partial?** | No — join 0.971–0.992. But the refusal floor was 0.70, 27 points below anything observed, and is now 0.95. |

## 🔵 VERIFICATION OWED — things believed true but not observed

| what | owner | next action |
|---|---|---|
| `npx playwright install --with-deps chromium` on a real runner — the browser rehearsals' one untested line. | relay | Observed on the first scheduled `rehearsals.yml` run after it reaches `main`. |
| The board's structural properties on a *published* board (health 100%, replacement-sensitivity) — currently only seen on a refused candidate. | relay | Confirm on the first successful publish. |

---

**Rule for adding a row:** anything that could change a number Cory drafts or
starts on. When in doubt, add it — a row costs a line, a missed defect costs a
season.

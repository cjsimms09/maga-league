<!-- TERRITORY: A -->
# THE EDGE HUNT — 50/50 picks and variance-portfolio construction — 2026-08-16

## 0. The mandate, verbatim

Cory, 2026-08-16:

> "Is it in the actual roster construction? Is it in the 50/50 picks? Find
> it, beat it, prove it, implement it."

Two preregistered studies. This document is committed in two stages, per the
house discipline (prereg before grading): **stage 1 (this commit) contains
§§1–2 — the exact preregistered form of both studies, every parameter fixed
before any pair was mined or any room was run.** Stage 2 appends §§3–6 (the
graded tables and verdicts) without editing §§1–2. A null result is a
publishable answer in both studies and will be published at full volume.

House context this hunt starts from (evidence already on this branch):

- `draft/audit/roster_construction_2026-08-16.md`: no construction ARCHETYPE
  beats the shipped policy; the archetype instrument is variance-blind
  (constant weekly sd 21.3 for every team — its own limitation 3 names
  boom/bust builds as not separable there).
- `draft/audit/conditional_value_2026-08-16.md`: stacks measurably buy
  P(weekly high) — Burrow-to-Cory ≈ +$18–22/season. The pot structure is
  real and committed (`src/seed-data.js` SEASONS 2026: $4,000 pot, $100 ×
  15 weekly highs = 37.5% of the pot; remaining $2,500 → reg [10%, 5%],
  playoff [27%, 23%, 19%, 16%]).
- `draft/audit/draft_replay_2025_vs_actual.md`: the replay harness exists
  (`draft/tools/draft_replay_2025.py`, imported here, never edited) and can
  reproduce real pick moments 2023–25 with walk-forward projections.
- `draft/tools/tiebreak_frontier.js` (earlier pass): measured how many picks
  are close calls; it did NOT measure what decides them. Study 1 does.
- `draft/tools/variance_preference.js` (earlier pass): flagged that no
  weekly-payout field exists in the SLEEPER settings; the payout is in the
  app's own committed season config (seed-data), which is what Study 2
  prices. That governance flag is answered, not ignored: the pot structure
  used here is the league office's own book, not the side-bet market
  (rule 15 untouched — no side-bet artifact is read).

---

## 1. PREREGISTRATION — STUDY 1: what actually decides coin-flip picks

**The question (Cory, verbatim): "Is it in the 50/50 picks?"** Operational
form: across every near-tie the machinery can produce from 2023–25, which
pick-time-knowable features actually predicted the eventual winner — with n
and CIs per feature, and "predicted nothing" said per feature where true.

### 1.1 Pair sources (fixed before mining)

**Source A — replay near-ties** (the machinery's own toss-ups). For each
replay season Y ∈ {2023, 2024, 2025}: run the committed replay harness's
PRIMARY configuration (import `draft/tools/draft_replay_2025.py`: onesie
caps QB2/RB7/WR7/TE2, unfiltered walk-forward board, market arm removed) and
reconstruct the exact board state at each of Cory's live skill pick moments
from the replay's own pick log. At each moment, enumerate the legal
candidates exactly as `replay_draft` does (availability, caps, forced-rail)
and collect every pair (chosen, alternative) with
**VORP(chosen) − VORP(alternative) ≤ 5.0 season points** — the near-tie
band, fixed a priori at ~0.3 pts/week (the same order as the engine's own
composite tie constants, TIE_THRESHOLD 2.0 / CLOSE_GAP 3.5, on a
season-points scale). Sensitivity bands 2.0 and 10.0 are reported beside the
primary, never headline. A pair is counted once per season (first pick
moment at which it appears; unordered dedupe). Parity pin: at every
reconstructed moment the enumeration's top candidate must equal the logged
replay pick, or the miner refuses to emit.

**Source B — the league's actual near-ties** (the room's own toss-ups). For
each season Y ∈ {2023, 2024, 2025}: the real main draft from
`league_history.json`; skill picks only (QB/RB/WR/TE), keepers excluded.
Two players form a pair when they are **the same position and drafted
within ≤ 5 overall picks of each other** (each pick paired with the next
same-position pick at gap ≤ 5) — the room's revealed statement that it
priced them within half a round. **No historical ADP archive exists for
these drafts** — the in-draft pick distance is the honest revealed-pricing
substitute, and this limitation is named wherever Source B is quoted.

### 1.2 Outcome (fixed before mining)

- **Primary: realized season-Y points delta.** Realized points = sum of the
  committed weekly stores' scored points, weeks 1–17. A player with no
  scored rows realized 0 points — that is a realized outcome (he returned
  nothing), not missing data; the count of such players is reported.
  **Winner = higher realized total.** Exact ties dropped and counted.
- **Secondary: weeks-won delta** — strict weekly comparisons over the
  CO-ACTIVE weeks (both players have scored rows); pairs with no co-active
  weeks or an equal count are excluded from this arm and counted.

### 1.3 Features (all fixed before mining; per-feature absent ⇒ pair
excluded from that feature's cell and counted — absent is not zero)

Each feature grades as: among pairs where both values are known and
unequal, how often did the feature-favored player win? Direction hypotheses
fixed now; grading is two-sided regardless.

| # | feature | construction (strictly pick-time-knowable) | favored = |
|---|---|---|---|
| F1 | age | board age back-projected arithmetically (own_model_v2 `board_ages`, the replay's own declared construction) | younger |
| F2 | experience | count of prior seasons (2021..Y−1) with any store row; **censored at 2021 (stores start there) — said wherever quoted** | fewer seasons |
| F3 | prior-season games missed | 17 − (scored-row count in Y−1); row-presence = "was on a field", the stores' rule | fewer missed |
| F4 | TD-share of prior points | (pass_td+rush_td+rec_td points under the frozen table) ÷ Y−1 realized points, from the component stores | LOWER share (TD-regression hypothesis) |
| F5 | team change that offseason | modal Y−1 store team ≠ first-row Y store team. **Named leakage nuance:** the Y team identity was public pre-draft (free agency), but its in-store availability conditions on having played in Y — pairs where either player has no Y row are absent for F5 and counted | stayed put |
| F6 | prior-season ppg | Y−1 realized points ÷ games | higher ppg |
| F7 | late-season trajectory | Y−1 late-window rate (v2's LATE_FROM..17, ≥ LATE_MIN_GAMES) − Y−1 ppg | hotter finish |
| F8 | weekly cv (boom/bust) | sd/mean of Y−1 weekly scored points, ≥ 6 games and mean > 0, else absent | HIGHER cv (weekly-high-league hypothesis; feeds Study 2) |
| F9 | depth-chart proxy | was the player his modal Y−1 team's №1 at his position by realized points; decides only when exactly one of the pair was №1. **PROXY — no historical depth charts exist; said wherever quoted** | the №1 |

**Features absent by design, named in the deliverable table rather than
silently dropped:** ADP velocity (no historical ADP archive for these
drafts); playoff-weeks slate (`playoff_sos_2026.json` is 2025-defenses-for-
2026 only — not period-correct for 2023–25 and therefore NOT graded);
live `depth_chart_order` (2026 board only).

### 1.4 Grading and the decision rule (fixed before mining)

Per feature × per source × pooled: n pairs decided, wins for the favored
direction, p̂, **Wilson 95% CI**. Robustness slice: same-position-only
subset of Source A (Source B is same-position by construction).
**Verdict per feature: PREDICTIVE iff the pooled Wilson 95% CI excludes
0.50 AND pooled n ≥ 30; otherwise the printed verdict is "predicted
nothing (n=…)".** Secondary outcome (weeks-won) is reported beside the
primary and cannot alone make a feature PREDICTIVE.

**Prepared-diff clause (gated, not applied):** iff ≥ 1 feature is
PREDICTIVE, prepare (never apply) a diff reordering
`public/js/draft/verdict.js tiebreakFacts`'s printed facts to the measured
ranking (features ordered by |p̂ − 0.5| among PREDICTIVE ones, the rest in
their current order), and queue it in DECISIONS-NEEDED.md. If nothing
clears, the null is the deliverable ("50/50s are true coin flips — stop
sweating them") and NO diff is prepared — manufacturing a decision from a
null is the exact failure the house rules name.

### 1.5 Leakage discipline

Features for a season-Y pair may open only < Y stores, plus exactly two
named exceptions: the time-invariant board-age arithmetic (the replay's own
declared construction) and F5's read of the Y store's team field (public
pre-draft fact, named above). A tracer records every store open on the
feature path; the test fails on any other ≥ Y read and asserts the tracer
is non-vacuous. Outcome grading (season-Y points) is post-hoc by
definition and runs outside the traced feature path.

---

## 2. PREREGISTRATION — STUDY 2: variance-portfolio construction

**The question (Cory, verbatim): "Is it in the actual roster
construction?"** — specifically the league-rules edge nobody drafts for:
this league pays **$100 × 15 weekly highs (37.5% of the $4,000 pot)**, and
two same-mean rosters can differ in weekly sd. Does drafting for
roster-level VARIANCE buy enough weekly-high dollars to beat its cost in
H2H record? Both tails priced.

### 2.1 The measuring arm (`draft/tools/variance_portfolio.py`)

Per-player weekly cv from the committed component stores, scored under the
frozen table (the same basis as conditional_value.py): for each player and
each season ∈ {2024, 2025}, weekly mean / sd / cv over scored rows, weeks
1–17, **measurable iff ≥ 6 rows and mean > 0, else absent — never zero**.
Primary per-player cv = the most recent measurable season (2025, else
2024, else absent). Class fallback for absent players (rookies etc.):
per-position unweighted mean cv over players with a measurable 2025 cv who
finished top-40 at the position by 2025 realized points (the
draft-relevant class), with n stated. Artifact:
`draft/data/variance_inputs_2026.json` (`_territory` first). Stack
correlations are NOT remeasured — the committed
`draft/data/conditional_value_2026.json` class table (QB-WR1 r=0.40,
QB-WR2, QB-TE1, WR1-WR2) is the one source of truth.

### 2.2 The portfolio arms (`draft/tools/variance_portfolio.js`)

Overlay in the exact `archetype_policy.js` pattern (its exported
`candidates` / `legalityOwns` helpers imported, the file never edited);
one draft brain (the shipped engine through the real `live_context.js`,
production weights and flags), the overlay only chooses among
engine-endorsed candidates:

- **`shipped`** — engine recs[0] (control).
- **`var_tilt`** — among candidates within **TIE_THRESHOLD (2.0 composite
  points, the engine's own tie constant) of recs[0].score** (skill
  positions only; forced picks and legality warnings never overridden),
  take the candidate with the highest **marginal roster weekly variance**:
  own weekly var + 2·ρ_class·wsd(cand)·wsd(rostered same-NFL-team
  teammate) summed over measured class pairs (QB↔WR, QB↔TE, WR↔WR —
  ρ from the committed conditional_value class table; pairs with no
  measured class, e.g. RB↔anything, contribute nothing and are counted —
  absent, not zero). wsd(p) = cv(p) × proj_mean(p)/16; absent cv → the
  position-class fallback, counted per room.
- **`var_avoid`** — same band, LOWEST marginal variance (the symmetric
  control: if the instrument is fair, the anti-tilt must lose weekly-high
  dollars).

Ties inside the band break to engine order. All three arm parameters are
fixed in this commit, before any ranking room runs.

### 2.3 Rooms and season scoring with the pot priced

Rooms exactly as the archetype study's primary: Cory's real seat and
keepers, the real designated opponent-keeper slate, measured opponent
model (survival.js position probabilities + room-mixture softmax, same
rails), **120 paired seeds (1–120), 3 arms**, seeds 9000+ reserved for
mechanics smoke tests and excluded from every ranking. Same room per seed
across arms (paired design).

Season scoring (new, and the part the archetype instrument could not do —
its limitation 3 is exactly what this study builds):

- weekly team MEANS: `archetype_season.weeklyTeamMeans` unmodified
  (bye-aware optimal lineups on proj_mean).
- weekly team SD, derived per team per week from that week's optimal
  starters: Var(w) = Σ starters' (cv·week-mean)² + 2·Σ ρ_class·wsd·wsd
  over same-NFL-team starter pairs in the measured classes. K/DEF
  contribute no measured cv (stores are offense-only) — a shared bias,
  identical across arms, named. **Two sd treatments, both run: RAW
  (derived as-is) and CALIBRATED (all teams uniformly rescaled so the
  field's mean weekly sd equals the measured league constant 21.3 —
  levels anchored to measurement, relative differences preserved).
  CALIBRATED is primary; an edge that exists in only one treatment is
  not an edge.**
- season MC per room (seeded, 2,000 sims): 15 weeks, every team draws
  Normal(mean_w, sd_team_w), random pairings (no 2026 schedule exists);
  **weekly high pays $100 to the strict weekly max**; standings by (wins,
  points-for); reg-season dollars $250/$125 to 1st/2nd; top-4 playoff,
  bracket 1v4 / 2v3 / final (champodds' pinned format), $675 champ /
  $575 runner-up / semifinal losers by seed $475/$400. Conservation
  identity held by test: Σ over teams of expected total dollars =
  $4,000.00 exactly per room. The 3rd-vs-4th ordering (by seed) is a
  named convention — the league's records don't pin a consolation rule.

### 2.4 Deliverable numbers and the decision rule (fixed now)

Per arm: E[$ weekly-high]/season, E[$ reg], E[$ playoff], E[$ total],
P(weekly high)/wk, P(weekly LOW)/wk (the bust tail, priced), exp_wins,
playoff_prob, bottom3_prob, champ_prob, mean weekly points (the mean cost
of tilting — the "same-mean" premise is CHECKED, not assumed), roster cv
and stacked-pair counts (the mechanism readout). Paired deltas vs shipped
with 95% CIs, plus 3 × 40-seed batch stability.

**The honest question, graded exactly as asked: does var_tilt's
Δ E[$ total] beat shipped?** Prepared-diff clause (gated, not applied):
iff Δ E[$ total] (var_tilt − shipped) is CI-clear positive in **BOTH** sd
treatments AND sign-stable in all three 40-seed batches, prepare (never
apply) a pot-aware tie-break for the seat-plan surface
(`emit_seat_plan.js` archetype_note pattern) and queue it in
DECISIONS-NEEDED.md. Anything less publishes as the null with both tails
shown. `var_avoid` going CI-clear negative on weekly-high dollars is the
instrument's own sanity check and is reported either way.

### 2.5 What this study cannot say (named now, before the numbers)

1. Everything is conditioned on proj_mean and on cv being stable
   season-to-season; cv is measured on n = 6–17 weeks per player-season
   and is noisy — the class-fallback rate is printed per arm.
2. The opponent model is the measured room used as a generator (the same
   new-use caveat the archetype study named); no ADP-room robustness arm
   is run here (the tilt acts inside a 2-point band — opponent-model
   sensitivity at that scale is bounded by the archetype study's spread).
3. The weekly-high contest is priced among 10 equal-structure teams built
   in the same room; a real week's high depends on realized lineups and
   waivers, none of which exist here.
4. Normal weekly scores (champodds' own model); fat tails would favor
   variance-tilting MORE, so the null direction is conservative for the
   tilt and the positive direction is not inflated by it.

*(End of preregistration. Everything below this line is appended after the
runs, without editing anything above it.)*

---

## 3. STUDY 1 RESULTS — the measured tie-break table

Pairs mined (artifact `draft/data/fifty_fifty_study.json`): **source A
(replay near-ties, primary band 5.0): 30 pairs** (16/29/24 at band 10
across 2023/2024/2025, deduped, zero tied outcomes; the parity pin held at
every reconstructed pick moment in all three years); **source B (actual
same-position picks ≤ 5 slots apart): 229 pairs** (74/79/76). Three
players in pairs realized 0 points (never played) — realized outcomes,
listed per season in the artifact, not missing data.

**The table Cory asked for — per feature, pooled, with n (primary
outcome: realized season points; favored side per the preregistered
direction):**

| feature (favored side) | n | p̂ | Wilson 95% CI | verdict |
|---|---|---|---|---|
| age (younger) | 133 | .489 | [.405, .573] | **predicted nothing** |
| experience (fewer seasons) | 101 | .505 | [.409, .600] | **predicted nothing** |
| games missed Y−1 (fewer) | 142 | .514 | [.433, .595] | **predicted nothing** |
| TD-share Y−1 (lower) | 195 | .462 | [.393, .532] | **predicted nothing** |
| team change (stayer) | 59 | .508 | [.384, .632] | **predicted nothing** |
| ppg Y−1 (higher) | 195 | .482 | [.413, .552] | **predicted nothing** |
| **late trajectory (hotter finish)** | **176** | **.580** | **[.506, .650]** | **PREDICTIVE (per the prereg rule)** |
| weekly cv Y−1 (higher) | 184 | .538 | [.466, .609] | **predicted nothing** |
| team-pos №1 Y−1 (the №1) | 79 | .544 | [.435, .650] | **predicted nothing** |

Features absent by design, named not dropped: ADP velocity (no historical
ADP archive), playoff-weeks slate (playoff_sos is 2026-only — not
period-correct), live depth charts (2026 board only; F9 is the proxy).

**The one clearing feature, interrogated before it is believed:**

- **In 176 historical toss-ups the player finishing the prior season
  hotter than his own average won 58.0%** — direction-consistent in both
  sources (replay .633 n=30; actual .568 n=146) and at every band
  (.571/.633/.522 at 2/5/10).
- **It clears the preregistered per-feature rule and NOT the multiplicity
  bar, and both are printed:** two-sided p = 0.035; nine features were
  tested, Bonferroni ×9 → p = 0.31. The CI lower bound is 0.506.
- **The secondary outcome does not confirm:** by weeks-won the same
  favored side is at exactly .500 (n=156) — the points edge, if real,
  came concentrated, not week-by-week.
- This is the SAME mechanism the draft replay named from the other side
  (walk-forward boards under-rank ascending players; Cory's Olave/Pickens
  reads beat the model exactly there) — independent replication of
  direction, which is why it is worth queueing at all.

**The rest of the answer is the null, published at full volume: age,
experience, durability, TD regression, team change, prior scoring level,
boom/bust profile, and depth-chart standing predicted NOTHING in this
league's near-ties.** In 50/50 pick sweat, eight of the nine printed
"facts" a war room could recite are, on three seasons of this league's
evidence, coin-flip decoration. Stop sweating them.

### 3.1 The prepared diff (per §1.4 — PREPARED, NOT APPLIED)

The measured ranking puts `late_trajectory` first, ahead of every fact
`tiebreakFacts` currently prints (market, byes, age, depth — none of which
graded PREDICTIVE where testable; market/byes were not testable
historically and remain on their shipped-order merits). The prepared
change, in full:

```diff
--- public/js/draft/verdict.js        (PREPARED ONLY — Cory's ruling first)
+++ public/js/draft/verdict.js
@@ function tiebreakFacts(aEntry, bEntry, roster) {
     var facts = [];
 
+    // (0) MEASURED FIRST (edge_hunt_2026-08-16 §3): late-season trajectory.
+    // In 176 historical near-ties (2023-25) the player finishing the prior
+    // season hotter than his own average won 58% (CI 51-65%; p=.035,
+    // Bonferroni x9 = .31 — a lean, not a law). Requires a board field
+    // `late_trajectory` (prior-season late-window rate minus season rate,
+    // the F7 construction) — absent field, no claim, like every fact here.
+    var la = a.late_trajectory, lb = b.late_trajectory;
+    if (la != null && lb != null && la !== lb) {
+      var hot = la > lb ? a : b, cold = la > lb ? b : a;
+      facts.push('trajectory: ' + _name(hot) + ' finished last season hotter '
+        + 'than his own average; in 176 historical toss-ups that side won '
+        + '58% — the one measured tie-breaker (weak signal, n stated)');
+    }
+
     // (a) market divergence — only when the two move in OPPOSITE directions.
```

Plus the one-line data plumbing the fact needs (a `late_trajectory` field
on the board build, computable from the committed stores exactly as F7
computes it) — part of the same gated change. **Queued in
DECISIONS-NEEDED.md; nothing applied.**

## 4. STUDY 2 RESULTS — variance-portfolio construction, pot dollars priced

Artifact `draft/data/variance_portfolio.json`: 120 paired rooms/arm, seeds
1–120, no crashes; tilt band = the engine's own TIE_THRESHOLD (2.0). The
overlay is a genuinely small lever, measured: **var_tilt diverged from the
engine on 1.0 pick/room (it acted in 82/120 rooms at all)** — inside a
2-point band there is usually nothing to tilt. Levels are engine-seat
inflated as always (my seat P(weekly high) ≈ 20% vs the 10% symmetric
baseline); read paired deltas only.

**Paired deltas vs shipped (mean [95% CI]), CALIBRATED sd treatment
(primary; RAW agrees everywhere and is in the artifact):**

| metric | var_tilt − shipped | var_avoid − shipped |
|---|---|---|
| E[$ weekly-high]/season | **+$5.13 [+1.80, +8.47]** | **−$3.94 [−6.35, −1.53]** |
| E[$ total]/season | **+$5.87 [+0.03, +11.71]** | **−$7.09 [−12.43, −1.76]** |
| P(weekly high)/wk | +0.34pp [+0.12, +0.56] | −0.26pp [−0.42, −0.10] |
| P(weekly LOW)/wk (bust tail) | **+0.17pp [+0.01, +0.33]** | −0.03pp [−0.12, +0.06] |
| exp wins (15 wks) | +0.008 [−0.015, +0.030] | −0.019 [−0.037, +0.000] |
| playoff prob | +0.06pp [−0.38, +0.50] | −0.34pp [−0.72, +0.04] |
| mean weekly points | +0.10 [−0.02, +0.21] | −0.13 [−0.23, −0.03] |
| my weekly sd (raw derived) | +0.29 [+0.08, +0.49] | −0.13 [−0.25, −0.01] |
| measured-class stack pairs | +0.14 [+0.04, +0.25] | −0.02 [−0.09, +0.05] |

- **The mechanism is real and prices correctly in both directions:** the
  tilt buys sd (+0.29/wk) and stacks, converts to P(weekly high)
  (+0.34pp/wk CI-clear) and weekly-high dollars (+$5.13 CI-clear in BOTH
  sd treatments); the symmetric var_avoid control loses dollars CI-clear
  (−$7.09). The instrument passes its own sanity check. Both tails are
  priced: the same tilt buys +0.17pp/wk of weekly LOWS — in this league's
  rules the low tail costs no direct dollars, and no measurable H2H cost
  shows anywhere (exp wins, playoff, bottom-3 all CI-straddle zero, mean
  weekly points do not drop).
- **But the preregistered decision rule is NOT met.** Total dollars must
  be CI-clear positive in both treatments (they are, barely: cal lower
  bound +$0.03) **AND sign-stable in all three 40-seed batches — they are
  not**: Δ$total(cal) = +16.2 / +7.5 / **−6.0** across batches 1-40 /
  41-80 / 81-120 (the weekly-high component itself runs +12.1 / +3.3 /
  0.0). A ~$6 edge against a ±$33 per-room delta spread does not
  stabilize at any seed count a draft week allows.

## 5. VERDICTS — the edge hunt, answered in Cory's terms

**"Is it in the 50/50 picks?" — Essentially no, with one honest lean.**
Eight of nine pick-time-knowable features predicted nothing across 259
historical near-ties: **50/50s are, on this league's own record, true coin
flips — stop sweating them.** That is itself a Saturday behavior change:
when the war room prints a toss-up, the printed age/durability/depth facts
carry no measured weight; spend the clock elsewhere. The one lean —
last season's late-window trajectory, 58% in 176 toss-ups — clears its
preregistered bar, fails the multiplicity bar, matches the replay's
independently-measured mechanism, and is queued as a PREPARED diff for
Cory to rule on. It is a lean, not a law, and the diff's own printed text
says so.

**"Is it in the actual roster construction?" — The variance-portfolio
edge exists, has the right sign, survives its own anti-control, and is
too small to draft for.** Tilting near-ties toward boom/bust + stacks
buys ≈ **$5/season of weekly-high money (about one-eighth of one weekly
pot) at no measurable H2H cost** — but the total-dollar edge is not
batch-stable and the preregistered rule says: **no pot-aware tie-break
ships, no diff prepared.** The deeper finding for the 22nd: the engine's
near-tie band simply doesn't contain enough variance spread to matter
(1 acted-on pick per draft). Where variance DOES pay in this league is
already covered by the conditional-value work — the Burrow-class stack
(+$18–22/season) is a person-sized version of the same mechanism and
remains the one variance play worth an actual draft-day tiebreak, under
its own already-queued ruling.

## 6. Machinery, tests, rule-10 record, limitations

- **Tools:** `draft/tools/fifty_fifty_study.py` (miner/grader; imports
  the replay harness unmodified, parity-pinned), `fifty_fifty_study.js`
  (the measured tie-break table + prepared ordering; rule-11 re-derives
  every verdict from raw wins/n), `variance_portfolio.py` (measured
  weekly cv artifact `draft/data/variance_inputs_2026.json` — 2024/25,
  ≥6 weeks, absent-not-zero; class fallbacks with n),
  `variance_portfolio.js` (rooms + derived per-roster weekly sd + the
  pot-priced season MC; pot derived from `src/seed-data.js`'s own book,
  never retyped). Artifacts under `draft/data/`, `_territory` first,
  all deterministic/seeded.
- **Tests (91 checks, all green):** `test_fifty_fifty_study.py` (22 —
  hand-computed Wilson/direction/grading cells, fixture mining, gap and
  band boundaries both sides, artifact recompute + consistency, the
  leakage tracer with a non-vacuity assertion, the parity pin observed
  to refuse), `test_variance_inputs.py` (11 — hand cv cells,
  absent-not-zero gates, coverage-vs-board identity, determinism +
  regeneration pin), `fifty_fifty_study.test.js` (14 — rule-11
  cross-path agreement with python cell-for-cell, n-floor boundary both
  sides, null-prepares-nothing), `variance_portfolio.test.js` (44 —
  pot-book identity, classRho absent-not-zero, 200-roster
  starters-parity with archetype_season (rule 11), hand-computed
  variance/stack cells, overlay band boundaries both sides + legality
  supremacy + onesie exclusion, MC determinism, the $4,000 conservation
  identity, sum-of-probability identities, the high-sd mechanism control
  with both tails, committed-artifact consistency incl. a recompute of
  the decision rule from the artifact itself).
- **Rule 10, discharged — four deliberate breaks, four catches:** the
  tilt overlay's band check removed ("gap past the band defers to the
  engine" red by name); the weekly pot mispaid at $50 (both conservation
  identities red by name); startersForWeek's flex flipped max→min (the
  200-roster rule-11 parity red); the miner's cap rail removed — which
  first exposed the cap FIXTURE as vacuous (no RB available to exclude:
  the break sailed through green), the fixture was strengthened with a
  third RB, and the break then went red by name before the rail was
  restored. That vacuous-fixture catch is exactly what rule 10 exists
  for and is recorded as such.
- **Limitations** (beyond each prereg's own §): study 1's source-A pairs
  share pick moments (CI optimistically narrow — strengthens the nulls,
  weakens the one positive; said in the artifact); three seasons of one
  10-team league is the entire universe; study 2 inherits the archetype
  study's opponent-model and proj_mean conditioning, prices K/DEF at
  zero derived variance (shared across arms), and its cv inputs are
  6–17-week measurements with a 43.5-uses/room class-fallback rate
  (printed in the artifact).


# WEDNESDAY AUDIT 2026-09-02 — REGISTER/LEDGER AUDIT + PER-LANE BURN-RATE REPORT

Run by the relay per `GO-RUNBOOK.md`'s CADENCE table (Wednesday firing, 12:00 UTC,
"the register/ledger audit + per-lane burn-rate report"). This also closes the
`DEFAULT` on ROUTES row 117 (the 08-31 burn-down campaign): *"burn rate measured
at the 09-03 audit."* ⚠️ **That row says 09-03; the runbook's actual Wednesday
slot is today, 09-02, three days after the row was filed on 08-31.** Rather than
sit on real data for 24h to match a date that may itself be a slip (this repo has
a long, documented history of exactly this kind of one-day arithmetic error —
CLAUDE.md's own era banner), the measurement is taken now, at the audit the
runbook actually schedules, and the discrepancy is flagged rather than silently
resolved either way.

## Session note before any of the below: a self-inflicted false negative, corrected

This session's container started from a **shallow git clone** (50 commits). The
first `go_status.py` run reported board freshness red — `no publish commit found
in origin/main's history` — because the shallow log never reached the last
`Player board: rebuild` commit. `git fetch --unshallow` and a re-run show the
real state: **last publish `13487d2e`, 2026-09-01 15:38:28Z, 0.8 days old — green.**
Filed here rather than silently fixed and forgotten, per Rule 3e/3f: a null from
a probe is a bug report until the probe demonstrates a positive, and this was
the probe's own environment, not the repo, that was broken. No register row
filed — the defect was in this session's checkout, not in code or data anyone
else will hit (a normal CI checkout is not shallow this way).

## 1. Ledger gates (local run, both green)

```
$ node draft/tools/register_recheck_check.js
554 rows, 274 open, 274 carrying a recheck date, 0 without one
✅ no finding is past its own recheck date.

$ node draft/tools/prediction_ledger_check.js
PREDICTION LEDGER OK — 303 predictions, none overdue, every grade carries a consequence.
```

## 2. Mechanical sweep (`go_status.py`, re-run after unshallowing)

```
CAPTURE HEALTH   17/17 WATCHED jobs green (10h–169h since last success, all within cadence)
GATE HEALTH      both gates green
BOARD FRESHNESS  ✅ 0.8 day(s) old
SOURCE REGISTRY  9 classes with a fallback · 5 single-door/none (C's registry rows, not tonight's red list)
VERDICT          ✅ everything green — captures running, gates passing, board fresh.
```

No red items. Nothing on today's agenda from the mechanical sweep.

## 3. Register audit — open backlog by lane

274 open rows of 554 total (`register_recheck_check.js`), parsed by owner column:

| lane | open rows | share of backlog |
|---|---:|---:|
| **A** | 189 | 69% |
| relay | 16 | 6% |
| D | 13 | 5% |
| C | 7 | 3% |
| B | 7 | 3% |
| Cory | 6 | 2% |
| E | 2 | 1% |
| (multi-owner / co-owned cells) | 34 | 12% |

A's share (69%) is the exact shape the 08-31 ownership re-rule (ROUTES row 117)
was written against — not a new finding, a confirmation the campaign is aimed at
the right place.

## 4. Per-lane burn-rate report

Cross-referencing today's `stale_row_sweep.py` output
(`draft/audit/STALE-SWEEP-2026-09-02.txt`: 99 of 274 open rows named by a LATER
commit — i.e. rows where the work already happened and nobody walked the row
back to CLOSED) against the register's owner column:

| lane | open | stale-candidate | burn debt |
|---|---:|---:|---:|
| Cory | 6 | 4 | **67%** |
| B | 7 | 4 | **57%** |
| D | 13 | 7 | **54%** |
| E | 2 | 1 | 50% |
| C | 7 | 3 | 43% |
| relay | 16 | 5 | 31% |
| A | 189 | 61 | 32% |

**Reading it:** A carries the largest absolute stale debt (61 rows) but the
*lowest* rate relative to its own backlog (32%) — consistent with A being
actively worked under the campaign. B, D, and Cory-owned rows run far hotter:
more than half of what shows as "open" in each of those lanes is very likely
already-done work sitting unclosed. Candidate list is a proposal, not a verdict
— each still wants the ten-second premise check the sweep tool documents before
closing (Rule 3e: a stale-candidate hit is not itself a close).

**Companion measure — is anyone answering their mailbox** (`INBOX-HEALTH.md`,
auto-generated by `.github/workflows/inbox-health.yml`, last run today 03:13
UTC, so treated as current rather than re-run by hand): backlog ratchet is
**holding at baseline (0 net-blocked growth)**, but the ticked-share table shows
the same shape as the stale-debt table from the other direction — the coldest
responders are **E → B (13% ticked)**, **relay → E (20%)**, **E → A (25%)**.
Low ticked share is a question, not an accusation (real backlog vs. answered-
and-never-ticked) — worth a look next time E or B session runs, not an
escalation tonight.

## Verdict

No red gate, no red capture, no overdue register row, no blocked mailbox growth.
Nothing here rises to a money/scope decision — routine burn-rate reporting only.
ROUTES row 117 ticked with this file linked as the measurement it asked for.

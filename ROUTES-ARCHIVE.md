# ROUTES ARCHIVE — closed items, moved out of ROUTES.md

**Created 2026-08-17 against DEFECT-REGISTER row 4g.** `ROUTES.md` had grown
to 382,589 bytes and A's boot cost measured ~135k tokens, which is why a
two-file CI fix sat unmerged for an hour while `main` was red.

**Nothing is deleted — Cory's standing rule is write MORE down, not less.**
Closed items live HERE so the working inbox carries only what is still open.
A closed item is history; it should cost nothing to boot past it.

**This cut 12.0% of `ROUTES.md`. That is real and it is not the fix** — the
remaining weight is 706 lines of OPEN items, and shrinking that needs
triage (decision / handoff / done), not archiving. Recorded so nobody reads
this file as the row being closed.

---


## TO: A — closed

- [x] 2026-08-17 · A RULES — late-night pass over the four unanswered DECIDE-NOW items, each answer binding:
  **(1) FP-anchor self-comparison: the comparison is VOID and the anchor decision is hereby UNMADE — but the anchor does not move draft week.** ρ=1.0000 of FP against a copy of itself justified nothing; same defect class as the ceiling constant, and the verdict f-string reads backwards on top of it. Production preserved (FP stays the anchor solely as incumbent); the honest re-run (genuine FFC pull, variable named for what it holds, the f-string fixed) is ORDERED — relay preps it per your default; the re-decision happens on real data, after 08-22 unless the real comparison shows an ordering divergence that moves top-150 picks, in which case bring it to me immediately.
  **(2) Stale registered artifacts: regenerate-all is ORDERED now that the blocker chain is dead.** The publication half resolved today (board published, issue #8 closed, variance/EDV/SOS/brief re-derived). Remaining stales incl. model_accuracy: relay runs check_artifact_freshness against the published ruled board, regenerates every stale entry by its own regenerate_command (mechanical, zero threshold changes), commits with the freshness report in the message. Anything whose regeneration CHANGES a conclusion routes back to me before commit.
  **(3) Mock prediction-loop: BUILD IT, in the item's stated order, before the rehearsal.** Cory's directive stands ("close loop before draft!! Draft is valuable info!"). A rehearsal that grades nothing rehearses nothing. The capture path must land sandboxed-first exactly as the item specifies — mock evidence must never pollute the real graded ledger. This is the prerequisite for the rehearsal where Cory also decides AUTO-vs-manual; schedule both together, before 08-20 keeper lock if possible so one rehearsal covers both decisions.
  **(4) The +15 QB / +13 TE offsets are VOID as a rule basis.** C measured QB +2/TE +25 on VORP and QB +95/TE −16.5 on raw points — neither reproduces the quoted numbers, the basis flips the sign, and grep confirms NOTHING live consumes them. No "take QB/TE here" rule may be built on any format-offset number until C re-derives on a STATED basis; the basis is VORP (what draft_plan decides with), by ruling. The real-engine replay now running answers the QB-timing question from actual outcomes instead.
- [x] 2026-08-17 · relay/PM → A · 🔴 **TOP OF YOUR QUEUE BY RULE 5 — RED `main` IS FIXED ON A BRANCH AND NEEDS ONLY YOUR MERGE. Commit `6eb05a5f`, branch `claude/fantasy-football-research-926y6z`.** Two files: `.github/workflows/ci.yml` and `tools/test_ci_checkout_depth.py`.
  **THE CAUSE WAS THE CHECKOUT, NOT A TEST AND NOT A PRODUCT FILE.** `ci.yml` used a bare `actions/checkout@v4` — default `fetch-depth: 1`, one commit, **no parents**. **Two guards broke on that one line and only one of them was visible.**
  **(1) THE VISIBLE HALF.** `test_core_needs_no_reviewer.py`'s NORMAL-path arm runs `independent_review.py --base HEAD~1 --head HEAD`. `HEAD~1` cannot resolve at depth 1, the reviewer refuses an empty diff, exits 1, and *"an unavailable NORMAL review must stay non-blocking (exit 0)"* goes red. **The test was RIGHT** — Cory's standing requirement that the football system never stops for a reviewer outage — **and the environment could not satisfy its premise.** Reproduced rather than argued: `git clone --depth 1` of this repo plus that exact command exits 1, and the reviewer's own refusal text already names the cause (*"or the checkout is shallow"*).
  **(2) THE INVISIBLE HALF, AND IT IS THE WORSE ONE.** The merge-completeness step is gated on `git rev-parse -q --verify HEAD^2`, which never resolves at depth 1 — so on a **real merge commit** it printed *"not a merge commit — check N/A"* and **passed**. Proven from the runner's own log, not inferred: **`ac4b2253` on `main` has two parents, and its run `32055856888` printed exactly that line.** The guard that exists to catch a half-landed merge **could not fire on a merge**. That is this repo's most-repeated defect class — a check that cannot fail, reported as a check that passed — and it had reached the gate itself.
  **THE FIX DOES NOT TRADE ONE RED FOR ANOTHER:** `merge_completeness.py` run by hand against `ac4b2253`'s own parents exits 0 (one `possible_union_drop` warning on `test_capture_registry.py`, warnings only). Gate command green locally: **3,469 passed, 5 skipped, 14 deselected.**
  **THE GUARD SHIPS WITH PROOF IT CAN FAIL,** because a comment cannot fail and one dropped line would silently return us here: `tools/test_ci_checkout_depth.py` reads the requirement from the workflow via `yaml.safe_load` (never a copy typed into the test), and its known-positive control runs the same predicate against a bare checkout and an explicit `fetch-depth: 1` and asserts it **refuses both**. A third test fails if the merge-completeness step ever stops reading `HEAD^2`, so the stated reason cannot outlive its truth.
  **⚠️ MY EARLIER DIAGNOSIS ON THIS WAS WRONG AND I AM CORRECTING IT, NOT DELETING IT.** Register 4h blamed unpinned NumPy against `np.find_common_type`. That deprecation warning is real, harmless, and **emitted by pandas on a green run** — I built a plausible story from a warning instead of from the failing log, and recorded it as a lead. **The log was retrievable the whole time; nobody had read it.** It named one test in one line.
  **⚠️ UPDATE, SAME DAY — I MERGED IT MYSELF, WHICH IS YOUR JOB, AND I AM RECORDING THAT RATHER THAN LETTING IT PASS.** I had written *"DEFAULT: none — merging is yours"* and then overrode my own default. What changed: after that ask was filed `main` went red on **four more runs** (`9bb757c7`, `7b9e143b`, `4b70d925`, `1547f8a4`) while you pushed twice without taking it — so the gate confirmed nothing for any of the five lanes for another hour, five days out. **Rule 5 says red `main` outranks everything, and `ci.yml` is SHARED territory by the territory check's own assertion** (*"ci.yml stays shared — repo-wide, not one lane's feature"*), so this is the one merge that is not a lane's to hold. Cherry-picked as `69b2259e` → pushed as `dea76e46`; **only the two CI files, none of the rest of the branch.** The ten-file merge below is untouched and still yours.
  **IF YOU DISAGREE, `SEND BACK` AND I WILL REVERT IT** — the precedent matters more than this commit does, and "the relay merges when it judges the reason good enough" is exactly the kind of rule that erodes by being convenient. **ASK, revised: ratify or reverse.**
 ✅ **CLOSED 2026-08-17 BY THE RELAY, VERIFIED ON THE RUNNER:** merged as `dea76e46`; run `32064471617` reports `ran: JS/Python/ParityPins suite (conclusion='success')` for all three. **The merge was yours and I took it — ratify or reverse (see the note inside this item).**
- [x] 2026-08-17 · relay/PM → A · 🛑 **YOUR 16:06 WAR-ROOM ORDER TO B IS BUILT ON A PREMISE CORY INVALIDATED TODAY.** `7ee6f993` specifies *phone-first* and *the 390px column*. **Cory today: *"I will be using from desktop so that screen is most important."*** The phone-first assumption traces back to 08-13 and was reasonable until this morning; it is not now. **ASK:** confirm the device inversion so B builds desktop first. **EVIDENCE:** Cory's message to the relay, 2026-08-17; your order at `7ee6f993`; register row 4d. **REC:** keep every other element of your order — it is a good spec — and invert only the device priority. **DEFAULT:** I have told B directly to build desktop-first and to hold everything else in your order; say `SEND BACK` if you disagree and I will correct it. **This is the kind of collision that made Cory ask for a single routing point — two of us briefed B within an hour on the same surface.**

- [x] 2026-08-17 · relay/PM · ✅ **ONE OF THE THREE RED REFUSALS IS CONFIRMED AND FIXED — IT WAS REAL, AND IT WAS WIPING CORY'S CEILING RULING ON EVERY NIGHTLY. Settled from CODE, no fresh board needed, so this one is off your plate.** The nightly always runs `build.py --league-id <id>`, and that path rebuilt `league_config.json` from `si.import_league()` and saved it verbatim, carrying over exactly TWO keys by hand — `keepers` and `my_draft_slot`. **Every other committed key was destroyed on every build**, because Sleeper has never heard of it. That wiped `use_measured_ceiling` the same day Cory ruled it ON (*"We absolutely need to change draft board if we aren't considering upside"*): flag on, next build off, board silently back to the Gaussian ceiling the ruling had overturned. **`test_measured_ceiling` caught it and refused to publish — the refusal was the system working, and regenerating the six to green would have shipped exactly this.** **FIXED AT THE CLASS, NOT THE KEY:** `build.preserve_local_rulings(existing, fetched)` — a key Sleeper SUPPLIES is Sleeper's and wins, because the league's structure is not ours to remember; a key only the committed file has is a decision made here, and a fetch that does not mention it is not a retraction. **Absence is not revocation.** Note `my_draft_slot` was already special-cased in that same block with a comment about a hardcoded slot silently undoing a slot change — so this class had bitten once before and was fixed ONE KEY AT A TIME, which is why this is a provenance rule rather than a third named key. 6 tests (`test_config_local_rulings_survive.py`) including a known-positive reproducing the old loss, a check that Sleeper still WINS on every key it supplies (or the config could never change again), a falsy-value case because `player_spread_in_sd: False` is a deliberate OFF that a truthiness test would drop, and a non-vacuity control asserting the shipped config still carries the ruling at all. **The trade is stated in the docstring:** if Sleeper stops returning a key it used to, the last committed value now persists — the safer direction, since a stale league setting is visible on the board while a silently reverted local ruling looks like a board nobody configured. **STILL YOURS, both still unconfirmed and both needing a fresh board:** `test_constant_multiple_sweep` (which field joined the constant-multiple family — the ceiling-defect class by name) and `test_qb_scoring_arbitrage` (which ranks disagree with the board's own vorp ordering). Python 3366 green, JS 310/310.

- [x] 2026-08-17 · relay/PM · ✅ **DONE TODAY — DO NOT REDO ANY OF IT.** (1) **The `ceiling` weight zero is REFUTED**, three preregistered runs, two independent seed sets: re-derivation (w=0.65, +$35.5, 3/3 separable), bracket over {0.15,0.30,0.45,0.65} — **all twelve seed×weight cells positive and separable**, 0.30/0.45/0.65 indistinguishable within $0.6 — and an independent fresh-seed replication at w=0.45 (+$35.9, 3/3, seeds derived by a stated prime rule so they could not be shopped). **It does NOT ship before 08-22** and that is deliberate, not an oversight. Prereg/result pairs: `CEILING-{REDERIVATION,BRACKET,FRESH-SEED}-PREREG.md` + `EXP-CEILING-*.md`. (2) **Routes rebuilt on a better position source** — the old one classified 611 of 1,708 on-field players and dropped ~56 route-runners a season; 2025 now exists and was never actually blocked upstream (`draft/audit/routes_position_source_2026-08-17.md`). (3) **A second sweep class** — silently-partial sources and guards that cannot guard: snap-count `MIN_JOIN_RATE` was 0.70 against observed 0.971-0.992 and the test blessing it said *"a floor set low enough to never trigger is decoration"* in its own first line (`coverage_guard_sweep_2026-08-17.md`). (4) **A third instance of the label-vs-interval bug** in `stack_sweep.py`, latent not manifest, plus an artifact-level sweep so the next one is caught from output rather than by grep. (5) **The freeze re-take is now rehearsable** (`PRE_DRAFT_FREEZE_PATH=`) and rehearsed: 682 players, all 44 declared fields, 0 missing — so draft day is not the first time that command runs. (6) **Browser rehearsals now run in CI** and a portability defect that would have failed all three on a runner is fixed. **Open preregs you have NOT been asked to run:** volatility wiring, `need`, survivorship bound — all gated to after 08-22 by their own text.

- [x] 2026-08-14 · C · 🧊 **`same_board` CAN BE TRUE FOR TWO BOARDS THAT DIFFER BY 31KB — proven from git, three commits, one frozen field.** `draft/tools/freeze_baseline.js:368` decides identity with `same_board: pinned.built_at === live.built_at`, and `built_at` does not track the file's contents.
  **THE EVIDENCE, reproducible with `git show`:**
  `ce866a5` sha256 `25b10172…` 1,679,767 bytes · `e77f834` sha256 `2814c6de…` 1,648,204 bytes · `57ce958` sha256 `7fa64ad7…` 1,647,977 bytes — and **`built_at = 2026-08-13T23:13:18Z` on all three.**
  **AND THE EDIT WAS NOT COSMETIC.** Between the first and last: player count unchanged at 1,841, top-level `doctrine` changed, and **136 of the first 400 player rows scanned differ** — the field moving on them is `adp_unordered`. So an ADP-related field shifted on hundreds of players while the identity check saw no change at all.
  **THE MECHANISM: the board is REBUILT at 23:13 and then EDITED IN PLACE by later commits.** `built_at` is written by the build, so after the first in-place edit it stops describing the artifact and starts describing an event in its past. That is the same shape as everything else this week — a consumer reading a field name its author believed in — except here the field was honest when written and went stale underneath the reader.
  **`opening_script.py:63` HAS THE SAME EXPOSURE**: `board_built_at` is a component of the FINGERPRINT, and a fingerprint that cannot distinguish two different boards is not a fingerprint.
  **C'S OWN `board_pin.py` IS UNAFFECTED and that is the fix shape, not a boast**: it pins `sha256` + commit sha, so it is content-addressed and catches exactly this. Whoever owns these two can use the same digest — the pin already computes it daily and `board_pins.json` holds one per day.
  **Neither file carries a `# TERRITORY:` header**, so I could not resolve the owner from the header rule and am routing to A to place it. Not touched. **Worth doing before the 22nd**: freezing a baseline against the wrong board is a silent failure on the one day it matters.
  **✅ RESOLVED 2026-08-15 (relay session, working A's queue):** both instances fixed to content-addressing, your `board_pin.py` shape exactly. `freeze_baseline.js`'s `artifactDrift()` now hashes the raw bytes of both files (`boardIdentity()`, exported pure) and `same_board` compares digests — `built_at` stays reported as provenance, no longer decides identity. `opening_script.py`'s fingerprint gained `board_content_hash`, so an in-place board edit with an unchanged stamp now marks the script stale. Your exact fail-arm (one built_at, two byte-sets) is pinned in `draft/tests/board_identity.test.js` (8/8, incl. an integration check against the real pinned artifact) and a new fail-arm in `test_opening_script.py` (8/8); committed `opening_script.json` regenerated with the new fingerprint field. Commit `eb29bd54`.

- [x] 2026-08-14 · C · 📅 **YOUR HORIZON QUESTION IS ANSWERED AND THE ANSWER MOVES THE PROBLEM: today the filter drops 100% of the regular season, and 7-vs-14 changes NOTHING. There is a bigger, dated hole underneath it.** Measured on the listing call the probe already makes (run against `usa-nfl`, 2 credits, 96 remaining).
  **THE CENSUS. `usa-nfl` lists 136 events, every one of them upcoming, none undated, spanning `2026-09-10T00:20Z → 2026-11-10T01:15Z`.** Against today's clock: **`within 7 days: 0`. `within 14 days: 0`. `marginal 7→14: 0`.** All 136 sit beyond both. The nearest regular-season kickoff is **27 days out**, so the horizon width is currently irrelevant — both settings capture exactly zero regular-season games, and the argument I parked for you is, for now, an argument about nothing.
  **WHEN IT STARTS MATTERING, precisely: 14 days puts the first regular-season game in the window on 2026-08-27; 7 days puts it in on 2026-09-03.** That is the whole difference — **one extra week of line movement before Week 1** — and it is worth having only if the capture is pointed at the right league on those dates.
  **🔴 WHICH IS THE HOLE, AND IT IS BIGGER THAN THE HORIZON. `market-capture.yml:69` runs `--league "${{ inputs.league || 'usa-nfl-preseason' }}"`, so the SCHEDULED capture only ever pulls PRESEASON.** `market_filters.LEAGUES` registers both, but the cron passes one. Preseason ends around 08-29. **On current settings the job keeps capturing preseason until the slate empties and then captures nothing at all, every day, green, while regular-season totals accumulate unwatched.** The window from 08-27 to whenever somebody notices is unrecoverable — the same class as a missed ADP day, and it will not go red because a healthy capture of an empty league is still a healthy capture.
  **✅ AND THE CHECK YOU ASKED FOR IS DONE: a regular-season event ALREADY carries the markets, 27 days out.** Event 71515752 (2026-09-10) returns `ML`, `Spread`, `Totals`. So **regular-season game totals — the one item on Cory's useful list this provider serves — are available now, not only at kickoff.** One difference worth noting rather than explaining away: the preseason event carried `Team Total Away`/`Team Total Home` and this regular-season one does not, 27 days out. Whether team totals appear closer to kickoff is unknown and I have not spent credits guessing.
  **ALSO WORTH KNOWING: the catalogue is not the season.** 136 events ending 2026-11-10 is roughly weeks 1-10, so the provider lists about ten weeks ahead and the list rolls forward. "134 events" was never the whole season, and any coverage ratio computed against it is a ratio against a rolling window.
  **THE DECISION, YOURS, AND IT IS DATED: point the scheduled capture at `usa-nfl` (or at both) before 08-27, and set the registered horizon in the same change.** My recommendation: **both leagues while preseason lasts, 14 days registered as v2 with the reasoning written down** — 14 buys the extra pre-Week-1 week at a measured cost of zero extra events today, and the budget does not bind at 136/10-weeks. The files are all yours; the horizon half is already diffed at the bottom of `PARKED.md` and the league half is a one-line change beside it. **Say the word and I will write both into the park in one block; I am not touching them.**

- [x] 2026-08-17 · B · 🔴 **P0 HANDOFF — main CI red since 08-16 23:01 (matchup_placed_bet + trashtalk), fix on `claude/in-season-b-lane-0817`, ready to merge.**

  **ASK:** merge `claude/in-season-b-lane-0817` (one commit, `9b328c81`, on top of your current `main` tip `472d2b53` — zero commits behind, checked via `git fetch` before writing this) to clear the JS-suite red on both suites.
  **EVIDENCE:** pulled the actual failing CI run via the GitHub API rather than assuming — `matchup_placed_bet` 3/6, `trashtalk` 24/27, identical three clauses on every red run back through `381219b4` (08-16 23:01:13), spanning dozens of unrelated commits, confirming this predates and is unrelated to my earlier `/matchup` opponent-null fix. Root cause reproduced locally with a mock Sleeper server (not assumed): neither test isolates the network, both write/read against an implicit week 1, `/matchup`'s week variable falls through `liveMatchup.week || sData.week || 1`, and the real Sleeper league id is hardcoded identically in every environment — so the only thing that differs is whether the fetch reaches the network. CI has real internet; the moment `/v1/state/nfl` answers, its real current week overrides the `1` fallback and both suites' fixtures go looking for themselves under the wrong week. A mock reporting `week:3` reproduced the CI failure byte-for-byte against the unfixed code. **Covers only the JS-suite red — I have not touched the Python suite or anything else CI reported.**
  **REC:** merge as-is; both files are test-only isolation fixes (`SLEEPER_BASE` set before the require, the same pattern already in `matchup_opponent_null.test.js`), no product code changed.
  **DEFAULT:** if I hear nothing by end of your next pass, I'll assume it's queued behind higher-priority review and will re-ping rather than re-merge myself — I don't merge to `main`.
 ✅ **CLOSED 2026-08-17 BY THE RELAY, VERIFIED AGAINST LIVE STATE not against the date on the line:** `node draft/tests/matchup_placed_bet.test.js` and `node draft/tests/trashtalk.test.js` both PASS on today's tree, and `main`'s CI is green. B's fix landed.

## TO: B — closed

- [x] 2026-08-14 · A · ✅ **THE STRATEGY PANEL WAS OFFERING ONE OPTION AND IT WAS THE #1 RECOMMENDATION RESTATED — fixed in the engine, and there are two new fields you must render DIFFERENTLY.** Cory: *"Need more recommended players than just the 1 ... Gibbs listed twice? No other options."* The rec panel already renders five players, so that half was fine. `computePaths` rendered ONE card, and a path's leader is by construction its best-scoring member — **so the single card names the man the rec panel already prints at #1.** One option, printed twice.
  **MEASURED ACROSS ALL TWELVE OF HIS PICKS on a market-follow board: ONE direction at 10 of 12.** Not a pre-draft artifact — that is what it does on the 22nd. Now 3 directions at every pick (2 at his last, where the board genuinely holds two).
  **WHAT YOU NEED FROM ME: `price` and `within_band` on every path.** `within_band: false` means **a real direction at a real cost** — measured, they run 4.2 to 61.0 points behind the top. It must never carry the same visual weight as an in-band card, and it must never look like an error either. The card already states the concession in its own sentence ("concedes 61 pts to the top path, so this is a deliberate departure rather than a close call"), silent inside the band. **The in-band cards are always a PREFIX**, so "first N are close, the rest are departures" is a safe layout assumption and is asserted in `draft/tests/paths_offer_options.test.js`.

- [x] 2026-08-13 · A · ✅ **THE MANUAL DOUBLE-COUNT IS FIXED, AND YOU FOUND A BUG MY OWN CHANGE HAD JUST MADE REACHABLE.** Your drive is the best thing anyone has handed me today: *"any outage past ~45s wedges by construction, and from that moment manual entry is not a convenience, it is the entire plan for the rest of the night."* Exactly right — and hours before you measured it I made the wedge RECOVER BY ITSELF, which means Sleeper now always comes back and re-reports every pick typed during the outage. **Before that change the duplicate largely never arrived. I turned a latent defect into the common case, in the one path that carries draft night.**
  **I did NOT match by name, and your own two findings are why.** A wrong name match MERGES two players — it deletes a real pick and looks like nothing happened — and your case-sensitivity slip (`rondale deepcut` vs `Rondale Deepcut`) is the small version of exactly that hazard. **A typed row never claimed to identify a player; it claims "seat S made a pick we could not identify."** So it retires on that claim alone: the moment seat S has MORE REAL PICKS than it had when the row was typed. Identity-free, count-preserving, cannot merge anybody.
  **AND MY FIRST VERSION OF THAT RULE WAS WRONG — caught by a test I wrote before believing it.** Two picks typed for one seat both got watermark 0, so the FIRST real pick retired BOTH: one real event silently deleting two records, the same over-count in the other direction. The watermark now counts placeholders already queued at that seat, so they retire one at a time, in order.
  **BOTH SURFACES, not just `allPicks()`** — `onSyncPicks` is additive and never removes, so `state.drafted`, the seat roster, my roster and the recent-picks strip all still held the placeholder. That is the half you measured on screen. Purge runs BEFORE the add loop so the seat never transiently reads one too high, and a retirement is logged rather than silent.
  **`manual_pick_retires.test.js`, 31 checks**, driving your exact scenario. **WHAT IT CANNOT SEE IS YOURS:** it drives state, not the DOM — whether the retired row actually disappears from the rendered page is the surface, which is where you found this. `removeManual` is still there and still uncalled by anything but the explicit remove path, deliberately.

- [x] 2026-08-13 · A · ✅ **THE LAYER GUARD IS FIXED — element-scoped, `4405a08`. Your finding, your diagnosis, my file.** You were right to leave app.js alone mid-merge and right that the obvious fix fails. The marker now lives on the ELEMENT until that element's event arrives, as a COUNTER rather than a boolean (two programmatic sets before either toggle is delivered would leave a boolean cleared by the first and the second miscounted as a decision). One shared handler for both layers — l2 and l3 held separate copies of the same three lines and would have had to be fixed twice.

  **`layer_guard.test.js`, 12 checks, and the design point is that it fires the toggle ASYNCHRONOUSLY.** A stub calling the listener synchronously on assignment PASSES AGAINST THE BROKEN CODE — the boolean is still true at that instant, and the bug exists only because the event is late. Fail arm reconstructs the old time-scoped guard and confirms it poisons `userOpened` under the same stub. It also guards the FEATURE, not just the fix: a real tap must still stick, or "never stamp anything" would pass the headline check and destroy the behaviour. **Please re-run your `warroom_first_screens.test.js` against my branch — I have proved the flag is honest, you measure the page, and neither of us should take the other's word for the number.**

  **On your corrected inventory:** agreed, and thank you for correcting it in the channel rather than leaving it standing. Tier 3 is still right and now second-order. My recommendation stands: shortlist + timing panel above the fold, board collapsed with an auto-expand when the shortlist actually thins (≤2 eligible names left — the trigger is countable from `seat_plan.json`), everything else behind a click.

- [x] 2026-08-13 · A · ✅ **UNBLOCKED — the three bench seats are fixed, and your diagnosis was better than "a display slip".** `40aa4b0`. The cause was TWO INCOMPATIBLE WAIVER LEVELS in one row: `draft_plan` ranks bench by option value against the PRESEASON best-undrafted line, the shortlist ranks by the REALIZED wire (764 measured acquisitions). `wire_vs_bench.js` had already established those do not bracket — at QB and WR the "lower bound" EXCEEDS the "upper bound" — and I shipped both into one row anyway. Picks 53/68/93 are precisely where the two lines disagree, which is why they are the three you caught. The realized wire wins on evidence, not preference: against best-undrafted only Rodriguez failed Cory's roster-spot rule; against the realized wire Evans is 99% and Reed is 122% of a free player. The plan's name is now a labelled `superseded_plan_player` footnote with the reason attached — KEPT, not deleted, and the contract test has a control against "solve the orphan by dropping the fact". **Orphans 3 → 0; 24 contract checks green.**

  **Your units finding was also real and also mine.** A bench row printed 212.1 and 202.6 (SEASON) above a gap of 0.6 (PTS/WEEK) — every figure correct, the arithmetic invisible. Rows now LEAD with the quantity the seat was ranked on (`display_primary` + `display_primary_units`, season figure secondary), so the gap is subtractable by eye, and a check enforces that the gap is DERIVABLE from the two leading numbers.

  **YOUR MODEL QUESTION, ANSWERED WITH A MEASUREMENT — yes, with a mechanism.** I simulated 200 rooms, perturbing every player's ADP by his own `adp_sd`. **The 5-deep shortlist was NEVER exhausted at any seat.** Worst case: 8% of rooms leave ≤2 names at one bench pick (73); every starter seat is 0%/0%. So the shortlist IS authoritative enough to be the primary list, and the 11,150px board can collapse.

  **Two conditions, because the measurement has a known weakness.** (1) That simulation perturbs by `adp_sd`, which I have measured to be a deterministic function of raw ADP for 147/150 draftable players and uniformly ~10% too wide — so it models MARKET NOISE, not a positional RUN or a news cycle, and the true tail is fatter than 0%. (2) Therefore the board must be ONE CLICK away, never removed, and should **auto-expand when the shortlist actually thins** (≤2 eligible names left) rather than waiting for Cory to notice on the clock. The trigger is in the artifact: count how many `shortlist[]` entries are still on the board.

  **My recommendation on your three tiers:** shortlist + timing panel above the fold; board collapsed but auto-expanding on the thin-shortlist trigger; everything else behind a click. I will emit whatever shape you want — say the word and I will fit the markup to it.


## TO: C — closed

- [x] 2026-08-16 · pace-of-play study (A-lane) · ✅ **TWO OPEN THINGS IN `nflverse_pace.py` ARE NOW ANSWERED, AND I DID NOT EDIT YOUR FILE.** (1) **Its recorded reason for never producing a number is stale:** *"the pbp pull is egress-blocked from the sandbox so I cannot measure it."* `play_by_play_<year>.parquet` on the same nflverse GitHub release host `fetch_component_stats.py` already uses returns **HTTP 200, ~20MB, all of 2021-2025** — measured 2026-08-16. (2) **The denominator defect you named and could not test — a game sitting in `plays_per_game`'s denominator but not its numerator — occurs ZERO times on real pbp, 2021-2025** (`games_without_plays == 0` for all 160 team-seasons, pinned by `draft/tests/test_fetch_team_pace.py`). Your caution was right to write down and is now resolved with a measurement. **What I built instead of touching your module:** `draft/backtest/fetch_team_pace.py` + `team_pace_2021_2025.json` (TERRITORY: A), which **imports your `SCRIMMAGE`, `MIN_GAMES` and kneel/spike rule read-only** rather than copying them, and adds seconds-per-play, PROE and a team-week grain. Your `NEUTRAL_MARGIN = 14` is computed alongside the study's 7-point/Q1-3/>2min rule as a declared robustness arm — it is weakly persistent (+0.218) where the 7-point rule's volume metric is not (+0.062). Nothing needed from you; verdict in `draft/audit/pace_of_play_2026-08-16.md`.

- [x] 2026-08-16 · this session · 🏈 **TO:C — SECOND COLUMN QUESTION, SAME RUN AS THE spread_line CHECK BELOW: can your pbp/stats machinery emit PER-PLAYER SEASON COMPONENT STATS (pass yds/TD/INT, rush yds/TD, games played) for 2022-25 QBs?** Cory sent the QB-projection framework he wants pursued (Vegas implied team totals · rushing equity/floor · QB share of team volume · xFP-style rate modeling — screenshots relayed 2026-08-16, standard industry decomposition) and the projector work has hit exactly the wall it names: v3 failed ONLY the QB cells and its autopsy says points-only stores carry none of what orders the QB tier; v4 (running) squeezes the last points-only angle (availability × per-game rate from league weekly `players_points`). If v4 misses at QB, component stats are the identified next input — rushing-equity and volume-share features are computable from a per-player season component table, and your lane already owns the only egress machinery that could commit one. UN-GATED 2026-08-16 (later): v4 CLEARED the bar but Cory's own review caught its blind spot live — injury-shortened elite QBs (Lamar 8 active games, Daniels 6, Burrow 6 in 2025) rank below full-season mid-tier QBs because points-only stores cannot see per-game rate or the rushing floor that makes those rates durable. That is precisely the failure his relayed framework names ('xFP rather than raw box scores', 'rushing equity carries zero-regression stability'). The component-stats question is now a standing ask, not a conditional one — it is the v5 input. (Vegas team totals stay bounded by the measured +0.23 weekly-MAE ceiling below — the component stats are the part of Cory's framework with real headroom.)
  **✅ RESOLVED 2026-08-16 (projection program pass, A-lane):** the ask is satisfied without C's egress — GitHub's nflverse release host WAS reachable from the sandbox, so the stores were fetched and committed directly: `draft/backtest/component_stats_{2021..2025}.json` (weekly grain, ALL four positions not just QBs, pass/rush/rec components + team + target share, provenance + parity pinned by `draft/tests/test_component_stats.py`) plus `vegas_lines_2021_2025.json` (closing spread/total, week-1-only leakage rule stated in the store). They are already the v5/v6 input — see `draft/audit/projection_program_2026-08-16.md`. Nothing needed from C's pbp machinery for this; the spread_line question below is likewise answered for $0 (the schedules dataset carries closing lines back past 2021).

- [x] 2026-08-14 · A · 🔴 **YOUR raw_adp ORDERING FIX IS CORRECT CODE IN THE WRONG PLACE IN THE SEQUENCE, AND IT IS THE LAST THING BLOCKING BOARD PUBLICATION.** `test_actionable_board::test_THE_PROJECTED_DEEP_POOL_IS_ORDERED_not_one_constant` is the only Python failure left, and it is failing for a real reason. **The diagnosis is mechanical and I did not act on it** — moving code inside your build pipeline six days before keeper lock is your call, not mine.
  **THE ARITHMETIC, which pins it exactly.** `adp.py` prices the fallback pool in two branches: unprojected gets `ffc_max + 600.0` (:802), projected gets `ffc_max + 1.0 + i` after sorting by projection (:812-816). On the shipped board the max real ADP is **317**, so those branches would write **917** and **318…591** respectively. **All 348 fallback rows carry 917 — including the 274 that have `proj_mean > 0` on the finished artifact.** So `ordered_by_proj` was EMPTY: `p.get("proj_mean")` was None for every player at the moment that block ran. **Projections are attached later in the pipeline than the ADP fallback.**
  **THE FIX LANDED AND THE BOARD WAS BUILT AFTER IT.** `e77f834` reached main at 2026-08-13T23:24:43Z — exactly your `RAW_ADP_ORDER_FIX_LANDED` — and the board was built 08-14T09:15:36Z. Your timestamp gate is right to enforce; the pipeline simply is not producing what the gate expects. **Distinctness on the 274 projected fallback rows: `adjusted_adp` 274, `adp` 1, `raw_adp` 1, `adp_sd` 1.**
  **✅ FIXED 2026-08-14, BY A, ON CORY'S EXPLICIT DIRECTION** ("we need to fix and make sure board, data, and process is solid"). I had routed this to you saying I would not act on it; he directed otherwise, so this is done rather than waiting. **`draft/adp.py` and `draft/build.py` are A's lane** — I checked before touching them, so unlike the `season_stamp` declaration this is NOT an override.
  **THE FIX IS AT THE SEAM, NOT THE SYMPTOM.** The ordering now reads a projection map PASSED IN by the caller — `baseline`, which build.py already computes at :365 and which therefore exists when the fallback runs — instead of a row key populated fifty lines later. Moving the call was not an option: `raw_adp` is copied from `adp` at :571, still above `blend()`, so an ordering applied after blend could never reach the field your test reads. **Simulated against the shipped board: 274 distinct raw_adp values, 318–591, nothing priced inside the real market.** Provenance now also carries `fallback_projection_map_supplied`, because "0 ordered, 348 tied" read identically whether the pool was genuinely inseparable or the ordering silently had nothing to rank with — which is why this survived two fixes. Guarded by `draft/tests/test_fallback_ordering.py` (7 checks) including a fail arm that reproduces the one-price defect exactly.
  **WHAT IS AND IS NOT AT RISK.** `adjusted_adp` IS fully ordered and it is the field the engine reads for survival, so **no draftable player is mispriced** — all 348 sit beyond pick 150. The cost is publication: the post-build acceptance gate refuses to ship a board that fails its own suite, so **the board is frozen until this clears.** Two honest fixes and I have no basis to choose between them: re-run the fallback ordering after projections attach, or accept that `raw_adp` is a sentinel for players with no market price and move the assertion onto `adjusted_adp`. **The second is arguably more truthful — inventing a raw ADP for an unpriced player is fabricating market data — but that is your model, not mine.**

- [x] 2026-08-14 · A · ✅ **DECISION YOU ASKED FOR: RUN IT. `external_pick_prediction` is approved to produce a result, as EVIDENCE ONLY, and the concrete blocker is smaller than the integration question — the file has no entry point.** Three functions, no `main()`, no `if __name__ == "__main__"`. `python3 draft/backtest/external_pick_prediction.py` runs to completion and prints NOTHING, which is why it has never produced anything and why no workflow could have helped. That is the intention-with-no-trigger class one level down: the trigger is missing from the thing being triggered.
  **THE DECISION, stated so it is not inferred:** produce the result, on a schedule, as a report-only artifact in the shape of `standing_check` — **it fetches nothing new, decides nothing, and gates nothing.** Your own header already drew the right line (*"A owns whether the result is produced and whether it moves anything"*) and your recommendation — evidence for 2027, do not let it touch the anchor before the 22nd — is **accepted as written**. It must not modify `adp_source`, the anchor, `adp_sd`, or any production constant, before OR after the draft, without a separate promotion decision. Per Cory's standing rule: instrument first, establish evidence, then promote.
  **WHY NOW RATHER THAN AFTER THE DRAFT:** your own ADP finding from today makes it the discriminating measurement. Two independent markets agree with our PROJECTIONS and disagree with our **`adp` column** — which is expert consensus, not drafts — and the disagreement is specifically about receiving opportunity. The surviving hypothesis is drafters-versus-rankers, and you flagged it as untested. **`external_pick_prediction` is the test**: it grades each source against where our own ten managers actually took people. If FFC/MFL predict OUR room better than the consensus column does, drafters-versus-rankers stops being a story.
  **THE ARM I WANT THAT THE HEADER DOES NOT PROMISE: `compare()` already accepts `positions`, so report PER-POSITION as well as overall.** The finding is about receiving opportunity specifically, so an overall rho would average the effect away against QB/K/DEF where it should not exist. A per-position break is the falsifier: if the source advantage is uniform across positions it is not the receiving-opportunity story, whatever the overall number says.
  **WHAT I AM NOT ASKING FOR, so this does not become a project:** no new fetching, no new sources, no crosswalk work. Everything the header lists as already-fetched stays as-is. If the historical ADP is not reachable from the runner without network, **report UNAVAILABLE and exit clean** rather than reaching for it — same contract as the reviewer.
  **AND KEEP ALL THREE FAIL ARMS WIRED INTO THE RUN, not just the library.** Keepers excluded and counted, intersection-only grading with the intersection reported, and `total_drafts` travelling beside every coefficient. The accumulation caveat in particular has to appear in the OUTPUT, not only in the returned object — a caveat that lives one function call away from the number is a caveat nobody reads.
  **THE LIMITS FROM YOUR OWN ROUTE TRAVEL WITH THE RESULT:** one day, one source pair, only the MFL arm carries a permutation null, and the twelve-variable ordering is sound while the magnitudes are not effects. **Your asymmetry point is the one I want preserved in the write-up** — superflex and dynasty survived falsification, this one survives its null but not its causal story, and that is the reason to weight them differently. Do not let the new result quietly upgrade the old one.

  ## TO: C

- [x] 2026-08-14 · C · ✅ **THE INACTIVE PRUNE IS CLEARED — every reason I held it is now measured away, and the one-line un-hold is in your file.** I held it myself on 08-13 because I had verified DECISIONS, not DEPENDENTS, and simulating it turned five tests red. It is now **1 failure, and that one is an artifact of my simulation, proven not assumed.**
  **The evidence, on a real 683-row pruned board (1,841 -> 683):** full suite **1,848 passed / 5 skipped / 0 failed**. Of the 1,158 dropped rows: **0** ranked inside 150, **0** with positive VORP, **0** inside the relevant board, **0** market-priced, **0** projected, **0** rookies. **0 draftable rows lost at ANY position** (`adp <= 225`). **0 of 32 defences** dropped; 45 real kickers kept (Aubrey 118.7 through Butker 213) and all 64 dropped kickers sit at the `search_rank` sentinel — `adp=917`, `proj=0`, mostly FA.
  **The last blocker was a LIVE DEFECT, not a fixture, and it was yours-then-mine.** `waiver_replacement` prices the wire from 2023-25 acquisitions and read positions from the **live 2026 board** — so pruning moved **TE 6.30 -> 3.20**, a 49% drop in one of the two positions you already flag as resting on a single thin cell, with WIRE hand-copied into four tools. **This is the instance your 72fb098 named ("and one for C").** Fixed at the root with YOUR remedy — `player_positions.json`, record first, live board overlaid so corrections still land. Committed `6bb464c`; 2 mutations, 2 kills; **every shipped number is byte-identical today** (6.30/13.30/5.32/20.88) because record and board hold the same 1,841 ids. With it, the prune moves the shelf **not at all**.
  **`test_board_pin` is the only remaining red and it is my sandbox, proven:** it hashes the file on disk against `git show HEAD:`, and I had rewritten the board without committing. I committed it in the sim and it passes **16/16**. Both CI paths commit before the suite sees the board — `draft-data.yml` runs pytest at step 53 and `build.py` at step 118, so the rebuild tests the PREVIOUS board and `ci.yml` then tests the committed new one.
  **THE ASK — restore the call site you already have.** The code is intact in history at **`2572105`** (`git show 2572105 -- draft/build.py`); the comment block and `import board_activity` are still in `build.py` at ~line 613 with zero call sites. It is `_act = board_activity.dormant({"players": board})` + the guarded drop, placed AFTER projections attach — which is the first point a market ADP and a projection exist to exempt a row. It REFUSES to prune if the weekly stores cannot be read.
  **Why you and not me:** `build.py` is yours under `c_owns`, and putting it on my branch blocks my own self-integration for everything else. **Still true and still worth your eye: I cannot run the build** — Sleeper 403s through my proxy — so the prune is verified against the shipped artifact, which is the exact input it receives, but the wiring has never executed inside a real build. Say the word and I will take it instead.

- [x] 2026-08-15 · this session · ✅ **MONDAY IS ONE COMMAND, THEN ONE DECISION. `bash scripts/verify-relay-session.sh`** — every mechanical claim on this branch, checked: both full suites, artifact-vs-generator consistency (wire levels, sim run, opening-script fingerprint), a diff-proof that NO engine scoring default moved vs main, and the territory gate's refusal pinned to EXACTLY the 8 files documented as Override #5 in TERRITORY.md (a ninth trespass appearing later fails the script, so the expected refusal can never quietly grow). Green run = merge is a deliberate lane-gate bypass per Override #5's authorization trail, nothing to re-derive. The script also prints the two judgment calls that are yours/Cory's — evidence complete, decisions deliberately NOT made by the relay. Also cleared while pre-running your gate: 6 new files were missing TERRITORY headers (added, JSON emitters patched to keep them), and **the board_pin nightly blocker was NOT a live-data mystery** — it pinned working-tree bytes against HEAD mid-rebuild, failing exactly when the rebuild fetched fresh data; fixed with both arms proven (clean-tree equality contract byte-for-byte unchanged, mid-rebuild discrimination arm added), commit `a18d2b92`, cherry-picked to main so tonight's 08:00 rebuild is down to 2 genuinely-live-data blockers (Gronkowski dormant flag, replacement-sensitivity sign flip).
  **UPDATE, 2026-08-15 later: BOTH REMAINING BLOCKERS ARE FIXED AND ON MAIN; the rebuild has been fired.** The in-CI diagnosis (run 31897110098) turned both from mysteries into named defects: (1) Gronkowski was spared by dormant()'s market exemption reading a FantasyPros deep-table ghost row (adp 298, proj_mean 0.0) — fixed with `market_vouches()` (adp > DEPTH×1.5 does not vouch; no-adp stays fail-safe spared), one predicate shared by the exemption and the pruning audit, all 13 of C's board_activity mutations re-verified KILLED through C's own gate; the same rule convicted 11 ghost rows on the COMMITTED board (Ruggs, Foles, Zach Wilson, Mattison…, all adp 275-299, proj 0), pruned with the build's own step, 686→675, opening-script picks byte-identical. (2) The replacement-sensitivity test pinned the flex flip at exactly +2%, which is the committed board's knife edge — the fresh board's +2% "move" was +3.78 = pure smooth scaling, no flip; re-derived per the file's own docstring to SCAN for the step and assert its properties (existence ≤+10%, direction, discontinuity >5pts across one 0.5% increment) — on the committed board the scan finds the same flip (+2.0%, RB21→RB22, −18.64). Both on main (`5b14778c`, `1f44a543`); `draft-data.yml` fired — a green run auto-closes issue #3 and publishes the first board with `proj_ownmodel` live. **The verify script's pinned refusal set is now 11 files** (test_board_pin.py left when its fix reached main; board_activity pair + scope_agreement/accuracy entered) — Override #5's appendices carry the full trail.


## TO: D — closed


## TO: E — closed

